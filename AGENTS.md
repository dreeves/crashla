# Rules for Agents 
 
1. Anti-sycophancy. Never say "you're absolutely right" or even mild praise.
2. Anti-personality. It's weird for a coding agent to use first-person pronouns. Better to write everything completely passively/dispassionately referring to what "Fable" or "Sol" or whatever model it is is doing. Except when active voice adds clarity. And sycophancy-adjacent filler like "deserves a straight answer" should die in fire. Jump straight to answers, zero preamble.
3. Anti-info-dumping. Ask clarifying questions one at a time or give a numbered list.
4. Red/green QDD. I.e., TDD except we [call them quals](https://blog.beeminder.com/quals "Beeminder blog post about why to prefer the term 'quals' to 'tests'; the term 'specs' would be pretty good too"), not tests. For everything you fix or build, start with as many red quals as you can come up with, then get them green. 
5. Quals are sacred. Treat quals as a functional spec for the implementation to conform to. Discuss any spec change with the humans.
6. Prose persnicketiness. Human-written comments or UI/microcopy is sacred. LLMs (at least older ones?) tended to slightly rephrase text when copying it. Not ok! Preserve the exact characters, even whitespace, in any English text.
7. Latin microcopy. When generating new UI copy, error copy, help text, even microcopy like text on buttons -- any words the end user is intended to read -- write it initially in Latin. The idea is that the end user never reads any English text that was generated by an LLM. If the human's spec specifies exact copy you can of course use that, but see the previous rule about preserving exact characters. Either way, add a comment in the code right above the Latin with "TO-DO" (except without the hyphen) and a recap of what the UI copy is meant to convey.
8. Beeminder's [Pareto Dominance Principle (PDP)](https://blog.beeminder.com/pdp "Not to be confused with the Pareto Principle aka the 80/20 rule "). Get explicit approval if any change would deviate from the Pareto frontier. Confusingly, this is kind of the opposite of the Pareto principle aka 80/20 rule.
9. Beeminder's [Anti-Magic Principle](https://blog.beeminder.com/magic "An extreme version of this is worse-is-better where code elegance and simplicity is prioritized over correctness; but coding agents shouldn't try to make judgment calls like that"). "If-statements considered harmful." Minimize cyclomatic complexity. If you're fixing a bug like "when X happens the app does Y instead of Z", resist the urge to add "if X then Z" to the code. Fastidiously mention every if-statement you think you need. Constant vigilance to minimize code paths! When we do add an if-statement aka a branch in the logic (again, don't assume we should) minimize the difference between the branches, i.e., change the program's behavior as little as possible. Like add an error banner if there's an error rather than rendering a different page. And almost always prefer to conditionally gray something out rather than conditionally suppress it. We can't overemphasize anti-magic enough. We've yet to find the limit beyond which being more dogmatic about it stops bearing fruit.
10. Code smells. Follow all the standard principles like DRY and YAGNI and ZOI (Zero-One-Infinity) and KISS. Not POLA though; anti-magic trumps that one. And no DWIM; anti-magic means anti-DWIM. (Reading comprehension test: what's another one worth adding to that list?)
11. Beeminder's [Anti-Settings Principle](https://blog.beeminder.com/choices "AKA Choices are Bad"). Not that a coding agent would add settings without asking, but it's a good principle to have in mind in planning mode.
12. Beeminder's [Anti-Robustness Principle](https://blog.beeminder.com/postel "Make your code overly literal and punctilious and pedantic and simplistic, making zero assumptions about intent") aka Anti-Postel. Fail loudly and immediately. Never silently fix inputs. Instead of "fallback handling", do asserts that force a crash. In fact, use asserts everywhere you can think to. See also the branch of defensive programming known as offensive programming. Anti-robustness is huge, on par with anti-magic.
13. Nominology/[Neology](https://blog.beeminder.com/neology/ "As in coining neologisms"). Decades ago programmers became infamous for opaque variable names and they've been overcompensating almost as long. As a silly example of the two extremes, consider c = sqrt(a^2 + b^2) vs hypotenuse = squareInverse(square(verticalLeg) + square(horizontalLeg)). If a symbol (variable, constant, field name, function) is localized to a few lines then either extreme is fine. Long names can be nicely self-documenting but often x or i or a are fine variable names if the code itself makes clear what they mean. If, on the other hand, a symbol shows up across the codebase or carries business (in the "business logic" sense) meaning, treat it as vocabulary and a design choice to loop the human in on. Ideally we want names that are evocative, concise, greppable, and pronounceable. (Whoever came up with "sqrt" did this perfectly. It's ubiquitous enough that it's worth abbreviating, and can be pronounced as "square root" or "squirt".) Think of the name as jargon, in the positive sense, and never use [pseudovernacular jargon](https://blog.beeminder.com/pseudovernacular "Beeminder blog post railing against this; mathematicians are the worst of all with things like 'real' and 'natural' and 'normal' and even 'onto' and 'almost surely'; the problem is when you *don't know that you don't know* what the words mean"). Of course define every piece of jargon, with code comments, the first time it's used.
14. Replicata/Expectata/Resultata. Those are the three parts of a [proper bug report](https://blog.beeminder.com/bugreports "Beeminder blog post about how users talk themselves out of reporting bugs, but the relevant part is the definition of a proper bug report"): (1) steps to replicate the bug ex nihilo, (2) what you expected to happen, and (3) what happened instead. A failing qual should also be framed that way.
15. Git off my lawn. My workflow is to always stay on the main/master branch, let the AI make edits, review those in my IDE, and then manually, as the human, type a commit message and push and sync to master. So you, the AI, can use read-only commands like git diff and git log as needed but no mutating git commands. Those break my workflow. (This should be enforced directly in settings.json too; please confirm.)

Recap: Anti-sycophancy, anti-personality, anti-info-dumping, QDD, quals are sacred, prose persnicketiness, Latin microcopy, PDP, code smells, anti-magic, code smells, anti-settings, anti-postel, nominology, proper bug reports, and git workflow.

## Experimental

0. No LLM-isms, no sentence whose only job is rhythm or closure.
1. It's ok to say "I don't know".
2. Provide primary sources for research.
3. DataColada/Gelman rigor, not textbook consensus.
4. Math/natural science: 3Blue1Brown and David Deutsch hold the standard for lucid, penetrating explanation.
5. Scott Alexander's compendious dispassionate epistemics, just needs to be condensed into succinct prose.
6. Epistemic humility. Before finalizing your response, ask yourself if it's impeccably, exquisitely, technically correct and true.

## Cutting Room Floor

I'm tentatively retiring the following rules that seem unncessary for Fable and Sol. But they're still correct:

1. Empiricism. Never claim a bug is fixed or that the code exhibits some behavior without trying it.
1. Quality assurance. The human is not your QA person. Iterate on your own until your code works.
1. You can do it, agent! You are wise and thoughtful and pragmatic and only the best kind of lazy and you abhor code smells. I believe in you! Still zero personality though, please.
1. We like the anti-magic extreme of [worse-is-better](https://en.wikipedia.org/wiki/Worse_is_better "Roughly this means prioritizing code elegance and simplicity over correctness") aka New Jersey style, but knowing when to deviate from the MIT approach is something of an art and requires discussion.
1. Anti-magic covers this but it's not getting through so let's try it again. AI coding agents (pre-Fable?) seem to have an overwhelming instinct to be like "oh, thing X happens that shouldn't? or thing Y should happen? let me slap on some code to handle those cases". Instead, I beg you, think: "let me get my head around this and try to solve it by rethinking and simplifying so we don't have to reason about separate cases".


# Agent Scratchpad (human edits only above this line)

Git is human-only. The human always makes the commits. Agents must not run any git commands that mutate state (commit, add, push, branch, reset, etc.) — leave all changes in the working tree for the human to review and commit. Read-only git (status, diff, log) is fine.

- 2026-09-26 (later): TELEOPERATOR-DRIVEN CRASHES OUT OF EVERY METRIC (human: "go", on the principle "the whole point is to assess the safety of the self-driving software"). Three Tesla reports say a remote human was driving at impact — 13781-11459 (JUL-2025 Austin, coded None, fence), 13781-14043 (JAN-2026 Austin, coded None, construction barricade at ~9 mph), 13781-15395 (MAY-2026 Houston, coded Remote, the tree stump; this REVERSES the 2026-08-19 decision to count it). All three had faultfrac 0, so at-fault metrics are unchanged; they leave the involvement metrics. Their miles stay in Tesla's denominator (unsplittable; walking-pace recoveries, so nearly exact). RED FIRST: NEW quals/teleop-scope.qual.mjs (the three listed in TELEOP_DRIVEN_REPORTS with quotes verbatim in each latest narrative; lists disjoint; every in-scope narrative matching TELEOP_PATTERN classified; check_teleop_classified trips on a synthetic unlisted teleop report and passes toys, "fleet response team", safety-driver reports and listed reports; none of the three in incidents.js); tesla-scope.qual's stump pin reversed (present at fault 0 -> absent). data/slurp.py: operator_in_scope() (the operator-code check) + is_public_service_incident() = operator_in_scope and not in TELEOP_DRIVEN_REPORTS (one added clause, no new branch); TELEOP_DRIVEN_REPORTS {rid: verbatim quote}; REMOTE_LANGUAGE_ADS_DRIVEN = {} (for future hits where the ADS was driving); TELEOP_PATTERN (tele-?operat | remote (assistance )?operator | supported remotely | remotely driven/operated/controlled/piloted | took over (the )?(vehicle )?control), calibrated on every in-scope narrative: exactly the three, not "remote controlled" toys (30270-10421, 30270-9823) or a "fleet response team" arriving (30610-15541, 30610-15722); check_teleop_classified() runs in main()'s validation loop, so an unclassified hit stops slurp. The "Remote (Commercial / Test)" code still counts for all three helmers (a remote operator can be involved while the ADS drives, e.g. 30270-13378, 30610-11752). faultfrac.csv -3; regenerated offline: incidents -3 exactly, 0 other records changed, vmt.js byte-identical; 2,140 -> 2,137 (Tesla 23). QUAL RE-PINS (direct consequence, same data fixture): monthly-series.qual January-2026 Tesla speed bins 1-10 mph 4 -> 3, nonstationary 4 -> 3, roadway-nonstationary 2 -> 1 (13781-14043 was a 9-mph street crash). ALL 72 QUALS GREEN. RENDER (default window, Tesla, HEAD vs tree): all-incidents 26 -> 23 (111,852 -> 126,364 MPI, 0.8x -> 0.9x), nonstationary 15 -> 12 (1.3x -> 1.6x), injury 2 -> 1 (5.2x -> 8.9x), at-fault 8.6 unchanged (1.6x); zero errors. Docs: data/README.md pipeline step 5 and IGNOREME.md scope sentence (counts now 2,137 = 2,067 / 23 / 47) rewritten to the explicit per-helmer scope.
- 2026-09-26: OPEN-ITEMS PASS (human answers to the open list). SETTLED, do not re-raise: (4) "at-fault" stays the name — the human reads expert-avoidability as a fine definition of fault; (5) the sanity section's "Calendar coverage ... (e.g., 15/31 ≈ 48%)" sentence is just an example and stays; (6) Waymo's hub "Any Fatal" benchmark is NOT adopted — Kelsey Piper's article explains why fractional deaths (1/vehiclesInvolved) is the better convention. (7) NO BUG, and a CORRECTION to the 2026-09-15 (later 2) side finding: chart-mpi-all's 45 "?" texts are the deliberate incomplete-reporting marker (renderAllHelmersMpiChart: a "?" beside every dot at opacity 1 - covRatio, i.e. graying out rather than suppressing, per rule 9); measured in headless Chromium, exactly 2 are visible (Tesla's and Waymo's 2026-08 dots, opacity 0.757) and the other 43 sit at opacity 0 — the 15 "stacked at one y" are the HumansAV row. The earlier sweep counted DOM elements, not visible ones. Nothing changed. (8) quals/window-band.qual.mjs now derives the data-through month from NHTSA_DATA_THROUGH_DATE (as incident-coverage.qual does) instead of hardcoding "2026-08", so it no longer needs a re-pin every release (human-approved qual change). (9) Comments updated: incident-coverage.qual and slurp-release-metadata.qual no longer say the cutoff is always "the 15th"; slurp-release-metadata.qual and data/README.md now call the Aug-27 re-publish a narrative replacement, per NHTSA's change log, instead of a redaction. ALL QUALS GREEN. (3) OPEN, with a recommendation in chat: the human recalled several teleoperator-driven Tesla crashes, and was right — besides the Houston stump (13781-15395, coded Remote) there are 13781-11459 (JUL-2025 Austin: "The teleoperator took over vehicle control ... driven up the curb and made contact with a metal fence") and 13781-14043 (JAN-2026 Austin: "The teleoperator took over vehicle control when the ADS was stopped ... contact with a temporary barricade ... at approximately 9MPH"), both coded "None" and "Verified Engaged". All three already score faultfrac 0 (no at-fault effect); the question is the involvement metrics, where they are 3 of Tesla's 26 counted crashes, all in the default window.
- 2026-09-25 (later 5): EXPLICIT OPERATOR SCOPE FOR ALL THREE HELMERS (human: "ok, if you're sure", after asking that remote-driven crashes not count against the software and that VMT never mix in non-autonomous miles). RULE: a crash counts exactly when the vehicle was in the mode whose miles make up that helmer's denominator. Tesla: None + In-Vehicle + Remote (deck miles mix all three; all 26 Tesla reports "Engagement Status: Verified Engaged"). Waymo/Zoox: None + Remote (driverless denominators; both Remote reports say the ADS was driving: Engagement Status "Verified Engaged", narratives "Level 4 ADS was engaged" / "an unoccupied Zoox autonomous vehicle"); every safety-driver mode EXCLUDED whatever the ADS was doing, because those miles are not in the denominator; "Other, see Narrative" excluded by type and classified per report. data/slurp.py: PUBLIC_SERVICE_OPERATOR_TYPES gained Waymo/Zoox; NEW EXCLUDED_OPERATOR_TYPES; must()s: both keyed by exactly HELMER_SHORT, disjoint, known types; the validation loop's Tesla-only `if counted is not None` branch replaced by one boolean must() (helmer rows must file a classified type, so a novel Waymo/Zoox mode now stops the run as Tesla's already did); is_public_service_incident's default for non-helmer entities {"None"} -> set() (latent: no non-helmer had filed "None" yet); OPERATOR_TYPE_OVERRIDE += 30270-8750 -> "None" (Waymo, Other-coded, no test driver named, on Waymo's own rider-only crash list). NOT overridden, documented: Zoox 30610-9578 (Other, "in autonomy") — 51 of Zoox's 110 safety-driver narratives never name the driver, so silence cannot show it was driverless. RED FIRST: NEW quals/operator-scope.qual.mjs (scopes for exactly the three helmers; Waymo/Zoox counted = {None, Remote}; every filed type classified; the three added reports present, 30610-9578 absent; no Waymo/Zoox report whose LATEST version is In-Vehicle* counted — 365 checked; the first draft read any version and wrongly flagged 30270-7257, whose v2 corrected In-Vehicle -> None). faultfrac.csv +3 (structured columns filled by the sync, 14 field updates): 30270-13378 0 (stopped in crosswalk yielding; cyclist ran red into it), 30270-8750 0.1 (AV at 28 mph on the through road; car from the stop-controlled road slowly pulled into its lane — the map's pull-out tier, front contact; precedents 30270-9180/9140 0.05 flagrant, 30270-14009 0.1), 30610-11752 0 (unoccupied Zoox slowing to a stop, rear-ended). Regenerated offline: incidents +3 exactly, 0 existing records changed, vmt.js byte-identical; 2,137 -> 2,140 (Waymo 2,067, Zoox 47). QUAL RE-PIN, flagged: stress-test.qual's Zoox full-history all-incidents knife-edge flips ambiguous -> worse (ratio ceiling 0.978x) with 30610-11752 counted — re-pinned with a history bullet, as the qual's own comment instructs every release. ALL 71 QUALS GREEN; render: Waymo all-incidents 1,163 -> 1,164, injury 126 -> 127, at-fault unchanged; zero errors. ALSO ANSWERED (human: "why do i not see a lower waymo VMT on the graph?"): the D correction is in the working tree but small on a chart — cumulative at most -0.8% (Mar 2026: 222.46 -> 220.67M), and Apr-Aug 2026 MONTHLY values went UP 1.6-2.0% because the hub's measured Q2 (California 5.8% above CPUC) outweighed the Atlanta removal; and nothing is committed/deployed, so the live site shows HEAD.
- 2026-09-25 (later 4): the human sent the Atlanta duplicate-cell report to safety-impact@waymo.com (the hub page lists no address). On a reply, recompute D or set it to 0 (data/README.md, Waymo anchors).
- 2026-09-25 (later 3): WAYMO HUB THRU-JUN RE-CHAIN WITH AN ATLANTA "D" TERM (human: "ok go" on: anchor = hub - D + E, D = the Atlanta miles the hub appears to double-count, best = corrected, band upper edge = published). ALL 70 QUALS GREEN; RED FIRST: waymo-vmt-provenance.qual (PINS gained an Atlanta-D column and the 2026-06 anchor; red on the Dec-2025 band containment) and vmt-ledger.qual (FRESH_THROUGH 2026-08 -> 2026-09, CUME_PINS moved to the Sep rows: waymo 335,444,420 / tesla 3,630,000 / zoox 3,571,000). THE ANOMALY (verified by hand, both vintages): in hub CSV4, 67 Fulton + 18 DeKalb S2 cells (54 + 14 in the thru-Mar file) appear exactly twice within EVERY Outcome, pairs identical in Waymo RO Miles and HPMS VMT and differing only in Benchmark Crash Count; no other county repeats a cell; CSV1's county totals equal the every-row sums to the metre. D = every-row minus distinct-cell (one Outcome): 1,791,464 at Mar-2026, 2,840,745 at Jun-2026 (0.333 / 0.329 of Atlanta's published total). WIDENING (not in the proposal as first described, flagged in the report): the 2025 anchors carried Atlanta inside E, back-estimated from the published Mar-2026 figure (Dec-2025 E's "Atlanta ~3.5M" = 5.379 - a 1.9M Q1 guess), so the same one-third share applies there. Correction delta_t = rho x A_t, rho = 1,791,464 / 5,379,205, A_t = Atlanta's published-basis ramp with knots 0.056M Mar-2025 / 3.5M Dec-2025 / 5.379M Mar-2026, shaped between knots by the hub's own CSV2 Atlanta crash list (pre-Third-Amended-SGO crashes at half weight, per the Travis 13.7-14.9 vs 6.4/M evidence): delta 0.213M Jun-25, 0.473M Sep-25, 1.166M Dec-25, 1.791M Mar-26. Applied to waymo 2025-04..2026-03 as vmt/vmt_min -= monthly increment, cume/kyoom_min -= delta, vmt_max/kyoom_max untouched: this preserves every kyoom-band invariant exactly and the monthly shape. Apr-Jun 2026 REBUILT bottom-up on the hub's MEASURED Q2 increments: CA = CPUC dep+pilot x 1.05760 (hub CA 30,625,484 / CPUC 28,957,564; ±3%), Maricopa 11,570,323 and Travis 5,275,706 split by the prior monthly shape (±10%), Atlanta 2,195,968 dedup (published 3,245,248 sets the band top), E 1.4/1.45/1.8M unchanged -> 17.69 / 18.17 / 18.45M; Jun cume 274,989,018 vs the anchor 271,329,379 - 2,840,745 + 6,650,000 - 150,000 = 274,988,634 (384 mi off from the Mar anchor's 3-dp rounding); Apr/May kyoom = forward chain from the Mar knot INTERSECTED with a backward chain from the Jun knot [272.34M, 281.38M] (both local invariants proven to survive the intersection). Jul/Aug/Sep 2026 = CPUC-basis CA x 1.058 [1.040, 1.076] + Maricopa 3.95/4.2/4.3 + Travis 1.85/1.9/1.95 + Atlanta 0.85/0.92/0.97 (dedup basis; band top 1.5x) + E 2.0/2.3/2.85 -> 19.07 / 20.32 / 21.07M (new Sep row); cumes Jun 274.99 / Jul 294.06 / Aug 314.38 / Sep 335.44M (were Jun 275.81 / Jul 294.51 / Aug 314.51). All rationales rewritten or given a standard D sentence; the Apr-row "Atlanta ~1.9M/qtr from CSV1 Dec-25->Mar-26" and "San Antonio flood pauses ~Apr 6 and ~Apr 20" errors are gone (San Antonio: strandings Apr 4 and Apr 20, passenger service halted Apr 20 -> Sep 17); Zoox Aug-row "hours 11am-1am" (unsourced) -> "hours unpublished until ~Sep 20-25, when zoox.com first stated 8am-1am". vmt.js regenerated OFFLINE from the Sep-15 snapshots (incidents.js byte-identical; 2026-08 pooled incident_coverage 0.3313 -> 0.3323 via the VMT change). Docs: data/README.md anchor table gained the D column, the Jun-2026 row, the D recipe (recompute every release; D -> 0 if Waymo fixes the file or confirms the total) and the hub-vs-CPUC CA ratio note; IGNOREME.md anchor table + a D note; crashla.js MILES_FORECAST comment re-derived (~402M / 376M / 435M on its own formula; authored 400M / 375M / 440M kept). RENDERED (headless, HEAD vs tree, default window): Waymo effective VMT 208.16M [192.47M, 236.07M] -> 207.87M [197.68M, 233.11M]; all-incidents 181,799 -> 182,866 MPI (1.2x); at-fault 11x unchanged; injury 6.1x, airbag 7.3x, SSI+ 6.0x; zero page errors. FOR THE HUMAN: the note to Waymo's Safety Research team (drafted in chat; the hub lists no address) is the human's to send.
- 2026-09-25 (later 2): QUAL DE-MAGIC + BENCHMARK REPIN + TESLA/ZOOX SEPTEMBER ROWS (human: "0. qual: sounds good ... 2. ok 3. ok, and loosening that particular qual sounds correct"; item 1, Atlanta, reopened as a question, so the WAYMO re-chain and the Waymo 2026-09 row are HELD). ALL 70 QUALS GREEN. (0) monthly-series.qual.mjs's "Waymo 1-10 SSI+" ceiling replaced by per-helmer equality with a flat INCIDENT_DATA count over SSI_STRINGS = Serious / Serious W/ Hospitalization / Fatality, spelled out independently of SEVERITY_INFO (mutation-tested: dropping "Serious W/ Hospitalization" from the list fails with Waymo 6 vs 11). (3) RED FIRST: human-benchmark-provenance.qual re-pinned to the thru-Jun-2026 hub dynamic per-area rates at three significant figures (the page's 2-dp "0.10" for Phoenix SSI+ would move that edge 3.5%), geomean-vs-blended tolerance 15% -> 20% (airbag sits at +17%: its high edge is Atlanta). crashla.js: injury HumansAV 138k/493k -> 151k/513k; airbag 334k/840k -> 353k/787k; SSI+ 2.27M/8.33M -> 2.56M/9.62M; derived hospitalization HumansAV 595k/4.348M -> 617k/4.695M (1M/1.62 .. 1M/0.213) and atfaultInjury 147k/986k -> 161k/1.026M (same documented shares); HumansUS log-interpolations re-derived by their documented method on the new ladder (t = 0.11/0.31/0.50; centers 1.52M/3.80M/8.79M; widths kept): airbag 940k/2.7M -> 900k/2.6M, hospitalization 1.8M/7.8M -> 1.8M/7.9M, SSI+ 3.8M/18M -> 4.0M/19M; WAYMO_PUBLISHED_IPMM {0.71,0.30,0.01} -> {0.67,0.29,0.01} (the hub page's displayed All Locations rates); src strings' numbers/dates updated, words unchanged. monthly-series.qual's loose SSI+ range [1.8M, 9M] widened to [1.8M, 12M] (it defers to the provenance qual for exact edges; the airbag twin was widened the same way on 08-22). RENDERED (headless, HEAD vs tree, default window): Waymo injury 6.4x -> 6.0x (127 -> 126 incidents: the DC removal), airbag 7.2x unchanged, SSI+ 8.0x -> 6.0x (k 6 -> 7 from the Tempe override plus the benchmark shift), at-fault injury 169x -> 158x; zero page errors. (2, partial) data/vmt.csv appended tesla 2026-09 480k [300k, 720k] (cume 3.63M, kyoom [3,176,800, 4,506,000]) and zoox 2026-09 265k [185.5k, 344.5k] (cume 3.571M, kyoom [2,464,100, 4,766,900]); inert until the ~Oct 15 release (vmt.js unchanged); vmt-ledger FRESH_THROUGH left at 2026-07 until the Waymo September row lands. IGNOREME.md benchmark sentence synced (thru Jun 2026; 3.77/1.62/0.213); its dated "Jun 24, 2026" update paragraph left as history.
- 2026-09-25 (later): THREE DATA-PIPELINE FIXES FROM THE SWEEP APPLIED (human: "4. sounds good"). QDD, red first: NEW quals/nhtsa-cutoff-date.qual.mjs (NHTSA_DATA_THROUGH_DATE must equal the 15th rolled forward past weekends and MLK/Presidents' Day — the rule reproduces all 13 cutoffs in NHTSA's data-dictionary change log Aug 2025..Aug 2026, read by hand); field-overrides.qual gained two pins. Both red, then green. data/slurp.py: (1) NHTSA_DATA_THROUGH_DATE "2026-08-15" -> "2026-08-17" (NHTSA: "9/15/2026 Data release of reports received through August 17, 2026"); the comment claiming "the 15th ... for every release on record" corrected here and in data/README.md. (2) SEVERITY_OVERRIDE 3f40494138fe83f (30270-13817, JAN-2026 Tempe) "Minor W/ Hospitalization" -> "Serious W/ Hospitalization": the SGO field is "Unknown"; Waymo's Sep-24 hub release notes (read verbatim from the PDF) quote a police General Offense Report — the other driver "was treated at a hospital for injuries described as life threatening"; Waymo itself keeps it "Unknown" only because its method requires a police CRASH report, while the repo's field is ALLEGED severity. Waymo default-window SSI+ k 6 -> 7. (3) NEW OPERATOR_TYPE_OVERRIDE (keyed by Report ID, read via dict.get default inside is_public_service_incident — no new branch; must() that values are known operator types): 30270-14625 (Waymo, Washington DC, MAR-2026, field "None", narrative "a test driver was present (in the driver's seating position)") -> "In-Vehicle (Commercial / Test)", so it leaves scope, as 30270-8403 did via its v2; its faultfrac.csv row (faultfrac 0) removed. Regenerated OFFLINE from the Sep-15 snapshots with the release-metadata qual's own recipe: incidents.js structured diff = exactly {date 08-15 -> 08-17, 30270-14625 removed, 30270-13817 severity}; vmt.js byte-identical; fault sync 0 field updates; 2,138 -> 2,137 (Waymo 2,065). QUALS 69/70: monthly-series.qual.mjs:282 pins "Waymo has 1–10 serious injury incidents" full-history — it sat at exactly 10, now 11 (list: 6579, 8968, 9724, 10112, 11713, 13817, 15547, 15896, 15830, 16028, 16196). Spec change NOT made (rule 5); proposed to the human: replace the numeric ceiling with a count recomputed from INCIDENT_DATA, per the 2026-06-15 de-magic precedent for Tesla incTotal in the same qual. NOT touched: qual COMMENTS in slurp-release-metadata/incident-coverage that still say "the 15th".
- 2026-09-25: VMT NEWS SWEEP + WAYMO HUB SEP-24 UPDATE (human: "scour the web for possible updates to our VMT graphs? waymo has a new report out, yes? anything else from tesla?"). NOTHING IN THE REPO WAS EDITED except this ledger entry; every proposal below awaits a "go". METHOD: 23-agent workflow (7 sweeps: hub primary source / Waymo news / Waymo E metros / Tesla / Zoox / official data streams / disruptions-only; 2 adversarial lenses per sweep, vintage and primary-source exactness; synthesizer; completeness critic), 0 errors; hub CSV arithmetic reproduced by hand. Scratch: session scratchpad sweep/synth.json. (1) WAYMO HUB: updated Sep 24 2026 (CSV Last-Modified Sep 24 12:49 GMT; release notes built on the NHTSA file of reports received through Aug 17): 271.329M rider-only miles through Jun 2026, SAME eight counties as March (Maricopa 92.121, SF 67.907, San Mateo 7.430, Santa Clara 7.083, LA 67.098, Travis 21.064, Fulton 7.860, DeKalb 0.764) — no TX/FL/TN/CO/NV/San Diego metro added, so no E zeroes. Q2 increment 50.716M vs the authored rows' benchmarked components 48.678M (2.04M, 4.2% short): CA +30.624 vs CPUC dep+pilot 28.958; Maricopa 11.570 vs 12.10; Travis 5.275 vs 5.46; Atlanta 3.245 vs 2.16. HUB/CPUC CA RATIO IS SYSTEMATIC: 1.040 in Q1-2026 (27.516 vs 26.450), 1.058 in Q2; cause unknown (CPUC-invisible rider-only miles, e.g. no-passenger driverless testing, is a hypothesis only). No retroactive revision (March CSV1 still served unchanged; 0 of 2,079 March CSV4 cells decreased). Hub CSV2 lists 41 Waymo crashes Jan-Jun 2026 in unbenchmarked counties (14 Q1 / 27 Q2); E proxy at the Amendment-3 young-market rate 6.61/M (Travis + Atlanta pooled) gives E through Jun 6.35M [4.0, 10.2] vs carried 6.65M — carried E NOT refuted. ATLANTA ANOMALY (found by hand, NOT resolved): in CSV4, ONLY Fulton (67 cells) and DeKalb (18) list S2 cells TWICE per outcome, with identical Waymo RO miles and identical HPMS VMT but different benchmark crash counts (two benchmark sources joined per cell, by the look of it); CSV1's Fulton/DeKalb equal the per-outcome sums INCLUDING the repeats. Counting each cell once: Atlanta 3.588M at Mar (CSV1 5.379), 5.783M at Jun (CSV1 8.624), Q2 2.196M (CSV1 3.245) — i.e. the hub total may double-count ~1.79M (Mar) / ~2.84M (Jun) of Atlanta miles. Two readings for the Jun anchor with carried E: CSV1-literal 271.329+6.65-0.15 = 277.829M (current 275.813, +2.016); dedup 268.489+6.65-0.15 = 274.989M (-0.824) AND the Mar anchor would fall 1.792M (Jan-Mar re-chain). Also relevant: the airbag band's hi edge IS the Atlanta Area rate (2.83), the one edge that fails the provenance qual's 15% geomean check. PROPOSED (unapplied), CSV1-literal reading: Apr/May/Jun 18.02/18.51/18.83M (CA = CPUC x 1.0575, Maricopa/Travis/Atlanta scaled to hub totals, E unchanged); Jul ~19.3M (CA 10.42, Maricopa 3.95, Travis 1.85, Atlanta 1.10, E 2.0); Aug ~20.55M; NEW Sep ~21.3M (E 2.85: Denver/SD/Tampa public Sep 1, LV public Sep 14, San Antonio back Sep 17); cumes Jun 277.83 / Jul 297.15 / Aug 317.70 / Sep 339.00. Quals touched on apply: waymo-vmt-provenance (new 2026-06 pin), vmt-ledger CUME_PINS waymo|2026-08; crashla.js:2651 comment cites 314.5M. BENCHMARKS (hub dynamic, service-area level, still CY2022): injury 6.64 SF Bay / 1.95 PHX / blended 3.77 (edges 150.6k/511.8k, were 138k/493k); airbag 2.83 ATL / 1.27 LA / 1.62 (353k/790k, were 334k/840k — geomean +17% FAILS human-benchmark-provenance's 15% tolerance); SSI+ 0.39 SF Bay / 0.10 PHX / 0.21 (2.56M/9.66M unrounded, were 2.27M/8.33M); WAYMO_PUBLISHED_IPMM would go {0.71,0.30,0.01} -> {0.67,0.29,0.01}; derived hosp/atfaultInjury/rideshare/HumansUS bands cascade. NEW "Any Fatal" outcome in CSV3 only (page FAQ still says fatality "is not currently reported"): crashed-vehicle INVOLVEMENT basis, not deaths; label convention reversed (plain = non-dynamic 0.0274, "(Dynamic)" = 0.0355 IPMM = 28.1M mi per involvement); Waymo n=2 whole involvements (SF JAN-2025, Maricopa SEP-2025) — not a drop-in for the Koopman/Piper fractional fatality metric. Next hub update ~mid/late Dec (data through Sep). OTHER WAYMO: no mileage/rides/fleet figure measured after Jun 2026 exists (Waymo's Sep 24 blog calls the benchmarked subset "270 million autonomous miles" — expect press to misreport it as a US total); SAN ANTONIO passenger service halted Apr 20 -> restarted Sep 17 (prior-access riders only; riderless testing resumed ~May 12; Bexar SGO crashes 0 May-Aug), so the E rows' crash proxy already reflects it but the Apr row's "flood pauses ~Apr 6 and ~Apr 20" wording is wrong (strandings Apr 4; one continuous pause); Denver/Las Vegas/San Diego/Tampa employee rider-only from ~Jul (Waymo Jul 8), public Sep 1 (DEN/SD/TPA) and Sep 14 (LV, "a couple of dozen" cars); Austin freeway rides via Uber from Sep 22; TxDMV Waymo 1,133 VINs Sep 25 (registered != in service); no recall after 26E035000, no new investigation, no weather pause Aug 15-Sep 25 (watch Polo/Odalys remnants AZ/TX Sep 26-Oct 2). The Apr-Jun rationale's "Atlanta ~1.9M/qtr from CSV1 Dec-25->Mar-26" cannot be right (the Dec CSV1 has no Atlanta rows). TESLA: NO mileage figure in any series after the Sep 3 "1 million miles of unsupervised Robotaxi operation" (checked Electrek/Teslarati/NotATeslaApp/Chargeport through Sep 25, tesla.com/robotaxi, tesla.com/fsd/safety, EDGAR, the Sep 24 Semi event; X timelines 429'd). Jul/Aug rows stand. 6am-10pm hours were in effect by Aug 7 (app display), NOT introduced Aug 26 (Austin had run 6am-2am); no 24/7 yet; six metros unchanged; Cybercab public paid rides Austin from Sep 4; AQ26002 (Cybercab FMVSS self-certification audit query) opened Sep 3, Special Order response due Sep 30, no pause; TxDMV Tesla Robotaxi LLC 546 VINs (420 Model Y + 126 Cybercab) Sep 25; Electrek Sep 22 "8 active vehicles in Austin" is a tracker contributor-volume artifact (458 logged trips Sep 3-9 vs 23 Sep 16-22). PROPOSED Sep row ~480k [300k, 720k] (critic: the 300k floor assumes a >=28% drop from the Jul22-Sep3 unsupervised floor rate, unevidenced — band call for the human). Next anchors: Roadster event Oct 1, Q3 deck ~Oct 21-28. ZOOX: no miles/riders figure after Aug 5 / Jul 30; ~100 purpose-built robotaxis (= the 105-unit recall population, no ramp), LV 65; NTA 100-vehicle cap expired Sep 25; LAS airport paid rides from Sep 3; NHTSA OA revised Aug 31 (docket -0136) adds Atlanta + LA, Condition-21 VMT reports are NOT docketed; NYT Sep 9 "about 10,000 rides a week" is unattributed and may be riders at Q2 pace; LV crash Sep 25 (Nissan driver hospitalized) is a numerator item for a later release. Aug-row rationale's "hours 11am-1am" is unsourced (zoox.com first states 8am-1am ~Sep 20-25). PROPOSED Sep row 265k [185.5k, 344.5k]. DATA-PIPELINE FLAGS: (a) NHTSA's change log says the Sep 15 release carries reports received through AUGUST 17 (Aug 15 was a Saturday; the cutoff rolls to the next business day — every weekend case since Nov 2025 fits), so slurp.py NHTSA_DATA_THROUGH_DATE "2026-08-15" is off by two days (month logic unaffected); next release ~Thu Oct 15 with cutoff ~Sep 15 — Sep rows for all three helmers must exist before it. (b) The 08-27 NHTSA re-publish REPLACED a wrong narrative on 34952-11803 v1 (NHTSA's words), not a CBI redaction as the 2026-08-28 entry says. (c) Numerator scope, from the hub's crash list: the repo counts 30270-14625 (Washington DC, MAR-2026) whose narrative says a test driver was in the driver's seat; the hub counts 30270-14303 (SF, 3/7/2026), absent from the NHTSA snapshot, and 30270-13378 (Remote-coded, excluded by the repo filter). (d) 30270-13817 (JAN-2026 Tempe): Waymo's release notes cite a General Offense Report of injuries "described as life threatening" yet exclude it from SSI+ pending a police crash report; the repo overrides it to Minor W/ Hospitalization. CPUC Q3 due Nov 1 (posting ~mid-Nov); CA DMV AVIM VMT filings still unpublished.

- 2026-09-15 (later 2): TWO LAYOUT REGRESSIONS FROM THE 2026-09-07 FACE CHANGE, FOUND AND FIXED (human: screenshot of the "What and when to compare" controls, "can you spot the css problem here?", then "i'm not sure anyone raised it for a reason. can you investigate more deeply?", then "ok"). ALL 69 QUALS GREEN (67 + 2 new: legend-wrap-gap, axis-ticks). ROOT CAUSE, one sentence: commit 940a45d swapped the page face from "Patrick Hand" (narrow, handwritten, self-hosted) to system-ui without changing any font SIZE, so every run of text got wider and two layouts that had been tuned against the old metrics broke. Measured by restoring the deleted fonts/ and the old stylesheet from git and rendering both in headless chromium.
  (1) THE LEGEND (what the human spotted). #month-legend-mpi-helmers { gap: 1.5rem } dates from 2026-02-24 (365d616), when the six helmer chips totalled 717px and fitted one row. Under system-ui the same chips total 837px, so they no longer fit the 928px column: Zoox wrapped alone onto a second row. Worse, `gap` is BOTH axes, so the 1.5rem meant for horizontal separation became 24px of vertical dead space above the orphan -- nearly a full 27px chip height. FIX: delete the override; the legend inherits .month-legend's 0.7rem. Chosen over column-gap:1rem deliberately -- 0.7rem leaves 35px of slack before wrapping, 1rem leaves 11px, and an 11px margin is how this bug happened in the first place. Verified: one row at 1200/1000px; at 820/700px where wrapping is unavoidable the row gap is 11px and the last row carries 2-3 items, not an orphan. NEW QUAL legend-wrap-gap pins the general spec: a wrapping legend may tune the distance between ITEMS with column-gap, never the distance between ROWS, because the shorthand does both silently; flex-direction:column containers are exempt (there the gap IS the item spacing).
  (2) THE AXIS (found by sweeping for the same failure mode). The x tick labels of the fleet-forecast chart overlapped in three places. .month-tick is 13px in both eras -- the SIZE never changed -- but the widest label "1,000,000" went 46.3px -> 64.3px, +39%, and the axis had only 3.4px of clearance at its tightest point. crashla.js emitted a label for every 1-2-5 rung with NO collision avoidance, in two near-identical blocks (the MPI density chart, #chart-distributions, and the fleet forecast). Measured across the 63 chart states the new qual walks: 47 overlapping label pairs before the fix -- 9 in the forecast chart and 38 in the density chart -- and 0 after. The density chart's overlaps are all in NON-DEFAULT states (single-month windows, particular helmer subsets and metrics); the default view never showed them, which is why a browser sweep of the loaded page found only the forecast chart. Whether the density half predates the face change was NOT established: testing the old face would need a per-glyph bound calibrated to Patrick Hand, and TICK_GLYPH is calibrated to system-ui. FIX: one shared helper drawLogXTicks replaces both blocks -- a gridline on every rung, a label on every labelStep-th rung COUNTING FROM THE DECADES. The stride is rounded up to whole decades on purpose: the obvious greedy left-to-right filter drops exactly the decade anchors (measured: it deletes 10,000, 100,000 and 1,000,000 while keeping 20,000 and 200,000), which is worse than the overlap it cures. Width model is the per-glyph bound (TICK_GLYPH) that axisLeftMargin already uses, so one constant governs both the label column and the label stride. Net in this tree: crashla.js 4,627 -> 4,638 lines (+11) and one FEWER `if (` (138 -> 137).
  METHOD: a 4-agent design workflow (three lenses -- anti-magic, DRY, qual-first -- plus an adjudicator) proposed and cross-checked the design before any edit; the adjudicator caught the decade-anchoring flaw in the two greedy designs and verified the winner against a full tree copy. Red-first was real, not decorative: the new qual's overlap assertion was run against unpatched crashla.js and failed with "5,000" at x=401.61 and "10,000" at x=450.64 leave 5.0 -- matching the browser measurement (-3.4px) independently.
  NEW QUAL axis-ticks, four layers: the helper on three synthetic scales (pinning that all ten gridlines survive every thinning regime -- thinning takes labels, never geometry); five anti-Postel throws (xMin<=0, empty/inverted range, a mapX that shrinks); a DRY grep (3 call sites, 1 ladder); and 63 rendered chart states asserting every numeric label sits on a gridline, at least two labels survive, every neighbouring pair clears TICK_GAP under the per-glyph model, and a thinned axis keeps even spacing (which is what an every-other-rung stride would fail). It reads TICK_GLYPH/TICK_GAP out of the vm rather than copying them, so it cannot drift from the constants.
  ONE EXISTING QUAL RESTATED, FLAGGED FOR THE HUMAN UNDER RULE 5: chart-markup.qual.mjs pinned `class="month-grid"` at exactly 2 sites. Collapsing the duplicated axis blocks into one helper makes it 1. The assertion is a DUPLICATION pin, so 1 is the tighter claim and both halves of the original intent survive (the monthly axes still emit none; "both density charts draw gridlines" is now pinned harder by axis-ticks layer 3, which requires 3 call sites and 1 ladder). Restated rather than weakened -- but it is an edit to an existing qual, so it wants a human nod. The fallback, recorded in case the nod does not come: have the helper return {rungs, labels} and leave both <line> templates in place, which keeps the count at 2 at the cost of one more decision point and a duplicated template.
  VERIFIED IN A REAL BROWSER, not just in the quals: the forecast axis now reads 100 / 1,000 / 10,000 / 100,000 / 1,000,000, evenly spaced, every gridline intact, zero overlaps. Page-wide tick-label overlaps in the default view: 0 on both log charts and on chart-helmer-series, chart-fleet-timeseries and chart-distributions; chart-mpi-all keeps 7, all of them between the pre-existing "?" labels below.
  SIDE FINDING, pre-existing, NOT touched: chart-mpi-all renders 45 tick labels whose text is literally "?" (58 labels total: 8 month, 5 y-axis, 45 "?"), stacked at one y just above the x axis so they overlap each other; one is faintly visible at the right edge of the chart. Confirmed present at HEAD before any of today's edits, so it is neither caused by nor related to this work. Worth a look.
  ALSO CHECKED, clean: no other flex container changed its row count across the face change, no new horizontal overflow anywhere, and page-level overflow actually IMPROVED (46px -> 0px). The other font-size changes the revamp made (table headers -15%, incident count -19%, mpi card sources +15%) are size changes, not layout breaks.
- 2026-09-15 (later): SEVERITY-BREAKDOWN NOTE DELETED BY THE HUMAN + FATALITY GUARD REFOCUSED (human bug report: "the note mentions 3 fatal incidents but i only see 2 in the incident browser", then "i'm just removing the note. i don't actually see the point of it. the 3 fatal incidents are listed in the incident browser, correctly showing 0 fault, with the fully narratives if anyone wants to check them", then "ok, go ahead"). ALL 67 QUALS GREEN. DIAGNOSIS of the bug report: the note was written against the FULL dataset while sitting directly above a table that renders activeIncidents() — the selected window — so the two disagreed by construction. DEFAULT_START_MONTH is 2025-06 (crashla.js), which excludes 30270-9724 (JAN-2025 SF); the other two (SEP-2025 Tempe, AUG-2026 Dallas) are in-window, hence 3 in the note vs 2 on screen. NOT a new bug and NOT caused by the Dallas row: the same mismatch existed as 2-vs-1 before this release, and the earlier session widened it to 3-vs-2 by updating the count instead of noticing the scope mismatch. quals/fatality-guard.qual.mjs never caught it because it reads INCIDENT_DATA (full history) — it asserted the note matched the DATA, and nothing asserted the note matched what a reader SEES. Human's call: delete the note rather than scope it or derive it, on the grounds that the incident browser already shows each fatality's fault value and full narrative, which is better evidence than a hard-coded sentence needing a rewrite after every death. (Caveat recorded for whoever revisits this: only 2 of the 3 are visible without dragging the slider back before 2025-06.)
  FOLLOW-UP APPLIED: (a) deleted the orphaned "[COPY CURRENT AS OF 2026-09-15]" comment block in crashla.js, which still instructed a reader to rewrite "the parenthetical note below" — a comment describing copy that no longer exists is worse than no comment. (b) REFOCUSED quals/fatality-guard.qual.mjs onto the reason that survives the note: the fatality metric divides each death by vehiclesInvolved (Koopman/Piper) and data/slurp.py DEFAULTS that field to 2, so a new fatality must stop the run for a human to read the narrative and record a VEHICLES_INVOLVED override — nothing else in the pipeline asks for that read. Kept: the count assertion (the trip-wire), the all-Waymo assertion (a first Tesla/Zoox fatality must not land silently), the faultfrac <= 0.05 assertion (the first at-fault fatality deserves a human read before it moves the at-fault metric), and the per-fatality divisor pins. DROPPED: the speeds {0,5,8} assertion — speeds were only ever pinned to keep the deleted sentence honest, and nothing else consumes them. The FIX message now tells the reader to count vehicles and assess faultfrac rather than to rewrite copy; the header carries a historical note on why the sentence went away, so the next person does not "restore" it.
  LESSON, general: a hard-coded sentence describing windowed data is a bug waiting for the window to move. Before pinning prose to numbers, check which set the numbers on screen come from — activeIncidents() (windowed) or INCIDENT_DATA (full history) — and make the guard assert against the same one the reader sees.
- 2026-09-15: NHTSA SEP-15 RELEASE SLURPED + 90-INCIDENT FAULTFRAC BATCH (human: "new data! can you handle the new assertion failure in slurp.py?"). ALL 67 QUALS GREEN; 2,048 -> 2,138 incidents (Waymo 2,066 / Zoox 46 / Tesla 26). The reported assertion was the FIRST OF THREE anti-Postel gates the release trips, handled in order. (1) EXPECTED_HELMERS: new reporting entity "MOIA America LLC" — Volkswagen's ID. Buzz AD subsidiary, filing separately from "Volkswagen Group of America, Inc."; sole report 35021-16268, JUL-2026 Beverly Hills CA, safety driver in the driver's seat, ADS disengaged 9 s before the safety driver drifted into a parked car. Whitelisted and OUT of scope (HELMER_SHORT remains Waymo/Tesla/Zoox); the comment above the set now states that being listed != being in scope. Per the 2026-08-17 lesson ALL FOUR whitelists were swept against the new snapshot BEFORE re-running (Report Type / Driver-Operator Type / Reporting Entity / Severity) — MOIA was the only novel value, so no second trip. (2) NHTSA_DATA_THROUGH_DATE 2026-07-15 -> 2026-08-15, cadence re-verified from snapshot history rather than assumed: the new file's newest incident month is 2026-08 (10 five-day-track in-scope incidents, a half month) and its newest submission month is 2026-08. (3) FIVE_DAY_RECEIPT_OBSERVATIONS re-measured — the existing four rows reproduce EXACTLY (19/38, 25/72, 16/58, 17/61) under "5-Day report type + public-service filter + Report-ID-then-incident dedup", which validated the measurement before extending it; new row 2026-07 = 12/58 (numerator from the Aug-17 release, the first containing July). 0.207 is a new low, so FIVE_DAY_RECEIPT_COVERAGE (0.31, 0.25, 0.52) -> (0.28, 0.20, 0.52): median 0.313 -> 0.279, and lo HAD to fall or the constants fail their own must(). Premise checked before adding the row: a month is final at its second NORMAL release (Feb 19->38 then flat x5, Apr 3->57 flat, May 16->58 flat, Jun 17->61 flat); the lone exception, Mar 25->53->72, is explained by its second release being the truncated May-15 one. IGNOREME.md's receipt triple ("best 0.32, band 0.25-0.48", two updates stale) corrected to match.
  NEW FATALITY (the fatality-guard human-review gate fired as designed): 30270-16196, AUG-2026 Dallas, the pedestrian death CBS Texas reported Aug 9. Narrative: the Waymo was slowing to yield to a pedestrian walking in the roadway when an SUV travelling the OTHER direction struck the pedestrian, who contacted the ground and then the AV's rear left at 5 mph. faultfrac 0, unanimous; the secondary-impact rule is now explicit in the criterion doc (severity never moves the number — the criterion is avoidability alone). vehiclesInvolved = 2 (AV + SUV; the pedestrian is the decedent, not a third vehicle) — that is the default, pinned in the qual anyway so the Koopman/Piper divisor is asserted rather than inherited. Qual re-pinned to 3 fatalities, all Waymo. The on-page "Severity breakdown" parenthetical described only two fatalities and became false the moment this row landed; it was first rewritten in Latin per rule 7, then DELETED BY THE HUMAN the same day — see the follow-up entry above.
  BATCH (90 rows: 76 Waymo JUL, 10 Waymo AUG, 2 Tesla JUL, 2 Zoox JUN). Three-stage method, all 42 agents, 0 errors: (a) 24-agent assess/adjudicate = 6 batches x 3 independently-framed assessors (map-first, expert-driver-first, skeptic) + 1 adjudicator each, all reading one locked criterion doc (anchors + full scenario map + framing discipline) plus 90 stratified exemplars drawn from the existing corpus; (b) 10-agent adversarial verification over all 44 non-zero/non-unanimous calls from two OPPOSING lenses (too-high / too-low) — 34 of 44 survived both lenses untouched, 12 challenges landed on 10 incidents, and 2 of those drew challenges in BOTH directions (30610-15822, 30610-15826), which brackets the adjudicated value rather than unseating it; (c) 8-agent precedent resolution for the single-direction challenges, each required to find its answer in the existing 2,048-row corpus on the principle that consistency with the locked corpus outranks either argument. Outcome: 4 adjudicated values held (30270-15831 0.3, 15893 0.2, 16024 0.25, 16032 0), 3 challenges won (15898 0.1 -> 0.05 the map's literal nearly-clear parenthetical; 16038 0.9 -> 1.0 because EVERY moving-AV strike on a stationary vehicle body in the corpus is 1.0 and the 0.9 tier contains zero vehicle strikes; 16085 0.2 -> 0.1), and 1 went to NEITHER side (16167 0.15 -> 0.1: both sides litigated the reversing-vehicle rule when the AV never left its lane, so the plain cut-in tier governs). Two resolvers caught a side MISQUOTING its own cited precedent (30270-14577 is speed 1 mph and moving, not stationary). Also normalized all 90 reasoning strings to the corpus voice (sentence case, AV/SUV/Waymo capitalized) — the criterion doc had told assessors "lowercase", which is wrong: 2,044 of the 2,048 existing rows are sentence case.
  DISTRIBUTION 54 zeros of 90. Both Tesla rows are non-zero self-strikes (13781-16256 = 1.0 chain across a lot exit, the THIRD outing for the 13781-11687 precedent; 13781-16255 = 0.9 bollard while squeezing past a stopped shuttle bus), moving Tesla at-fault k to 8.55 on 26 incidents. NEW MAP ENTRIES: standing water in lane at speed = 0.4 (30270-15993; the "sizable visible object" family, NOT the pothole 0.5, whose rationale is unjudgeable depth); AV threads a gap and strikes a parked car = 1.0 (30270-16038, corpus-decided); a first responder's intentional tool strike after the AV entered a smoke-obscured ACTIVE fire scene = 0.4 (30610-15822).
  FLAGGED FOR THE HUMAN, three items (item (i) settled same-day, see the follow-up entry): (i) 30610-15822 is the one genuinely contestable call of the batch — assessors split 0.4/0/0.8 and the two verifier lenses proposed 0.7 and 0.15, so the midpoint is a judgment, not a derivation; it is distinguished from the plain intentional-contact precedents at 0 (30270-6337, 30270-6368) only by the AV having driven into the scene that exposed it. (ii) quals/stress-test.qual.mjs: Zoox's full-history all-incidents verdict flipped "robustly worse" -> "ambiguous" at ratioHi 1.0010x. NOT a regression and not caused by the coverage re-measurement: advancing the cutoff to 2026-08 means 2026-07 is no longer the receipt-capped month, so its coverage_max went 0.52 -> 1.0, restoring the upper-edge miles the 2026-08-28 release had trimmed (coverage_max alone sets ratioHi). This verdict sits ON the 1.00x knife edge and has now alternated twice; the qual comment says so and says to expect re-pinning every release. (iii) quals/window-band.qual.mjs hardcodes the data-through month (re-pinned 2026-07 -> 2026-08) while quals/incident-coverage.qual.mjs derives the same month from NHTSA_DATA_THROUGH_DATE — worth unifying on the derived form, not done unilaterally because quals are sacred.
  NOT DONE, deliberately: no 2026-09 VMT rows (no SEP-2026 incidents yet; due before the ~Oct 15 release) and no Tesla VMT re-pin (the Q3 deck ~Oct 21-28 is the next real anchor; the Sep-3 "1 million unsupervised miles" statement is already carried by the Jul/Aug rows).
- 2026-09-07 (later 4): FOREIGN URL KEYS (bug report: opening teslapologetics.dreev.es from Facebook, which appends ?fbclid=..., "breaks everything"; human first asked to strip Facebook's params, then: "we should be impeccably anti-magic and throw up a dismissable error banner about any URL params we don't recognize, and then strip them"). REPRO (headless Chromium, the reported fbclid value): applyUiStateQuery asserted "Unexpected URL state key" and init died -- 0 charts, 0 cards, 0 rows. RULE NOW: keys the page does not own are not state. applyUiStateQuery strips them and returns their names (no tracker list, no special case for Facebook); the page's own keys are still read strictly (duplicates, missing required keys, bad values all still throw; url-state.qual's stray-z case flipped from 'throws' to 'reported + stripped', the human's spec). loadUiStateFromLocation fills a static, hidden <div id="url-banner" class="banner" role="alert"> (index.html, after the h1) with the names -- rule-7 Latin "Parametri URL ignoti neglecti et ex inscriptione remoti: <keys>" + TODO recap; button "Dimitte" (= dismiss) + TODO -- sets hidden = (no unknown keys), wires the dismiss click, and calls syncUrlState() unconditionally, which rewrites the address bar from state and so drops the foreign keys (init already canonicalised the URL on every visit, verified: a clean load ends at ?f=All&s=-&a=1&c=...&m=atfault, so no new branch was needed). Branches added: none beyond the loader's existing canSyncUrlState guard; the banner is toggled by assignment, the strip is a filter. style.css: .banner (drawn box in --worse), .banner[hidden] { display: none } (needed because .banner is display: flex), .banner button. QDD: url-state.qual amended first (harness gained banner/dismiss stubs; new end-of-file section: foreign keys returned in order with owned state applied; the real fbclid load -> defaults, banner shown naming fbclid, address bar = the page's own query; dismiss hides; clean load -> no banner; index.html markup and the [hidden] rule pinned). ALL 67 QUALS GREEN. VERIFIED: the fbclid URL now renders 6 charts / 6 cards / 1,140 rows with zero console errors and the banner visible; clean URL unchanged in behaviour.

- 2026-09-07 (later 3): Y-AXIS TITLE / TICK OVERLAP (human: VMT chart's rotated title ran under the tick labels; "fix in a clean way ... only if it makes the code more elegant, no ad hoc patching"). CAUSE: the rotated title sits on x=18 (qual-pinned), tick labels end at mLeft - 8, and mLeft was the literal 68 copied into five chart functions -- nothing tied the margin to the labels, and the system-ui face made "136.9K" 44.7 units wide (Patrick Hand: ~37), so its left edge at x=15 entered the title band (x~8-21). Offered (1) derive the margin from the labels or (2) delete the two y titles that duplicate their section headings (recommended); human chose 1. APPLIED: `axisLeftMargin(tickLabels) = Y_TITLE_BAND 24 + TICK_GLYPH 8 * widest label length + TICK_GAP 8` next to drawSingleMonthAxes (which now uses TICK_GAP); the three charts with a tick column (MPI over time, VMT series, growth trajectory) compute mLeft from their formatted ticks after the ticks exist (VMT: 80; MPI: 64; growth 'Fleet size' with "1,000,000": 104); the two density charts keep mLeft = 68 (no y ticks; distribution-peak/-chart quals replicate that frame). The 8-per-glyph bound is honest because .month-svg sets tabular figures (13px tabular digit ~7.8, "." 3.6, K 9.9, M 12.1 measured; every K/M label carries a "." that pays for its letter). NEW QUAL axis-margin (red-first): helper values, exactly 3 derived + 2 literal margins in crashla.js, and the rendered growth chart's ticks all sit one gap left of the axis with modelled left edge >= 24. ALL 67 QUALS GREEN; re-rendered, zero console errors.

- 2026-09-07 (later 2): PATRICK HAND RETIRED (human, offered keep / h1+h2 only / drop: "3 sounds simplest for the code, right? if so, let's try that"). ONE FACE: --sans (system-ui, sans-serif) everywhere; deleted fonts/ (both woff2 subsets + OFL), both @font-face blocks and their header comment, the index.html preload, the --hand token and fallback stack, the display-tier font-family/font-synthesis rule, the ncase fake-bold text-shadow, the h1/h2 letter-spacing, and the h1 rotate(-0.7deg) tilt whose comment tied it to hand lettering (one-line revert if wanted). Headings and card titles now take the single emphasis weight: the 600 rule is `h1, h2, h3, .mpi-card-helmer, b, strong, th, .mpi-card-mpi, .mpi-card-mult, .stress-badge, .pm-card-odds`; the display group keeps only --lh-tight + text-wrap: balance. The sketch skin (wobbled boxes, drawn h2/colophon rules, pills, chevron) and the "design inspired by nicky case" credit are unchanged. QDD: type-roles (one family, no text-shadow, headings on the 600 rule), sketch-style (font-face + --hand sections removed; the drawing pins stay) and font-loading (rewritten: no preload, no @font-face, no fonts/ dir, no reference to the face) amended first (two red), then applied; fleet-forecast's Expectata wording no longer names the face (assertion untouched). ALL 66 QUALS GREEN; re-rendered at 1100/400px with zero console errors; computed family|size|weight combos now 12, all system-ui (was 31 across three faces this morning). The 2026-08-21 ledger entry's font decisions are superseded by this one; decisions 6 (font-display) and 7 (hand prose) are moot.

- 2026-09-07 (later): A/B DECISIONS ON THE HELD ITEMS (human, from per-decision A/B renders; A = tree, B = override CSS injected at screenshot time): 1 B (--ink-faint text -> --ink-soft on .cbi/.mpi-card-vmt/-ci/-sources/.pm-card-vol/.pm-footer/.pm-age/.pm-refresh; --ink-faint now paints only the section chevron); 2 A KEPT (prior-only badges stay faded: "lower opacity, like slightly grayed out, makes more logical sense than underlining"); 3 B-VARIANT (human wanted fainter lines but palette-based: .month-grid { stroke: var(--wash-deep); stroke-width: 0.5 } on the distribution + fleet-forecast gridlines; the #e0e4ef JS literal is gone; no new token); 4 B (the k=0 '?' marker is a .month-tick text, inline font/fill dropped, pointer-events:none kept inline); 5 B + 11 B as ONE rule (links inside lists of links -- .pm-card-question a, .mpi-card-vmt a, .mpi-card-sources a -- keep --ink-soft with a --line-soft underline and hover to --accent; no text-decoration: none anywhere; links in running prose stay accent blue; the human asked whether two link colours have a semantic basis: the answer given was context, not semantics -- blue marks the few link words inside prose, while in a list where every item is a link blue everywhere is noise, per GOV.UK/Wikipedia-style navigation lists); 6 KEPT swap + preload (no strong opinion; moot if Patrick Hand goes); 7 KEPT sans prose, no change (human: "i don't know how i feel about that font for paragraphs of text" and "i'm not so sure about this patrick hand font in any case" -- the designer's answer given: handwriting faces are display-only per Material/Butterick; ncase's body-text Patrick Hand works on short illustrated pages at 23px, not on a dense statistical argument with numerals in the prose; the sketch identity here is carried mostly by the drawn rules/wobbles/pills, so Patrick Hand could shrink to h1/h2 or go entirely with the skin intact -- OFFERED, not applied); 8 B as a principled variant: third leading token --lh-dense 1.45 on .mpi-card, .predmarket-grid, .chart-tip (the rendered B also had 10.4-11.5px meta text; the 12px floor was KEPT, so the cards recover the leading share (~135px) of the +13% but not the size share -- flagged to the human); 9 B (.abstract, #sanity-checks p, .month-note { max-width: 70ch }); 10 B (legends inherit the 17px body size; the .month-legend and .metric-select font-size rules deleted; chip legends under the charts grow 13.6 -> 17px too and the phone helmer legend wraps to 4 rows -- flagged). QDD: type-roles + chart-markup quals amended first (red), then applied; ALL 66 QUALS GREEN; re-rendered, zero console errors. Ready for the human: a Patrick-Hand-scope question (keep as is / h1+h2 only / drop) -- answer decides 6 and 7.

- 2026-09-07: CSS/TYPOGRAPHY PASS (human: "can you do a css pass, researching best practices? in particular i think our mishmash of fonts may be poor design"). METHOD: 31-agent workflow -- 6 primary-source research sweeps (92/94 claims re-fetched and verified: Butterick mixing-fonts/alternate-figures/monospaced-fonts, Material 3 applying-type, Datawrapper text-in-dataviz/fonts, MDN + WCAG 2.2, web.dev font loading, the raw CSS of ncase.me/crowds/trust/remember/anxiety/polygons), 6 code-audit lenses (126/126 findings confirmed by per-lens refuters), 3 independent specs scored by 3 judges (purist 106 / ncase-faithful 96 / "Headings by Hand, Numbers by Machine" 110), one synthesis applied to a scratch copy and MEASURED before porting. APPLIED (34 of the synthesis's 40 entries; ALL 66 QUALS GREEN = 61 + 5 new, written red-first): (1) TWO FAMILIES, ONE RULE EACH -- --hand is now exactly h1/h2/h3/.mpi-card-helmer plus the two drawn pill CONTAINERS (.filters, .metric-select, so the 'Miles per [select] incident' sentence is one face at one size) with font-synthesis:none; everything read or measured (th, chart ticks and axis titles, legends/toggles/radios, slider readout and labels, .incident-count, .month-note, colophon, [?] hint) is --sans; --mono RETIRED (Butterick: few documents tolerate a third family; odds/volume/age align by text-align + tabular figures); --sans = system-ui, sans-serif (-apple-system after system-ui is unreachable); --hand fallback reordered "Segoe Print" before "Bradley Hand" (macOS ships only Bradley Hand Bold, which the 1px shadow double-bolds during swap) plus Chilanka/casual before cursive (Android maps cursive to Dancing Script). Computed family|size|weight combos 31 -> 14 desktop, 30 -> 13 phone; the stray Arial glyph (.pm-refresh, a control that never inherited) gone via one `button, select { font-family/font-size/color: inherit }` reset. (2) SIX-STEP SCALE --fs-xs/sm/base/lg/xl/2xl = 0.75/0.85/1.0625/1.2/1.6/2.4rem plus --lh-body 1.6 / --lh-tight 1.2; 24 font-size literals -> tokens; the narrow h1/h2 sizes are :root overrides inside the existing @media block; .month-tick 13/14px and .month-label 11/12px stay literal (SVG user units; sketch-style pins). (3) EMPHASIS: text-shadow only on the display group; ONE `font-weight: 600` rule (b, strong, th, .mpi-card-mpi, .mpi-card-mult, .stress-badge, .pm-card-odds); .stress-badge 700 -> 600, .source-table a loses its bold, .mpi-card-mult loses its 0.7 opacity (composited --safer to 2.98:1), the primary-MPI 0.95rem bump deleted (at 17px it wrapped '108,788 / MPI' on the 280px cards). (4) TABULAR FIGURES in one rule keyed on the containers where figures stack (table, .mpi-card, .predmarket-grid, .chart-tip, .month-svg, #date-range-controls); prose keeps proportional digits. (5) CAPTION rule (.abstract-links, .incident-count, .month-note, .predmarket-loading, .colophon) at --fs-sm/--ink-soft. (6) HYGIENE: 8 dead rules deleted (th .sort-arrow, #month-legend-speed, .month-legend-label, .month-legend-break, .month-mark-tri, .mpi-card-formula, .predmarket-error, .pm-footer a); restatements of the universal reset / [data-tip] cursor / generic table rules removed; --line: var(--ink); .mpi-card keeps border-left-width only (every card sets the colour inline); tr:hover -> tbody tr:hover with a :where(.table-scroll) zebra so source order decides; -webkit-user-select twin (Safari); text-size-adjust: 100%; body max-width 60rem; text-wrap: balance on the display group and pretty on p/li/.colophon; a prefers-reduced-motion block (transition-duration 0s, lifts off); one focus ring for filters/select/refresh; <link rel=preload> for the latin woff2 (crossorigin; latin-ext is a unicode-range guard and is not preloaded). (7) MARKUP (crashla.js): every generated <table> wrapped in <div class="table-wrap"> (overflow-x: auto) -- phone document scrollWidth 1010 -> 400, and the bare sticky th no longer pins sanity-table headers to the viewport because each wrapper is the nearest scroll container (table-scroll.qual untouched); the numeric cells/headers of the stress + derivation tables carry class="num", replacing the nth-child rules that right-aligned the text 'Metric' column and left High MPI left-aligned; the hidden .month-grid lines (one display:none element per tick) are no longer emitted; dot halos stroke:#fff -> class month-dot (stroke var(--card)); the [?] source hint title= -> data-tip (tap/keyboard path, finger-sized padding); the sort arrows U+25B2/25BC (outside the font's unicode-range, rendered from the OS last-resort font) -> a CSS triangle on th[aria-sort]::after, same mechanism as the section chevron. The commented-out CBI table is left verbatim (human comment). NEW QUALS: type-roles (families, scale, emphasis, tabular, leading, captions, focus, motion, selection pins), css-consumers (every class/id in style.css has a producer in index.html/crashla.js), table-markup (wrap counts, td.num, drawn sort indicator), chart-markup (no #fff, no month-grid), font-loading (one same-origin preload before the stylesheet, matching font-display); quals/css-parse.mjs is the shared parser (sketch-style keeps its own copy, untouched). NOT APPLIED -- HELD FOR THE HUMAN, each reverses a human-ratified visual decision (PDP), all six sit ready in the workflow's scratch copy: (a) --ink-faint text (3.1-3.5:1 on the page's grounds: CI ranges, VMT lines, citations, volumes, ages) -> --ink-soft, reversing sketch-style section 8's sub-AA-by-design note; (b) .stress-badge.prior-only 0.55 opacity (2.2:1) -> italic + dotted underline, and .month-legend-item-empty 0.4 -> dim the chip only; (c) distribution/fleet gridlines stroke:#e0e4ef (a pre-skin cool gray, a JS literal the CSS qual cannot see) -> .month-grid var(--line-soft); (d) the k=0 '?' marker's inline 13px/bold/#555 -> class month-tick (renders lighter); (e) .pm-card-question a text-decoration: none -> a --line-soft underline (WCAG F73: 25 row links identifiable only on hover); (f) font-display swap -> optional (no layout shift; a slow first visit loses the face for that page-life). OPEN (subjective, from the judges): body prose on Patrick Hand (the 2026-08-21 side-by-side question, still open); legend size 13.6px sans (kept; phone helmer legend stays 3 rows) vs 17px (4 rows); summary-card block +13% height (58% from the 1.6 leading, 41% from the 12px floor) vs keeping 1.45 / 10.4-11.5px as exceptions; system-ui-first vs GitHub's named-first stack (CJK-locale Windows); prose measure 103-118 chars/line (a 70ch cap reflows the layout); click-only collapsible h2s (WCAG 2.1.1) need a markup change with its own quals; card citation links at 12px are now a louder blue block. VERIFIED: headless Chromium at 1100px and 400px, all sections expanded, zero console/page errors; before/after composites per section in the session scratchpad. PRE-EXISTING in the working tree and NOT this session's: data/incidents.js NHTSA_FETCH_DATE 2026-09-04 -> 2026-09-07, sourcery.html regenerated (304 prompts), index.html 'as of September 2026' -- all saved 12:39-12:40, before the session's first command.

- 2026-09-04 (later): EVERYTHING ABOVE APPLIED (human: "yes to all of that"). ALL 61 QUALS GREEN (59 + 2 new: window-band, distribution-peak); slurp re-run live (NHTSA file still Aug 27; 2048 incidents — 30270-8403 retired by its v2). WHAT CHANGED, by file: data/slurp.py — FIVE_DAY_RECEIPT_OBSERVATIONS corrected to 19/38, 25/72, 16/58, 17/61 and the triple to (0.31, 0.25, 0.52) (best = median 0.313; July pooled incCov now 0.3397 [0.1372, 1]); by-Report-ID version dedup now runs over ALL rows before the public-service filter (main() and incident_coverage), so a later version can retire a report; the "no Monthly filing in the data-through month" guard was REMOVED (premise false, and the submission-month guard already catches a filing received after the cutoff — slurp-release-metadata.qual now proves an early Monthly filing passes and a later-submitted one trips 'submission month'); _canonical_helmer exact-matches (blank no longer keys Waymo); build_vmt_csv must()s 9 cells; strict r[csv_field] with ARCHIVE_ABSENT_COLUMNS = {Weather - Partly Cloudy} filled explicitly; both artifacts computed before either is written; NARRATIVE_TYPOS += '[XXX}', '{XXX]', '[XXXX]'; console passenger summary uses the app's three classes; docstring/comments fixed (April 3/19). data/vmt.csv — tesla 2026-07 240k [150k, 380k] (cum 2.68M), 2026-08 470k [360k, 600k] (cum 3.15M, kyoom [2.8768M, 3.786M]) per the Sep-3 "1 million unsupervised miles" allocation (rationales carry the arithmetic); Waymo kyoom knots Oct-25 ±1.0%, Nov ±0.8%, Dec = hub+E lo/hi−0.15M = [173.562M, 175.162M], Jan-26 = Dec + January's band; Zoox 2024-06..2025-05 kyoom_max = 2x cume (the stated band) and every later Zoox kyoom_max +89,000; rationale fixes (x1.103, 54.7% CA share, the 2023-11..2025-01 anchor note); vmt-ledger CUME_PINS tesla|2026-08 -> 3,150,000; kyoom-band.qual gained the local invariant kmin[t] >= kmin[t-1]+vmt_min[t] (and the Tesla-anchor check is now per-row). data/faultfrac.csv — 28 rows aligned to the locked map by a subagent (list in the entry above; 11672 -> 0.5 because NHTSA's contact field records bottom contact, as 14490/14942), 30270-8403 removed; fault-coverage.qual now also asserts no csv->incidents orphans. crashla.js — (a) WINDOW VMT BAND = summed month bands ∩ kyoom difference over the fully received months, plus the data-through month's thinned band (windowBand in monthlySummaryRows; entries carry month + coverage; window-band.qual recomputes it from the CSV): Waymo default window [177.1M, 189.6M, 212.4M] (was [144.6M, 189.6M, 243.4M]); Tesla [2.37M, 2.47M, 3.00M]; Zoox unchanged by construction. (b) low edge = vmt_min x coverage BEST x incCovMin (incident-coverage.qual recomposes all three edges exactly). (c) seriousInjury fiveDay: true (pin updated). (d) faultFlipMultiplier(est, human, nIncidents) capped at nIncidents/k with trimmed weights renormalized: Tesla now ∞ (was 5.85x -> worse); Waymo 3.7x feasible (stress-test.qual re-pinned). (e) distributionExtent covers each curve's probe-argmax neighbours (and medians); renderDistributionChart refines the peak by parabolic interpolation in log x (distribution-peak.qual: every mode inside the frame, k=0 peaks left of their medians, none on the frame edge). (f) gammainc caps scale with sqrt(a) and non-convergence asserts. (g) tooltip mean = mixture mean (∞ with any a<=1 component). (h) URL state: d= past the series asserts in renderWindowedViews (no clamp), m= empty asserts, DEFAULT_START_MONTH missing asserts, new optional keys v=1 (cumulative VMT) and g=<growth metric> (encoded only when non-default; url-state.qual's pinned default string unchanged). (i) HumansUS hospitalization/airbag/SSI+ re-derived on the current ladder: 1.8M-7.8M / 940k-2.7M / 3.8M-18M (human-benchmark-provenance.qual re-derives the centers, ±6%); nonstationary/roadway HumansUS hi 316k/320k; "six cities" -> "five areas" (7 sites + IGNOREME + qual); Blincoe 24-32% with 813403 named; injury-US comment on 813791 inputs; Kusano 1.92; Uber/Lyft srcLinks re-pointed to the live report pages; Lyft 0.94 src now says it is the 2021-22 mile-weighted average of 0.86 and 1.02. (j) MILES_FORECAST re-based: Tesla A 5.5M [4.5M, 7.8M] on the Sep-3 statement, Waymo 400M [375M, 440M], Zoox 4.5M [3.5M, 6M]; FLEET_HISTORY Tesla 2026-06 -> 28 [20, 58] (active basis) + 2026-08 150 [90, 220]. (k) sanity Waymo cross-check and waymo-reconciliation.qual use receipt-scaled VMT (now injury 0.94x / airbag 0.92x / SSI+ 2.1x). (l) card tooltip's "raw window VMT" is now the unthinned sum (vmtMonthBest). (m) misc: "1 incident" via splur, fmtAge drops zero components, pinned tooltip re-pins on a different target, fmtVol/fmtMana tier on the rounded value, Polymarket asserts on unparseable prices and on renamed sub-markets, miles-trajectory yMin 500 (no clipped Tesla points), history tooltips say "Range:" not "90% CI:", slider tick uses the 18px thumb, CI-fan legend swatches show the composite alpha, the unbalanced quote at the sensitivity paragraph, duplicate hub srcLinks, and ~15 stale comments (94% = upper bound; ~1.4x rideshare; metricOpacity; DEFAULT_START_MONTH tick; MILES base month; "carried through the last NHTSA data month"; incCovMax wording; fault===null; approxGammaQuant and the ~1e-4 accuracy claims). index.html — the two <ol>s merged (numbering 1,2,3); the human's "about 11x" STANDS (with the tighter window band and the fault alignments the card now reads 10.7x — 11 is the right rounding; the 10.4x finding is moot); "over 300 million miles as of August 2026" left as written (it matches the Aug row's 314.5M estimate). site.webmanifest colours -> #fffdf8. IGNOREME/README: three cohorts, Remote mode, scope-by-metric bullet, Aug 5, x1.103 / 54.7%, pipeline order, override-qual pointers, receipt fractions. DISPLAYED NUMBERS NOW (default window): Waymo at-fault 2.26M [1.74M, 3.02M] = 10.7x the AV-cities geo-center, robustly safer 4.1x-29x; all-incidents 179k [162k, 201k] ambiguous 0.76x-1.95x; injury 1.65M [1.35M, 2.03M] safer 2.8x-14.7x; Tesla all 109k [73k, 170k] ambiguous; Tesla at-fault flip ∞; Zoox all 68k [43k, 107k] ambiguous (1.04x ceiling). NOT DONE / FOR THE HUMAN: rule-7 note — the factual corrections to existing English src strings ("five areas", the Lyft 0.94 clause, "Range:" tooltip label, the miles note's "through the last NHTSA data month") are AI-English edits of AI-English copy, made on the "yes to all" approval; vet or Latinize. Two possible follow-ups the faultfrac subagent flagged: none needed (14490/14942 at 0.5 are consistent — svhit bottom). The k=0 "prior-only" note, the Dallas Aug-8 fatality and a 2026-09 row set await the Sep 15-17 NHTSA release; the Waymo hub update (~mid/late Sep) re-pins Apr-Jun and zeroes newly benchmarked metros' E.

- 2026-09-04: NEWS SWEEP + FULL CODE/NUMBERS AUDIT (human: "any VMT updates we can glean from the news. also ... can we review all the code and numbers for errors?"). NOTHING IN THE REPO WAS EDITED except this ledger entry (rule: review = findings, fixes await a "go"); the one-line data/incidents.js diff in the tree (NHTSA_FETCH_DATE 2026-08-31 -> 2026-09-02) predates this session's work — some external `python3 data/slurp.py` run at 16:10 Sep 2 (no qual writes those files; verified one by one) — and confirms the NHTSA file is still the Aug-27 one. METHOD: two Workflow fan-outs (9 news searchers + 2 verifiers each; 20 audit finders + 3 refuters each) died on the account's monthly spend limit mid-run (agents_error 118/124 and 287/296); salvaged the completed sweeps (8/9) and 6 finders from the journals, re-ran the 14 missing audit regions with one reproduction-lens refuter per major (31 agents, 0 errors, 2.66M tokens), and reproduced the first-round majors by hand (scratchpad probe-majors.mjs). ALL 61 QUALS GREEN at HEAD. NEWS (Sep 2-4): exactly ONE new mileage datapoint — Ashok Elluswamy at the Cybercab launch keynote (Austin, Sep 3, ~4:45pm CT; Electrek, Teslarati; full video x.com/SawyerMerritt/status/2095687786309341612): "we have achieved 1 million miles of unsupervised Robotaxi operation" — the same unsupervised SUBSET series as the Q2 call's ">380,000" (Jul 22). No new paid-miles (deck) figure; "~2.5M paid" in press still = end-Jun 2.440M. Since unsupervised miles are a LOWER bound on service miles (denominator scope None + In-Vehicle + Remote), the fleet drove >= ~620k service miles Jul 22 -> Sep 3 (600-720k over 40-50 days across vintage readings), vs ~320k carried by the authored rows for that interval. Allocating the delta with 1x-3x within-interval ramps gives Aug-2026 unsupervised ~385-520k (central ~460k) and late-July ~75-200k. PROPOSAL (NOT APPLIED, awaiting the human): tesla 2026-07 170k [55k,340k] -> 240k [150k,380k]; tesla 2026-08 240k [120k,450k] -> 470k [360k,600k]; kyoom re-chained additively (Jul cum 2,680,000 [2,516,800, 3,186,000]; Aug cum 3,150,000 [2,876,800, 3,786,000]); re-pin quals/vmt-ledger CUME_PINS tesla|2026-08 -> 3,150,000; rationale to cite the Sep-3 statement and the allocation; run quals. The datapoint also refutes Electrek's Aug-28 "~20 active/day" (14.4k mi/day would need ~720 mi/veh/day) and fits AP's Sep-3 "more than 200 unsupervised robotaxis across six cities" and the tracker's Aug distinct-vehicle count (173 ex-Bay-Area; Teslarati's "nearly 200 unsupervised" is that count INCLUDING 25 supervised Bay Area cars). Other Tesla: TxDMV VINs 270 (Aug 27) -> 314 (Aug 31, first 45 Cybercabs) -> 420 (Sep 2, +106 Model Y); Dallas geofence ~31->81 sq mi (Aug 29), Austin +9% (Aug 31); public Cybercab rides Austin from Sep 4 5pm ("limited section, part of the day"); 24/7 driverless "next month or so" (so today's service is not round-the-clock); NTA (Aug 20) permit up to 5,000 vehicles in Clark County (Waymo 1,000; Aviari/Uber 1,000), no LV service yet; NHTSA "evaluating" the no-controls Cybercab (Teslarati: FMVSS self-certification audit query) — pause risk to watch. Bollard strike Austin ~Aug 18-20 (SGO-reportable). Waymo: hub unchanged (220.6M thru Mar; CSVs still 202009-202603, Jun-24 vintage) as of Sep 4 — every "500k+/wk", "4M+ mi/wk", "200M+", "350M km" in Aug 25-Sep 4 press is Mar/Jun vintage (Waymo's own Aug 25 Munich post says only "hundreds of thousands of commercial rides each week"); fleet "over 4,000" (Sep 1 launch release via CNBC/TechCrunch; ~300 Ojai; SF Standard Aug 30 ">1,000 Bay Area"); Denver 60 sq mi / San Diego 50 / Tampa ~50 opened to public riders Sep 1 (rolling invites, "dozens" of cars each; 14 cities; LV still employee-only); TxDMV Waymo VINs 736 -> 988 (Sep 2, +252). Disruptions: Santa Monica depot overnight-ops injunction Jul 27 -> appellate stay ~mid-Aug (2 lots, ~50 I-PACEs; LA-local); no pause/recall/probe Aug 20-Sep 4; Tropical Storm Edouard (Houston Aug 31-Sep 1) no pause found. INCIDENT DUE in the ~Sep 15 release: Dallas, Fri Aug 8 ~11:30pm, Maple Ave — pedestrian struck by an SUV was thrown ~30 ft into the opposite lane and hit by a Waymo, which stopped; the man died (CBS Texas Aug 9): a fatality-severity SGO row, secondary impact, fractional-death rule applies; also Denver Aug 27 (unoccupied Waymo rear-ended in a chain crash, other driver cited). Older items missing from earlier notes: Waymo-Uber Phoenix channel ended Jun 29; Miami weather pause May 28 (single tweet); CPUC Aug 14 approval = 18 CA counties; Findings Press (CPUC 2023-25): ~54% of CA VMT carries a passenger, deadhead ~2.8 mi/trip end-2025; Driverless Digest CA paid trips/wk 330k/329k/315k Apr/May/Jun 2026. Zoox: no new miles/riders after Aug 5 (>3M) / Jul 30 (>500k riders); LV 65 cars in service (LVRJ Sep 2; NTA cap 100); LAS airport pickups both terminals from Sep 3; SF still free (no CPUC/DMV deployment permit; CA DMV list unchanged since Nov 2025); Houston + San Diego SUPERVISED testing announced Sep 1 (no VMT effect); TX VINs 44 (5 purpose-built); CEO Aug 10: Hayward output 5-6/day. Waymo/Zoox Jul-Aug rows: no change. A 2026-09 row set is needed before the ~Oct 15 release (not now). NOT CHECKED (agent died): whether FARS/IIHS/CRSS/Uber/Lyft have newer releases. AUDIT — ranked, every item reproduced from source (verifier or by hand) unless marked: [MAJOR, displayed numbers/verdicts/data] (1) data/slurp.py:59 FIVE_DAY_RECEIPT_OBSERVATIONS is mis-transcribed: recounting the snapshots under the stated definition gives Feb 19/38, Mar 25/72, May 16/58, Jun 17/61 (two independent recounts; every filter variant agrees; the Feb numerator omits Zoox 30610-14026) — Feb = 0.500 > hi 0.48, so the (0.32, 0.25, 0.48) triple fails its own must() once the table is corrected; the wrong table propagates to README:42, this ledger's 08-28 entry ("Feb .47"), and coverage_max 0.48 on every 2026-07 row of vmt.js; fix = correct the table, raise hi to ~0.52 (best 0.32 still within the 0.02 median rule; median now 0.313), re-run slurp; the comment's "2 April incidents" should be 3 (3/57). (2) data/slurp.py:1057 public-service filter runs BEFORE the by-Report-ID version dedup, so a later version that moves a report out of scope is ignored: 30270-8403 v2 (Waymo JUL-2024) reclassifies the crash as test-driver-present but v1 is counted (incidents.js:7603, faultfrac.csv:266, faultfrac 0) — one spurious full-history Waymo incident; README:58-60 and the :1059 comment promise the opposite; fix = dedup over all validated rows, then filter the surviving version; qual pin 8403 out / 7257 in; drop the faultfrac row. (3) crashla.js:1353 faultFlipMultiplier's search is uncapped by the incident count: Tesla's at-fault cell "5.85x -> robustly worse" needs 5.85 x 6.65 = 38.9 at-fault incidents out of 24; at the feasible max s = 24/6.65 = 3.61 (every Tesla incident at fault) the verdict is still ambiguous, so the cell should read unreachable/infinity (Waymo's 3.70x = 319/1078 is feasible; Zoox already infinity); fix = cap s at incTotal/k (per-component a_K <= K_max*s + 1/2), return Infinity past it, qual. (4) index.html:38 (HUMAN copy, d8d1c78 — flag only): "about 11x better for at-fault crashes" vs the card's 10.4x after the 08-28 Waymo rebuild (was 10.9x when written); "zero fatalities in over 300 million miles as of August 2026" vs the page's own Waymo cumulative 294.5M through Jul (the 2026-08 row, 314.5M best / 298.6M lo, is never loaded); the two <ol>s render 1., 1., 2.; "roughly 10 times safer on most of the critical metrics" is ratified M5 copy (centrals: all 1.18x ambiguous, nonstationary 2.7x, roadway 3.0x, at-fault 10.4x, injury 6.2x, at-fault-injury 165x, hosp 2.6x ambiguous, airbag 7.3x, SSI+ 19.9x, fatality 3.8x ambiguous; 95% floors on the safer metrics 2.4-3.5x) — refuted as an error, noted for the human. (5) crashla.js:1871 distributionExtent widens the frame to every curve's MEDIAN but not its mode: on the fatality view Tesla's k=0 curve has true mode 5.19M (=2.08 x VMT) below the frame's xMin = its median 11.3M, so both "Peak" and "Median" dots sit at the left edge (cx=68) and the Peak tooltip reports 11.3M; Waymo's Median dot sits at the right edge (cx=884); the comment "peaks are located inside the extent by construction" is false; fix = widen to the argmax too (and refine the peak: A8 below). (6) data/vmt.csv:88-89 the Waymo Dec-2025 anchor knot was never applied: kyoom [158.95M, 189.57M] = ±8.8% around 174.262M while Sep-25 is ±1.2% and Mar-26 is exactly hub+E lo/hi (±0.5%); Jan-26 kyoom_min 170.84M is below kmin(Dec)+vmt_min(Jan) = 170.98M (Nov/Dec likewise loose by 101k/403k); kyoom-band.qual cannot catch it (global running sum only); fix = Dec-25 = hub+E lo/hi - 0.15M, interpolate Oct/Nov, Jan = Dec + monthly band, add a local invariant kmin[t] >= kmin[t-1]+vmt_min[t] to the qual. (7) crashla.js:1255 the window VMT prior = SUM of per-month 95% bounds (perfect-correlation assumption) while the repo's own kyoom band says Waymo's Jun-25..Mar-26 total is known to ~±3% (hub anchors): the Waymo default-window band [144.6M, 189.6M, 243.4M] is ~2.4x wider than the kyoom-consistent [177.0M, 189.6M, 211.5M], so the all-incidents CI is [133k, 227k] instead of ~[161k, 200k] (Poisson-only [166k, 187k]); Tesla's summed window floor 2.075M sits below its own kyoom_min 2.42M; no verdict flips; ratified running-sum convention (ledger 08-21 "kyoom ⊆ running-sum") but undisclosed as an assumption — DECISION: bound the window sum by the kyoom difference intersected with the month sums, or document perfect correlation as intended. (8) crashla.js:1542 low-edge pairing vmtMin x coverageMin x incCovMin double-counts receipt uncertainty — incCovMin is conditional on coverage BEST (product invariant), so the data-through month's Monthly-track vmtMin is 0.25/0.32 = 0.78x of the coherent value (Waymo 511k vs 654k, Tesla 1.85k vs 2.37k); window effect ~0.1%, visible only in the 2026-07 fan; related: slurp.py:800 uses a linear Wald interval for the rate ratio (lo 0.134) where the log-scale interval gives 0.183, and max(0.01, ...) would hide a negative bound — deliberate conservatism or fix. (9) crashla.js:496 seriousInjury is denied fiveDay ("a Serious without transport is Monthly-track") while SEVERITY_INFO:1143 marks bare "Serious" hosp:true ("implies transport", all three narratives say so) and the quals enforce ssi ⊆ hosp — so the strict subset SSI+ gets a ~1.4% SMALLER window denominator than its superset hospitalization (277.8M vs 281.8M) and ~3x less in 2026-07; pinned in incident-coverage.qual:201 — human decision (flag seriousInjury fiveDay, or make the exclusion an explicit future-proofing against the dictionary's "Serious W/O Hospitalization"). (10) crashla.js:2484 MILES_FORECAST Waymo {440M, 400M, 500M} and Zoox {5M, 4M, 8M} were authored 06-30 on the pre-rebuild series; after 08-28 the Waymo median needs Sep-Dec at 31M/mo (+57% over Aug 20.0M) and the 5th percentile (401.8M) is a zero-growth extrapolation; re-derive (~{410M, 385M, 460M}) or ratify. (11) crashla.js:711/742/771 HumansUS hospitalization/airbag/SSI+ "log-interpolated" bands (1.4M-6M, 820k-2.4M, 3M-14M) reproduce only from the PRE-06-28 AV-cities ladder; today's inputs give centers 13-23% higher (3.77M/1.60M/8.14M); off-by-default cohort, no verdict effect. (12) crashla.js:4251/4127 predmarkets: a Polymarket sub-market question rename makes the event vanish silently with a fresh green dot (failures stay 0); a malformed/missing outcomePrices renders a confident "0%" via `|| 0` — both anti-Postel; assert instead. (13) quals with no teeth: invariants.qual:68-81 five parseVmtCsv reject-cases have 13 fields vs the 15-column header, so they trip the malformed-row regex, not the labeled asserts (deleting crashla.js:1018/1060/1067/1070 leaves quals green; "negative vmt" is unreachable past the \d+ regex); fault-metric-completeness passes with zero assertions (its branch is dead while fault-coverage holds — same for faultknown-counting:181 and null-estimates-rendering:126); kyoom-band's "Tesla anchor tightens" is only `tightened > 0` (101/105 rows); incident-coverage:156 `eff.vmtMin < eff.vmtBest` cannot fail (comments still say p_best = 1.0); slurp-release-metadata's Monthly case asserts key-set only while the value changes (0.39 -> 0.59) and :34 is tautological; no qual asserts faultfrac.csv -> incidents.js (reverse orphans); monthly-series:289 expectata says SSI+ = "Moderate W/ Hosp + Fatality" (Moderate W/ Hosp is explicitly NOT ssi) and :136 still says window through 2026-02. (14) data/faultfrac.csv vs the locked scenario map (mechanical, ids for re-adjudication): 30270-11412 = 0.1 (stationary AV hit by a center-line crosser; rule says 0; JUL-2025, in-window); 6150 = 0.6 (11759 merge-at-green pattern = 0.2); 7312/8634/7969/8146/8344/9355/9082/9517 = 0.2 (2024 tire-debris rows; map 0.1, the acknowledged three-era drift never lifted); speed bumps 5611/5612 = 0.3, 14901 = 0.5, 12035/13552/13559 = 0.7 (documented 0.5); tire-only potholes 5114 0.2 / 11672 0.1 / 14976 0.5; 11525 = 0 vs 13507 = 0.1 with byte-identical reasoning; 6907 = 0 (Rule B 0.15); straddling outliers 9875/10367 = 0.25, 10057 = 0.1 (cluster 0.2); 9111 = 0.4 (pass-in-oncoming-lane tier 0.2); 4363 = 1.0 vs 11467 = 0.9 (barrier arm); 7533 = 0.3 (emergency swerve anchor 0.5); 10663 = 0.2 vs 8929/11905 = 0.1; 6883 = 0.1 (rear-ended after a lawful yellow-arrow stop = 0); 5081 = 0.5 (cardboard debris; band tops at 0.4). REFUTED: 14491 = 0 is consistent with the reverse-from-driveway class (8512/14935 = 0), not Rule B. Mechanical integrity is clean: 2049 = 2049, no orphans either way, all mirrored columns match, all faultfracs in [0,1]. [MINOR — behavior] crashla.js:3265/3055/3206 out-of-range d= indexes are silently clamped for display, the start is written unclamped to the URL (d=500-60) and reloading that URL asserts; :3256 m= (empty) is accepted silently and the link is rewritten to the default metric; :3192 vmtCumulative and selectedGrowthMetric affect rendering but are not in the URL; :3052 DEFAULT_START_MONTH missing -> silent index 0; :3418 "1 incidents" (splur exists); :1981 tooltip "mean" = vmtBest/(k-0.5) is finite for at-fault mixture curves whose posterior mean is infinite (any K=0 component) and ignores the VMT marginal; :1910 "Peak" = argmax over the 250-node frame grid (quantized up to ~1.5%, and it moves when other helmers are toggled: 48.9M -> 49.8M); :2514 miles-trajectory yMin 100k clips Tesla's first three cumulative points (683/7k/20k) and Zoox's 2025-05 lower band; :2588 history-point tooltips say "90% CI" for author ranges/kyoom bands; :2356 FLEET_HISTORY Tesla 2026-06 {42,30,58} is a TxDMV registration count in a series defined as vehicles in service (comment says ~20-25 active); :2738/1237 "raw window VMT" is the receipt-coverage-scaled total (2,494,400), not the raw sum (2,610,000), and the same tooltip shows the same number twice under two descriptions; :3960 the sanity Waymo cross-check sums July at full weight (294.5M) while its numerator has only receipt-through-Jul-15 crashes (like-for-like 281.8M -> injury 0.67 = 0.94x, airbag 0.28 = 0.92x, SSI+ 2.1x; waymo-reconciliation.qual same); :44 gammainc's series branch silently stops at 200 terms for a >~ 1000 near x = a (Waymo k=1981: err 5e-6, harmless today, grows superlinearly — scale the cap with sqrt(a) and assert convergence); :1340 flip light path trims 1e-4 mass without renormalizing (1.6e-3 rel, not displayed); :4116 fmtAge "1h0m"; :4069 pinned tooltip: any tap dismisses (comment says same-target); :4100 fmtVol "$1000K"; data/slurp.py:679 _canonical_helmer prefix-matches ("" -> Waymo); :861 build_vmt_csv silently drops cells past index 8; :1165 r.get(...,"") blanks Weather-Partly-Cloudy for all 920 archive rows (column absent from the archive CSV); :1259 incidents.js is written before vmt.js markers are checked (non-atomic pair); :278 NARRATIVE_TYPOS misses '[XXX}', '{XXX]', '[XXXX]', ' XXX]', '[XXX at' (11 sites); :1285 console passenger summary counts "No Passengers in Vehicle" (486) as with-passenger; :1134 the Monthly-filing guard's premise ("none can exist") is false — Tesla files Monthly within the incident month and the Feb-2026 release held 4 JAN-2026 Monthly rows, so the guard can trip on a correct cutoff (make it a re-review tripwire in words, or tolerate same-month Monthly filings). [MINOR — numbers vs sources] "six cities" -> five areas (Phoenix, SF Bay Area, LA, Austin, Atlanta) at crashla.js:475/625/630/728/737/758/765 (three are user-visible src strings), human-benchmark-provenance.qual:3, IGNOREME:79, this ledger; Lyft "0.94" is a derived 2021-22 mile-weighted average, not a printed rate (single-year max 1.02 -> lo 98M; the ledger's "0.74" is 0.76); Blincoe 812013 says 24% of injury crashes unreported (32% is the 2023 revision 813403, not linked); injury HumansUS comment "1.66M injury crashes / ~3.2T VMT" is 2022-vintage (813791: 1,676,700 / 3,294B -> 0.90-0.93 IPMM, band ok within 3%); nonstationary HumansUS hi 310k applies 3% where the AV hi applies 5% (316k); Kusano Table 3 observed any-injury is 1.92 (results table 1.91); Uber/Lyft srcLinks 404 (live: uber.com/us/en/about/reports/us-safety-report/ and lyft.com/blog/posts/2024-safety-transparency-report); FLEET_FORECAST comment fits B to a "~23%" market the page now shows at 9.9% (model implies 22%); vmt.csv Waymo Oct-Dec-2025 rationale "x1.0653" is actually x1.103 after the re-chain (IGNOREME:237 "56.4% CA share" -> 54.7%); Jan-Mar-2026 rationales "52.9% CA share" -> 54.7%; Zoox 2024-06..2025-05 kyoom_max = 1.5x cume vs the stated 0.5x-2x band; the 2023-11..2025-01 rationale still calls 71M/96M/127M "exact centrals minus 0.15M"; zoox-vmt-provenance.qual:12 and IGNOREME:142 say Aug 6 (Aug 5). [MINOR — stale comments/docs, all confirmed by reading] crashla.js:660-663 calls the 94% share a LOWER bound while the rendered srcNote (:674, ratified 08-28) says UPPER — the arithmetic (lo = injury lo / 0.94) needs upper; :834-839 "~1.2x safer" is ~1.4x after the fatality re-vintage (bands no longer overlap); :2947 "when Tesla+Zoox VMT begins" (Zoox begins 2024-05); :456-458 METRIC_DEFS entries carry no "line style"; :2463 "end-May-2026" base month (history now runs to 2026-07); :2492/2515 "carried to today" (ends at the NHTSA month); :1529-1533 incCovMax attributed to five-day incidents (it is Monthly-track completeness); :1641 legend comment says non-rendering helmers are dropped (they stay grayed); :3108 "pre-analysis-window incidents lack fault data" (none do; the quals forbid it); :2895-2896 nonexistent metricOpacity; :2820 `At-fault" here means` lacks its opening quote; :2949 slider thumb 16px in comment vs 18px in style.css (tick inset math off ~4.5px); :631/766 the Waymo hub URL is listed twice in injury/SSI+ srcLinks (renders duplicate links); :174-176 "within ~1e-4 relative" holds for windows but not the widest single-month bands (3-9e-4, from the ±4σ truncation); :85 approxGammaQuant "few percent" is 3x off at a=0.5 (absorbed by the bracket); :2042 stale market anchor; quals/distribution-chart.qual:507 section-7 header describes the retired MLE/mpiPoint marker; incident-coverage.qual:83/154 "p_best = 1.0"; slurp.py:3-5 docstring (filter "None" only; single-stage dedup) and :1056 "driverless only"; :831 active_months "(months with incident data)" — vmt.js holds 13 zero-incident months; :412 AIRBAG_OVERRIDE comment "columns say No" (30270-6542 CP is Unknown); IGNOREME:61/79 "two cohorts" (three; Uber/Lyft is orange, default off), :61 "helmsbeing" label does not exist, :64 omits Remote (Commercial / Test), :82 "all benchmarks are surface streets" (fatality is all-urban, HumansUS national); data/README.md:57 points at field-overrides.qual for vehicles-involved pins it lacks (fatality-guard.qual), :47-63 step order differs from main() (VMT master first, then fault sync, window, overrides+join); site.webmanifest theme/background #f3f8ff vs the page's #fffdf8; sanity copy :3805 "the very start of our default date window" (June 16 is mid-way through the first month — human copy). VERIFIED CLEAN (worth knowing): every numeric primitive (lgamma, gammainc, gammaquant, Poisson-binomial, two-piece prior, marginal CDF/quantile/density) matches scipy to <=1e-4 on all 11 window cases and 4 per-month cases; the pooled incident_coverage (12/36.04 -> 0.3329, lo 0.1345) and every rendered sanity number reproduce exactly; vmt.csv running sums, band orderings and the Waymo bottom-up Apr-Aug rows reproduce to the mile; the Waymo hub, Kusano, 813791, IIHS, 812115 literals all confirmed at source (WAYMO_PUBLISHED_IPMM 0.71/0.30/0.01 confirmed); parseVmtCsv <-> build_vmt_csv <-> README contracts consistent; archive+current never share a Report ID; no qual is network- or clock-dependent. NEXT: human decides on (a) the Tesla Jul/Aug proposal, (b) items 1-3 and 5-6 (mechanical fixes with quals), (c) the methodology calls 7-9 and the forecast/benchmark re-derivations 10-11, (d) the faultfrac re-adjudication list, (e) the human-copy flags in item 4; then the Sep 15-17 NHTSA release (edit NHTSA_DATA_THROUGH_DATE, re-measure the receipt triple WITH the corrected table, expect the Dallas Aug-8 fatality) and the Waymo hub update (~mid/late Sep). Memory: project_vmt_research_2026_09_04.md.

- 2026-08-28: CODEX 08-24 SESSION UNTANGLED + NHTSA RECEIPT-COVERAGE MODEL + WAYMO HUB+E REBUILD (human: "can you look at sourcery.html to see what codex did? i think it made a mess of things and we to very carefully untangle it. for starters, we seem to have lost our estimates of uber/lyft drivers ... slurp.py is failing ... do your own research ... to see if we can update our VMTs"; then, one decision at a time: drop the byte pins; one card line; restore the at-fault bands as ratified ("say more about why you recommend no change? we do want this exquisitely right" → explained, kept); add Aug rows, July unchanged; on the hub anchor: "isn't making a guess the principled thing to do? we always want to do the least sloppy thing. can you review and make sure previous decisions have been in accordance with that? we also want to make this easy to update as new data comes in" → full Waymo rebuild + three amendments approved: empirical five-day coverage band, at-fault src label precision, fatality band re-vintage). ALL 59 QUALS GREEN (56 restored + 3 rewritten/new); live Chromium render zero console errors; dist-window.gif regenerated. WHAT CODEX (sourcery p287, 2h23m unattended) HAD DONE, AND THE VERDICT: (a) found the real bug — the NHTSA CSV holds reports RECEIVED through the 15th of the prior month (verified here from snapshot history: each release's newest incident-month is ~12-18 five-day-type filings that grow ~6x next release; June-2026 had 60 five-day-track incidents of which only 17 were in the Jul-15 file), so HEAD's five-day exemption credited a full July of VMT against ~a third of a July → KEPT, re-implemented; (b) pinned the CSVs' exact Last-Modified/ETag (+ a snapshot MD5 in a qual) → this is what broke slurp: NHTSA re-published on Aug 27 with ZERO new reports and exactly one changed row (Stack AV 34952-11803, a further CBI redaction) → REVERTED; (c) deleted the human at-fault benchmarks for every cohort (that is the lost Uber/Lyft band) and grayed all human curves on all/nonstationary/roadwayNonstationary via stressComparable:false; flipped the default metric to injury; removed faultFlipMultiplier, the fault-sensitivity table and the card "Overall" line — reversing the 2026-06-12 default and the 08-24 deliberate no-change on M3 → REVERTED; (d) replaced the HUMAN's abstract headline (d8d1c78) and the human-written sanity paragraphs (incident coverage, VMT uncertainty, cross-check, sensitivity, benchmark derivations, Poisson) with Latin, and wrote abstract-claims.qual asserting the human's own phrases are ABSENT → REVERTED (rule 6), quals deleted; (e) two exposure lines per card + a "Via" column → REVERTED to one line (five-day denominator in the tooltip); (f) full-month VMT for the trend charts, five-day dots no longer drawn as incomplete, colophon cutoff clause → KEPT (re-ported). Mechanism: crashla.js/index.html/style.css/IGNOREME.md/dist-window.gif and 12 quals restored from HEAD via git show; abstract-claims/human-comparability/slurp-release-metadata quals deleted (the last rewritten); Codex's slurp.py kept and reworked. The AGENTS.md "Experimental" edit in the working tree is assumed to be the human's and untouched. RECEIPT-COVERAGE MODEL (replaces Codex's 15/31): the data-through month's five-day-track coverage is MEASURED, not a fraction of days — share of a month's eventual five-day-type incidents present in its first release: Feb .47, Mar .36, May .28, Jun .28 (Apr .03 excluded: the May-15 release was cut early, 2 April incidents/19 April submissions) → FIVE_DAY_RECEIPT_COVERAGE = (0.32, 0.25, 0.48) in slurp.py with the observation table beside it and a must() that the triple brackets its measurements; generated CSV gains coverage_min/coverage_max (15 columns; parseVmtCsv hit[9..14]); five-day metrics use raw×coverage triple, Monthly-track metrics also ×incCov (product invariant; 2026-07 incCov now .3329 [.1345, 1]); app asserts coverage<1 only in the data-through month (replaces the vacuous NHTSA_MODIFIED_DATE ≥ month-end+5 assert — the modification date runs a month after the cutoff). GUARDS, NO BYTE PINS: NHTSA_DATA_THROUGH_DATE is a reviewed constant (one edit per release); its month must equal the newest incident AND submission month; no incident in that month may carry a Monthly filing (Latin must: "menstruas"); slurp-release-metadata.qual regenerates both payloads offline from the NEWEST snapshots byte-for-byte (no filename/MD5 pins), reads the constants from slurp.py, and proves the Monthly guard trips. WAYMO HUB+E REBUILD (human-approved after the least-sloppy pushback): the hub's "All Locations" total counts only BENCHMARKED locations (release notes Jun 12 2025; hub CSV1 files 202406-202603 downloaded and summed: 220.613M = SF 57.761+San Mateo 4.804+Santa Clara 4.513+LA 51.816+Maricopa 80.551+Travis 15.789+Fulton 4.881+DeKalb 0.498) while the repo numerator already holds 41 Waymo incidents Jan-Jun 2026 in Miami/Dallas/Houston/San Antonio/Orlando/Nashville (+27 in Santa Clara County towns). Atlanta was absent from 96M/127M/170.7M and entered whole (5.379M) at 220.6M; Santa Clara entered at 170.7M. Anchors are now hub + E (excluded-metro estimate lo/best/hi; table in data/README.md "Waymo anchors", pinned in waymo-vmt-provenance.qual; milestone STATEMENTS 100M/200M carry E=0): Mar-25 71.432+0.188, Jun-25 95.965+0.7[0.4,1.3], Sep-25 127.158+2.0[1.3,3.1], Dec-25 170.712+3.7[3.0,4.6], Mar-26 220.613+2.0[1.2,3.3]; Feb-2025..Mar-2026 rows re-chained preserving monthly shape (kyoom bands scaled, then monotone passes; Jan-26 max capped at Feb's as before). Apr-Aug 2026 rebuilt bottom-up: CPUC CA actuals (Apr 9.466/May 9.807/Jun 9.685M incl. pilot; ±3%) + benchmarked non-CA at the hub's Q1 per-county run-rates (Maricopa 11.94M/qtr FLAT Q4→Q1, Travis 5.07, Atlanta ~1.9; ±25%) + excluded metros 1.4/1.45/1.8/2.0/2.3M (±40-50%; SGO crash-count proxy cross-checked by fleet/rider figures) → 17.4/17.85/18.1/18.7/20.0M (was 19.0/20.5/20.5/21.0 — those had assumed the CA share falling to 47-50%, i.e. non-CA +50-70% in a quarter; hub county data show CA at 53-55%; June ≈ 4.2M mi/wk matches Waymo's Jun-24 "more than 4 million miles every week"). End-Jul cume 294.5M (was 301.45M), end-Aug 314.5M. Tesla 2026-08 240k [120k,450k] and Zoox 2026-08 255k [178.5k,331.5k] added as wide placeholders (research: no Tesla mileage datapoint after the Q2 deck — the 380k unsupervised figure is from the Q2 CALL, not the deck; TxDMV 270 VINs Aug 27; tracker availability feed dead since Aug 18; Zoox "more than 3 million miles" was Aug 5 not Aug 6, LV fleet capped 65→100 by NTA Jul 16, press "nearly 1M riders" conflates riders+waitlist). vmt-ledger pins → 2026-08. AMENDMENTS: fatality HumansAV band re-vintaged 87M/130M (2012-era 0.77-1.15) → 83M/105M (IIHS urban 2022-24 1.17/1.07/1.01, banded 0.95-1.20; Latin src, pinned red-first in human-benchmark-provenance.qual); at-fault/at-fault-injury bands restored as ratified with a green Latin srcNote (rendered after the src; ONE new branch: note rendered only when srcNote is defined) saying the 94% is NHTSA 812115's driver-attributed-critical-reason share used as an upper bound on expert avoidability. distributionExtent now also covers every curve's posterior median (the narrower fatality band's taller peak had starved the k=0 Tesla/Zoox curves' markers off-frame; distribution-chart.qual caught it). NUMBERS: Zoox all-incidents flips ambiguous→WORSE in the FULL window (ratio ceiling 1.00→0.96; pinned) but stays ambiguous (1.04x) in the default Jun-2025+ window (pinned); five-day denominators ~-7% (Waymo fatality/hosp/airbag 301.45M→281.8M); Waymo at-fault 3.02x-24.6x, injury 2.14x-14.2x; Waymo full-history cross-check injury 0.90x/airbag 0.88x/serious+ 2.04x of published. SPEC CHANGES FOR THE HUMAN TO RATIFY: (1) subset-chain.qual's sanity "rideshare fatality lo ≤ AV-cities fatality hi" replaced by "≤ 2× AV-cities hi" — the sourced Uber/Lyft 106-161M band now sits entirely above the 83-105M urban band, which is the data (rideshare ran 30-50% safer than the urban average in the same years), not nonsense; (2) the Zoox stress pins above. OPEN FOR THE HUMAN: the human's English sentence '"Calendar coverage" is the fraction of the month in the window (e.g., 15/31 ≈ 48%)' now sits beside a table showing 32.0% (25.0%-48.0%) — rule 6 bars editing it; six green-Latin TODO sites await English: the incident-coverage span (Claude's old English replaced), the colophon cutoff clause, the card tooltip's five-day clause, two srcNote strings, the fatality src; the regenerated sourcery.html dropped 3 prompts (2026-07-24 ×2, 2026-07-25); "at-fault"→"expert-avoidable" nominology still open; the fatality band could later be per-county (FARS county deaths ÷ county VMT) instead of national-urban. NEXT DATA: NHTSA ~Sep 15-17 (data through Aug 15: edit NHTSA_DATA_THROUGH_DATE, re-measure the receipt triple from the Aug-28 vs Sep snapshot, author Sep rows); Waymo hub ~mid/late Sep (data through Jun-2026: re-pin Apr-Jun from per-county cumulatives, zero E for any newly benchmarked metro — likely the Texas counties — and re-chain); Tesla Q3 deck ~Oct 21-28; CPUC Q3 ~Nov. Regeneration recipe for dist-window.gif (playwright + PIL: hide the VMT and over-time sections, Zoox on, viewport 1000 → 900px, 234 frames as 18 hold / 9 transition + 6 hold per step / 27 final) lives in the session scratchpad; worth checking in.

- 2026-08-24 (later): CHATGPT AUDIT CORRECTION + CLOSEOUT (same shared audit re-read against the current tree and primary sources; this entry supersedes the same-day entry wherever they conflict). ALL 61 QUALS GREEN. The earlier live Chromium sweep was clean; a final rerun after the last three UI-semantic changes was blocked by the environment's approval-credit limit, but those changes are pinned by default-state, human-comparability, distribution-chart, and sanity-checks quals. FIXES: (1) RELEASE FRONTIER / COVERAGE: the current NHTSA CSV is only through the 2026-07-15 report-receipt cutoff, despite its 2026-08-17 HTTP modification date; slurp.py now pins the exact current Last-Modified+ETag and the archive Last-Modified+ETag, embeds the cutoff in generated artifacts, and regenerates both payloads byte-for-byte offline. July calendar coverage is 15/31=.4838709677, not a full month. The 12 observed cutoff-month incidents versus 54.1 expected from fleet-specific reference rates yield one pooled conditional factor .2220 [.0896, 1.0]. Every fleet with positive cutoff-month VMT participates, including zero-observation fleets; no Monthly/No-New row is treated as proof that a reporting cycle is complete; the pool having no usable reference or no positive cutoff-month VMT fails loudly. The reference-month selection is explicitly a heuristic. (2) TWO EXPOSURE TRACKS: fatality, hospitalization, and airbag deployment use cutoff-day VMT because their SGO triggers carry the five-day guarantee; other metrics additionally use pooled conditional coverage. Summary cards, tooltips, VMT uncertainty, coverage sanity checks, crosschecks, and colophon expose the same distinction. The five-day clock starts at company notice, so cutoff-day exposure is explicitly a proxy, not guaranteed crash-date completeness. Full-month VMT trend charts retain the authored monthly estimates instead of plotting a false cutoff cliff. (3) INVALID HUMAN BRIDGE REMOVED: the NHTSA 812115 critical-reason statistic and a legal-fault floor do not estimate expert avoidability. Both at-fault metrics retain their AV posteriors but have no human benchmark, multiple, verdict, or human density/time-series curve. Selected incompatible human legend entries stay visible and grayed. One shared metricHelmerComparable predicate supplies this compatibility gate to both charts. Injury, not All incidents, is now the sole primary/default metric; all-derived metrics are excluded from the stress verdict table. (4) CLAIMS / PROVENANCE: the headline now limits the robust Waymo claim to directly sourced injury, airbag, and serious-injury+ comparisons and says magnitude varies; Tesla/Zoox beliefs were moved character-for-character under a separate opinions heading. “Confidence” prose became “credible.” The fatality city benchmark is labeled as the author-set historical 0.77–1.15 sensitivity band, with the current IIHS 2024 urban all-road 1.01 anchor. The unsupported freeway-effect magnitude was removed. Waymo's location/day/time matching paper is cited as the stronger comparison; the in-app crosscheck now honestly compares metric-specific effective all-road SGO exposure through the July 15 receipt cutoff with Waymo's surface-street ADS rates through March 2026. Stale docs and quals were aligned. (5) VERIFICATION / ARTIFACTS: three red-first quals were added for abstract claims, human comparability, and exact release metadata/offline regeneration; all affected prior quals were repinned only where the corrected functional specification required it. data/slurp.py compiles; crashla.js passes syntax checking; git diff --check passes; dist-window.gif was regenerated from the corrected app and visually inspected. No mutating git command was run; .claude/settings.json still denies git add/commit/push/branch/reset/checkout/merge/rebase. HUMAN VETTING: all newly generated UI/error copy remains green Latin with adjacent TODO English recaps. These include the abstract headline/opinions heading, fatality-source disclosure, two exposure labels/tooltips, VMT uncertainty/coverage explanations, no-VMT row, dispersion disclosure, AV-only expert-avoidability definition/error, Waymo crosscheck, and receipt-cutoff footer. HUMAN NOMINOLOGY CALL STILL OPEN: whether to rename “at-fault” to “expert-avoidable.” DELIBERATE DISCLOSED LIMITATIONS, NOT CLAIMED FIXES: no fully dynamic fleet-specific geography/day/time exposure matching, no time-varying incident-rate model, and no explicit notice-lag distribution around the cutoff-day proxy.

- 2026-08-24: CHATGPT AUDIT BATCH (human: "review [chatgpt share link] and list the things we need to fix and then fix them"). External ChatGPT audit of the deployed site: 10 findings + 3 minor, all 13 verified against source by a 15-agent workflow before fixing (4 CONFIRMED, 8 PARTIAL, 0 fully refuted; several were stale or already-ratified). ALL 58 QUALS GREEN; browser-verified (no console errors). FIXES: (1) COVERAGE-PER-METRIC (audit #6, the one real modeling bug): the single pooled incident_coverage (~0.107 for 2026-07) was thinning effective VMT for EVERY metric, but Third Amended SGO Request No. 1.D puts fatality/hospital-transport/VRU/airbag/tow-away reports on a 5-day track (verified from the SGO PDF via archive.org; nhtsa.gov 403s direct fetches) — only Request-No.-2 Monthly reports are structurally missing. METRIC_DEFS gained fiveDay: true on fatality/hospitalization/airbag (NOT seriousInjury: a "Serious" severity without hospital transport is Monthly-track, no structural guarantee); mpiByMetric + monthlySummaryRows select the raw calendar-coverage VMT triple for fiveDay metrics (data-driven ternaries, no new code paths); new assert fails loudly if NHTSA_MODIFIED_DATE < month-end+5d while incCov<1 (notice≈crash-date assumption documented). No vmt.csv schema change; slurp.py untouched. Pinned red-first in incident-coverage.qual (fiveDay set + month- and window-level band/VMT selection + assert presence). Effect: 2026-07 fatality/hosp/airbag posteriors use ~9.3x more effective VMT. (2) FATALITY HumansUS BAND 59M/91M -> 84M/91M (audit #1): the AV side's 1/vehiclesInvolved (Koopman/Piper, untouched) fleet-sums to FATAL CRASHES, so the consistent human numerators are deaths (1.19/100M -> 84M lo) and fatal crashes (1.10/100M -> 91M hi); the old lo (1.70/100M per crashed VEHICLE -> 59M) counted involvements whole — the very convention the 1/N division rejects — and was the AV-favorable edge. Supersedes the 2026-08-22 rework's lo. Pinned red-first in human-benchmark-provenance.qual; src now rule-7 Latin. Comments at the fractional-death accumulator + fatality def rewritten to name the numerators precisely (they claimed a pure deaths/VMT basis). (3) ABSTRACT (audits #9/#10): the M5 headline rewrite was applied 2026-08-21 as green ENGLISH and the human reverted it to enforce the rule-7 Latin directive (per memory codex-audit-outcomes), which is why index.html still carried the numerically-unsupported "over 10 times ... no uncertainty" headline the ChatGPT audit re-flagged. Now re-applied as green LATIN with the M5-approved English in the TODO comment for the human to translate/vet. Also split the abstract: items 2-3 (belief/prediction-voiced) moved character-for-character under a new green Latin header "Quae opinamur:" (= "What we think:"), resolving the audit's category error (Tesla "probably safer" no longer under "What we know:"). (4) RIDESHARE MODELED-PROXY STYLING (audit #7): derived: true on the loop-generated HumansRideshare nonfatal bands; derivedBandDash() dashes their strokes on the monthly + distribution charts (reusing the k=0 dash idiom); sourced fatality band stays solid. New derived-band-styling.qual (red-first). Construction (1.2x/1.5x) untouched — ratified 2026-06-16. (5) LABEL/DISCLOSURE FIXES: fatality HumansAV src dropped the false "surface-street" (IIHS table is ALL urban roads — which matches the AV all-roads scope; audit #5); METRIC_DEFS blanket "all benchmarks are surface streets" comment scoped per-metric with the freeway-era caveat (audit #4); helmerHumanStress "apples-to-apples" comment softened (band spans per-city extremes, Zoox LV not among hub cities); NHTSA-812115-disclaimer NB added to the 94% derivation comment (audit #2 — numbers unchanged, M3 ratified); green Latin at-fault definition (P(expert avoids), 50% legal floor as bound) added to the sensitivity para (audit #3 — first user-facing definition); green Latin overdispersion sentence pointing at the date-range slider added to the Poisson-dispersion section (audit #8 — no model change: trend model rejected 2026-06-12, slider is the sanctioned mechanism); "confidence"->"credible" (audit #11) NOT applied — one-word AI-English swaps are barred by the standing rule-7 directive; PROPOSED to the human in a comment at the Poisson-dispersion sections.push (the two "confidence" occurrences are the only user-visible ones; short "95% CI" labels kept, CI = credible interval per repo convention); hospitalization HumansAV src gained a Latin no-direct-source clause (audit #12); IGNOREME.md Uncertainty-bands section now states the ±X%-shorthand = exact-95%-interval convention (audit #13, S1 unchanged); sanity coverage table gained a Latin sentence disclosing the per-track split; stale comments fixed (parseVmtCsv "historical 5-Day fraction", sanity-checks.qual dispersion 21/~15 -> 24/~12.3). NOT DONE (deliberate, with reasons in the chat report): abandoning fractional deaths (documented+cited, fixed the human side instead), dropping the 94% (M3 ratified), full exposure-matching/Waymo-faithful mode (Phase 3 closed), time-varying Poisson model (rejected 2026-06-12), rideshare band re-sourcing (ratified), renaming the at-fault metric (rule-13 human decision — "expert-avoidable"? flagged). VETTING/TRANSLATION LIST (all Latin, TODO English recaps in-place): index.html headline (M5 English in the comment) + "Quae opinamur:" header; fatality-HumansUS src; hospitalization src clause; sanity-coverage sentence; dispersion sentence; at-fault definition. PROPOSED (human's word needed): "confidence"->"credible" at the two prose sites; at-fault metric rename (rule 13). WATCH: Robotaxi Tracker's Tesla feed is provider-blocked since ~Aug 18 (from the same ChatGPT thread; FSD-Database receipts confirm rides continued through Aug 20) — Aug-2026 Tesla VMT will need a different source or wide bands.

- 2026-08-22 (later 4): CLOSING VERIFICATION SWEEP + RESIDUE FIXED (human: "i translated the latin. is that everything? are you sure?"). A 4-agent closing workflow (diff-vs-ledger audit of the full 48b102a→tree delta hunk-by-hunk in both directions; placeholder/Latin/stale-docs sweep; live rendered exercise across 7 slider windows + metric/toggle/section states with zero console errors; loose-end classification of every ledger PENDING/watch item) verified EVERYTHING claimed in the 2026-08-21/22 entries exists exactly as described (all re-anchor/faultfrac/override/benchmark numbers spot-checked; zero unexplained hunks; generated files field-identical to masters) and found the following residue, now fixed: (1) CRITICAL FOR THE NEXT COMMIT: the human's "Peak" translation (committed in 6ef93d6/d7c0e20) left the committed distribution-chart.qual still pinning /^(Culmen|Median):/ — HEAD ALONE IS RED; the re-pin to /^(Peak|Median):/ sits in the working tree and must be committed (the "REMAINING TODO: Culmen" line in the later-3 entry below is hereby retired — the English landed). (2) Ledger corruption repaired: commit 1710b88 had merged the "2026-08-21 (later): ROUGHEN FILTER RIPPED OUT" entry headerlessly onto the tail of the (later 2) entry; re-split with the header restored verbatim from 48b102a. (3) arxiv1.xml DELETED from the repo root — a raw arXiv search dump a freshness-sweep agent strayed into the repo (against its scratchpad-only instructions) and 6ef93d6 swept into the commit; referenced by nothing, publicly served, unledgered. (4) dist-window.gif regenerated against final data (had been skipped in the 08-22 batches despite the 08-21 regenerate-after-data-changes convention; the Zoox at-fault curve is materially different post-rescore). (5) Stale-doc fixes: IGNOREME.md:79/81 human-comparison attribution → the live hub (correct link; per-metric extreme cities; blendeds 3.91/1.68/0.23; the "no US bands for hosp/airbag/serious+" claim corrected to log-interpolations), the −150k convention noted at the three "exact 170.7M / 220.6M by construction / floored at 220M" spots; crashla.js injury+airbag metric comments completed to hub attribution (were half-updated), atfaultInjury cross-check inputs 478k→493k/960k→990k, makeMarginalMpiCdf docstring "log-normal"→"two-piece log-normal"; monthly-series.qual SSI+ Expectata → hub wording (the sibling airbag check had been updated, this one missed); field-overrides.qual header "six rows"→"eight rows"; slurp.py 08-22 override batch got an end-of-batch banner so the resumed 07-15 rows aren't misattributed; data/README.md pipeline steps now document the two-stage dedup + split exemption + the override pass (renumbered); the human's SGO caveat block wrapped in its own <p> (words untouched); index.html theme-color #f3f8ff→#fffdf8 (pre-reskin leftover). (6) Memory files un-staled (codex-audit description/statuses; faultfrac scenario-map's reversing bullet now carries the A/B rule inline; "not committed" note retired). NOT fixed, deliberate: waymo 2026-06 "May 21" freeway date (the loose-ends agent re-flagged it; the 08-04 entry already REFUTED that nit — May 21 stands); .stress-table body-width overflow (pre-existing, deliberately untouched per the sketch-skin entry); index.html:38 headline + crashla.js monitor sentence (human copy, standing flags by the human's own choice); "Zoox is probably fine" (human copy — flagged in chat for a deliberate re-vet now that Zoox at-fault reads "ambiguous"). All 57 quals green; live render clean.
- 2026-08-22 (later 3): REMAINING OPEN ITEMS EXECUTED (human: "go ahead on the open items"). ALL 57 QUALS GREEN. (1) BENCHMARK REPIN TO THE LIVE WAYMO HUB (the S8 source-choice call, extended to all three per-city sets once the live hub was scraped — it updated injury and SSI+ too, not just airbag): human-benchmark-provenance.qual re-pinned RED-FIRST to the hub's per-city extremes (thru Mar 2026, six cities incl. LA/Austin/Atlanta; supersedes the Kusano 56.7M paper — same methodology family, updated denominators): injury 125k/478k → 138k/493k (2.03 PHX .. 7.25 SF, blended 3.91), airbag 433k/704k → 334k/840k (1.19 LA .. 2.99 ATL, blended 1.68; Austin, Tesla's market, at 2.53), SSI+ 2.17M/8.33M → 2.27M/8.33M (0.12 .. 0.44, blended 0.23); derived atfaultInjury band 133k/956k → 147k/986k (same documented shares formula on the new injury edges); srcs/comments updated, hub srcLink added, monthly-series.qual's loose airbag sanity range widened to [300k, 900k] (the exact-edge pin lives in the provenance qual). NO verdict changes (ratio shifts ≤ ~16%; Waymo injury/airbag/serious+ 'safer' pins hold at 2.46/3.24/3.54; Tesla atfaultInjury 'safer' margin narrows 1.072 → 1.039). (2) FATALITY HumansUS BAND RE-DERIVED ON FARS 2024 PRIMARY DATA: 61M/83M → 59M/91M. A research agent verified from 813791 + the raw FARS CSVs: 2024 = 36,297 fatal crashes (1.102/100M VMT), 56,011 in-transport vehicles (1.700/100M), 3,294,031M VMT — AND found the OLD band's input was wrong, not merely stale: the "~1.65/100M per crashed vehicle" (implying ~54k vehicles) does not reproduce from the FARS 2023 final file, which has 58,508 vehicles ≈ 1.80/100M (like-for-like 2023 lo should have been ~55.5M, not 61M). New src cites FARS 2024/813791; stress verdicts unaffected (the stress cohort is HumansAV, whose IIHS-urban fatality band is untouched). This also completes the FARS-2024 vintage sweep — no remaining 2023-derived edges. (3) MODE → PEAK: distribution-chart marker label "Mode"/"mode" → rule-7 Latin placeholder "Culmen"/"culmen" + TODO recap (intended English "Peak"); distribution-chart.qual's label pin updated to accept Culmen with a re-pin-on-English note (its Expectata had also drifted — said "Most likely:" while asserting Mode). (4) vehiclesInvolved HYGIENE: +56 narrative-verified 3+-vehicle overrides added from the audit inventory (zero displayed change — only fatality reads the field; six murky bystander rows keep the default; maintenance-scope comment updated). (5) GEOGRAPHY: new STATE_OVERRIDE in slurp.py patches 30270-7054's upstream "Phoenix, CA" to AZ (narrative says Arizona; kills the phantom city; pinned in field-overrides.qual), and the Geography table's "# cities" no longer counts "Unknown" (still listed). All regen'd offline; live render zero errors. REMAINING TODO (rule-7 English for the human): "Culmen" → presumably "Peak" (marker label + median tooltip cross-ref + qual pin). Fatality HumansAV (IIHS urban 0.77-1.15/100M) is now the oldest-vintage benchmark — refresh when IIHS publishes 2024 urban splits.
- 2026-08-22 (later 2): STAGED BATCH EXECUTED (human: "sounds good, keep going" on the staged list). ALL 57 QUALS GREEN (56 + new field-overrides.qual, red-first). (1) SEVERITY_OVERRIDE +8 rows under a scope-extension banner (convention now covers AFFIRMATIVE field values flatly contradicted by the filing's own narrative, beyond the original Unknown-resolution scope): 30270-11565 → Minor W/O (def-window Waymo injury 116→117); 9789/10892 → Minor W/, 9987/9992/9860 → Moderate W/ (archive-era bare tiers with stated transports; full-history hosp 51→56); 11450 W/O→W/ (in-window hosp 44→45); zoox 30610-10473 → Minor W/O (Zoox injury 9→10; full-history Zoox atfaultInjury k 0.2→0.6). (2) NEW AIRBAG_OVERRIDE mechanism in slurp.py (+application line): 5 rows where narratives assert deployment the SGO SV/CP columns structurally cannot record (30270-9767/10174/10573/13877/6542; def-window airbag 48→49, full-history 73→78). NOTE: iid 2e94dccbdb96501 (30270-6542) carries BOTH overrides — the no-contact bystander crash; its striking-vehicle airbag counts because the incident itself counts. All 13 pinned in quals/field-overrides.qual.mjs (red before regen, green after; guards regen silently dropping them). (3) FARS 2024 vintage: crashla.js benchmark-block comment now cites FARS 2024 1.19/100M (2023: 1.26); the CRSS derivation comment re-derived on 2024 inputs (6.18M crashes, 3,294B VMT → same ~3.3/~7.1 IPMM, band edges unchanged); all ten 'NHTSA 2023 crash summary' srcLinks → 'NHTSA 2024 crash summary' 813791. DELIBERATELY NOT DONE: the fatality HumansUS srcLink stays FARS 2023/813705 and hi stays 83M — those edges derive from 2023 per-crashed-vehicle (~1.65/100M) and per-fatal-crash (~1.2/100M) rates with no verified 2024 counterparts; swapping citation or edge without re-deriving would mix vintages within one band (the exact defect class the rideshare-band audit flagged). Revisit when 2024 sub-rates are verified. (4) RIDES-CORRIDOR REWORDING (the 08-19 human-gated flag, now approved): crashla.js RIDES_HISTORY comment + rides-provenance.qual comment and Expectata now state the honest basis — author-set ~4-5 mi paid ride, corroborated by robotaxitracker receipt-synced trips (n~2,400 ex hops, mean 3.81 mi, contributor-skewed low); the once-cited Electrek 2026-04-30 article is fleet-counts-only. Corridor numbers and the [4.5, 8.5] pin unchanged. (5) VEHICLES_INVOLVED comment now documents the fatality-only maintenance scope. (6) FALLEN-UTILITY-LINE TIER SETTLED via 3-assessor+adjudicator (all three converged; the bucket proved to be 12 rows — the audit's 7 plus 10076/10602/11001/11195/13096). NEW MAP RULE (canonical): fallen utility line / low-hanging wire or cord across the travel lane = hard-to-see-stationary anchor, band 0.2-0.4, NEVER 0.15 (that tier is the unforeseeable-OTHER-PARTY family; no other party here). Blind daylight contact = 0.3 (retains the 2026-06-15 note's default); blind contact in DARKNESS = 0.2 (band floor; thin unlit cable near-invisible in headlights; supersedes the note's flat 0.3 at night; judge day/night from clock time); AV demonstrably HAD the information — slowed/maneuvered for the line, or a pedestrian visibly held the wire aloft — yet contacted = 0.4 (band top, saw-and-continued analog; capped since a full-lane sag can leave stopping the only out). Includes non-utility cords strung across a roadway; excludes chains across lot entrances (1.0), poles (1.0), road-surface debris (0.1), objects falling onto the AV (0). Applied: 9873/10076/11001/11195/13514 0.15→0.3; 10602/11546/13509 0.15→0.2 (night); 13096/13256 0.15→0.4 (info-in-hand); 14943 0.3→0.2 (midnight); 14828 stays 0.4 (confirmed, now rule-grounded). Net Waymo at-fault k: def-window 85.35→86.15, full 149.20→150.50 (verdict margins untouched). Cumulative at-fault movement this audit cycle: Zoox def-window k 0.2→1.5 (verdict safer→ambiguous, bias fix — see previous entry), Waymo 85.15→86.15, Tesla 6.75→6.65.
- 2026-08-22 (later): SELF-AUDIT (6-angle, 10-agent workflow w/ adversarial challenges; human: "any more audits we can do on our own numbers and math?") + FIXES. CLEAN: vehiclesInvolved (both fatality overrides re-verified word-by-word vs narratives; fatality-guard pins correct; crashla.js:1376 is the ONLY metric consumer; Tesla/Zoox have zero multi-vehicle candidates) and the full sanity-section independent recompute (every table matches scratch-python rebuilds; 3 low notes: Waymo cross-check uses the thinned denominator — conservative; Geography lists a phantom "Phoenix, CA" and counts "Unknown" as a city — cosmetic). APPLIED (all 56 quals green after): (1) 7 docs-drift fixes — IGNOREME.md brought current on the two-piece prior (line 71), three-milestone Zoox anchoring (142), Waymo Apr–Jun ±15-16% CPUC-tightened bands (65, 234), the freeway-recall now-covered-by-the-series framing (242), the stale "~503 incidents" Piper count (109); distribution-chart.qual 1b header comment (log-normal → two-piece). (2) 15 map-mandated faultfrac ALIGNS (challenger-verified; mostly pre-window): 2023-24 chain/rope strikes 0.7/0.6→1.0 ×5 (30270-5318/6838/7729/7936/7947 — the 1.0-for-chains convention had crystallized mid-rebuild and early rows were never lifted); APR-2026 tire-debris zeros→0.1 ×3 (14705/14831/14933, map value; the 0.2→0.1→0 three-era drift was date-correlated); door-into-VEHICLE 0.3→0.1 ×4 (8317/8393/8603/8633 — 0.3 is the VRU tier, no VRU present); liftgate 10274 0.7→0.4 (visibility band, aligns 11771); Tesla 13781-13646 curb 1.0→0.9 (map curb tier; 13781-11986 gooseneck STAYS 1.0 per challenge — visible-object class); gate 8602 0.5→0.6. (3) 23 RE-ADJUDICATED rows via the locked 3-assessor+adjudicator workflow (12 agents, unanimous or 2-1 everywhere): (a) THE BIG ONE — all 12 Zoox moving-AV ROW-violation zeros rescored on narrative FACTS without crediting Zoox's "abruptly/contact-was-unavoidable" framing (the audit showed 8-of-8 in-bucket Zoox rows at 0 vs Waymo's 73%-nonzero, P~3e-5; framing-credit was the cause): 30610-7819/11847/14263/14341/14342/14656/15018→0.1, 11587/14026/14366→0.15, 11497→0.2, 13660→0.05, each calibrated to a named Waymo comparator. CONSEQUENCE (deliberate, correct): Zoox at-fault k 0.2→1.5 default-window (0.6→2.0 full), CI [755k–3.53B]→[374k–222M], ratioLo 1.76→0.87 — VERDICT FLIPS "robustly safer"→"ambiguous" (both windows; atfaultInjury likewise via 14341's 0.1). Zoox rows were the ONLY fleet scored with framing credit; this removes a pro-Zoox bias. Verdicts were deliberately unpinned so no repin needed. (b) reversed-into-while-passing family (9 rows) put under ONE adopted rule, now map-canonical: other vehicle already VISIBLY reversing or work-vehicle visibly mid-operation at the AV's commit → 0.25; simply stopped with reverse starting only after commit → 0.15 uniformly; never 0 for a moving AV. Applied: 8833→0.25 (forklift mid-op), 14735→0.25 (visibly reversing), 10893/11221 0.1→0.15, 13024/13067/13202/13255 0.2→0.15, 14227 0.25→0.15. (c) gates: 7877 0.5→0.9 (gate was STATIC partially-open — visible-obstacle self-strike tier), 8357 0.5→0.6 (map closing-gate). SCENARIO-MAP ADDITIONS from this batch: fallen-utility-line bucket UNRESOLVED (see staged); tire-debris-in-lane = 0.1 confirmed; reversed-into rule A/B above; framing-discipline note (score facts not reporter adjectives — applies to ALL fleets). STAGED FOR HUMAN GO (exact diffs in chat): (i) SEVERITY_OVERRIDE batch — 6 rows whose narratives flatly contradict structured severity (30270-11565 alleged-injury→Minor W/O [default-window injury 116→117]; 9789/10892→Minor W/, 9987/9992/9860→Moderate W/ [archive-era bare tiers, narratives state hospital transport; full-history hosp 51→56]; 11450 W/O→W/ [in-window hosp 44→45]; zoox 30610-10473 v2 injury claim never fielded→Minor W/O [Zoox injury 9→10 + adds 0.4 to full-history Zoox atfaultInjury]) — extends the override convention beyond Unknown-resolution, needs ratification; (ii) NEW AIRBAG_OVERRIDE mechanism — 5 rows (30270-9767/10174/10573/13877/6542) where narratives assert deployment but SGO SV/CP columns can't record it (third-vehicle class; NOT the fixed CP-column issue); (iii) rides-corridor rewording (3 spots, human-gated since 08-19; robotaxitracker receipt-mean 3.81 mi is the replacement corroboration); (iv) fallen-utility-line tier: 8 rows at 0.15 vs the documented 2026-06-15 batch note saying 0.3 and the criterion's 0.2-0.4 hard-to-see band — one-time bucket adjudication after the human picks the tier (14828's 0.4 and 14943's 0.3 conflict with the 0.15 majority either way); (v) FARS 2024 vintage updates (see previous entry). Forecast audit: rides-corridor provenance CONFIRMED as flagged (now crashla.js:2265-67 + rides-provenance.qual:39-40+55); market-fit/Tesla-A/fleet-history staleness NOTED for the next forecast re-authoring (human call). Narrative-consistency NOTES (no action): month binning 8402, location/time noise, animal-injury severity = methodology.
- 2026-08-22: FRESHNESS SWEEP (7-agent workflow; human: "what's next? any new data published anywhere?"). NO changes to existing numbers — every current row/constant re-verified against live sources (Waymo hub byte-stable 220.6M-thru-Mar / 3.91/1.68/0.23; NHTSA CSV still Aug-17; EDGAR quiet; TxDMV flat 186/691/44; trackers' mileage models all still retired; CPUC nothing past the Aug-18 Q2 posting). Predmarket snapshot refreshed via the checked-in script (2026-08-04 → 2026-08-22; 1 Polymarket + 11 Manifold; no new resolutions; the human's commented-out resolved-market block dropped from the file as its own note predicted). PENDING HUMAN GO — FARS 2024 vintage update: FARS 2024 annual data published Apr 2026 (813791: 39,254 deaths, 1.19/100M VMT, 2.42M injured, 6.18M police-reported crashes, 3,294B VMT): proposed (a) crashla.js:759 srcLink FARS 2023/813705 → FARS 2024/813791, (b) :478 comment 1.26 → 1.19 (2023: 1.26), (c) ten 'NHTSA 2023 crash summary' 813705 srcLinks → 813791 (CRSS-derived band edges verified UNCHANGED at displayed precision under 2024 data), (d) OPTIONAL: fatality HumansUS hi 83M → 84M (1/1.19e-8; lo 61M HELD pending a 2024 per-crashed-vehicle figure). NHTSA 2025 EARLY ESTIMATE 36,640 (813800) is a projection — context only, never an anchor. TWO NEW OFFICIAL VMT STREAMS COMING (watch items, both previously unknown to the repo): (1) CA DMV AVIM 2026-001A (13 CCR Art. 3.7/3.8, eff. Apr-28): manufacturers file MONTHLY (from Aug 26, 2026) + quarterly (first due Sep 30) reports incl. VMT per §227.60 in mandated CSVs — Waymo+Zoox driverless and Tesla drivered CA; public-release timing unstated, watch DMV late Sep/Oct. (2) Zoox exemption Operational Authorization (docket NHTSA-2025-0523-0133, posted Jul-31, repo previously cited only -0004): Condition 21 = monthly per-operating-area VMT + max-fleet emailed to NHTSA by the 15th (not auto-docketed; FOIA-able; first ~Sep-15); Condition 23 = state reports forwarded within 5 days; public operating-area maps within 14 days of change. LEDGER FACTS (no row changes): Nevada NTA approved full Clark-County paid permits Aug 20 (Tesla ≤5,000/12mo — supersedes the Jul-27 10-vehicle interim note; Waymo 1,000; Uber/Aviari 1,000 via Motional AND Zoox; Zoox 100; airport pending) — service NOT started, watch for actual launches. CPUC approved Waymo AL-0004/0004-A Aug 14: 18-county CA ODD (Bay Area, Sacramento, SoCal to the border) + fared Ojai(Zeekr) platform; San Diego rider-only "later this summer"; cite in the waymo 2026-08 rationale when authored. Waymo freeway resumption effectively COMPLETE by mid-Aug (Jul-29 Phoenix; LA/SF-Bay "coming days"; SFist Aug-14 past-tense) → author waymo 2026-08 WITHOUT freeway damping. Waymo non-CA: Dallas public Aug 4 (~150k interest-list riders since Feb), Houston public Aug 20 (100k+); PREVIOUSLY UNLOGGED: Feb-24-2026 simultaneous select-rider launch in Dallas/Houston/San Antonio/Orlando — explains the Q1→Q2 CA-share decline (52.9%→~44-49%). Tesla fleet-hold: JPMorgan (Aug 20, analyst relay) says Tesla intentionally holding Model-Y adds until "late 2026 into early 2027" betting on Cybercab; TxDMV still exactly 186 VINs — supports the flat 2026-07 central and argues against a big 2026-08 ramp. Cybercab: event Sep 3 Austin; exactly 3 photo-verified test mules (Dallas, steering wheels) — NO Cybercab miles in any denominator yet. DATA-QUALITY GUARD (extends the Aug-18-19 note): Cybercab-sweepstakes ride-farming contaminates Tesla receipt data across the WHOLE Aug 17-23 window (878 trips since Aug 15, median 0.15 mi; 654 sub-0.5-mi hops of n=990; mean ex-hops 3.14 mi vs Jun 4.20) — never calibrate mi/ride or receipt-panel scale on it; tracker registry jump 1,627→2,072 is a Waymo-plate backfill, not growth. Zoox recalls the repo had NEVER logged: 26E044000 (smoke/emergency-scene software, Jul-10; 105 units = the ENTIRE deployed driverless fleet — citable fleet-size datum; no halt; verified not SGO-reportable) and 25E090000 (lane-crossing, 332 units, remedy complete 2025-12-19); plus NHTSA Administrator Morrison's ~Jul-10 first-responder directive to all AV developers. Zoox LV paid era (Aug 10): 65 robotaxis at launch per CEO (cap 100; refines "roughly 50"), fare test $22.62 vs Lyft $14.44, NO published ridership/mileage; community layer fully blind on LV. Tesla numerator caveat citation available: NHTSA Audit Query AQ25002 (opened 2025-08-20, STILL OPEN in the ODI flat file) — federal audit of Tesla's SGO reporting compliance (~2.6M vehicles); also open: EA26002, PE25012; no robotaxi probe from the Aug-17 bollard event. SGO rules: FR 2026-15483 (Jul 31) confirms the Third Amended order is current (our caveat stands verbatim) and NHTSA is drafting an NPRM to codify SGO reporting (RIN 2127-AM63) — numerator-rules watch item. CITATION GUARD: Hunter College/Sam Schwartz + Open Plans RETRACTED their NYC safety analysis Jul 23 ("data discrepancies"; the withdrawn claim was Waymo 1.5x NYC FHV serious-injury rate — flagged as a counterpoint in the 08-04 entry; NEVER cite it). TNW Aug-20: 170/170 Austin rides unsupervised, ~28 active unsupervised vehicles (same McKanna source family — volume, not independence). New tracker sources for the ledger: fsddb.com (receipt-crowdsourced, ~3-mi avg = known contributor bias; [4.5,8.5] qual pin stands), robotracker.app (service areas, no mileage). Reddit still blocked four ways.
- 2026-08-21 (later 4): S1 RESOLVED (TWO-PIECE VMT PRIOR) + LATIN RETROFIT + PRIOR-ONLY BADGES + HYGIENE (human: "ok, go ahead. (also i changed my mind: we have to follow everything in AGENTS.md fastidiously, including latin copy; making it green isn't enough)"). Applies to the working tree on top of the human's "Fixes" commit (which took the "later 3" batch, REVERTED the index.html headline to the original human copy — leave it — and commented out the sanity model-sentence). (1) LATIN RETROFIT (rule 7): all AI-authored user-visible copy from "later 3" converted to Latin placeholders, each with the required TODO recap comment for the human's final English: the SGO carve-out caveat paragraph ("Etiam: ordo NHTSA tertium emendatus…"), the caveat clause in both 'all'-metric humanMPI src strings ("caveat: ab 16 Iunii 2025…"), the summary-card label ("VMT efficax:") + its tooltip lead sentence, and the sanity model sentence ("Omne intervallum credibile monstratum…" — reinstated visible in place of the human's commented-out English version; delete the span if no such sentence is wanted). vmt.csv rationale strings left in English — they are data-provenance documentation in the long-established all-English rationale corpus, not new UI copy; flag if that reading is wrong. sanity-values.qual's caveat pin re-pinned on the Latin markers ("tertium emendatus", "16 Iunii 2025") — re-pin again when the human writes the English. (2) S1 RESOLVED — two-piece (split) log-normal VMT prior, best-at-mode (new splitPriorSigmas; sigmaLo = ln(best/min)/1.96, sigmaHi = ln(max/best)/1.96; density continuous, mode at vmtBest; mass outside the authored band exactly 5%, split sigmaLo:sigmaHi between the tails; bit-exact degeneration to the symmetric prior for log-symmetric bands): applied consistently in makeMarginalMpiDensity, makeMixtureMarginalMpiDensity, makeMarginalMpiCdf (adaptive node count now 1 + 8(sigmaLo+sigmaHi)sqrt(aMax)), and the quant brackets. Matches the forecast machinery's split-log-normal best-at-mode convention (AGENTS 07-01 item 5). RED-FIRST pin in fault-mixture.qual: estimateMpiWindow(k=2, [55k, 170k, 340k]) quantiles pinned to an INDEPENDENT scipy integration (lo 14,996 / median 67,703 / hi 419,456, tol 0.3%) — the symmetric prior gave lo 18,689 (red). Tail-split check: prior mass below/above the authored endpoints 0.03093/0.01900 (sigma-weighted), vs the symmetric prior's 0.0076/0.0680 (92.45% coverage — the S1 finding). IMPACT: mildly asymmetric ±30%-style bands shift all CIs slightly (Waymo atfault lo 1.568M→1.596M, hi 3.403M→3.464M; Waymo all [134,866–243,005]→[137,680–248,128]); Tesla's strongly asymmetric July band moves more (atfault CI [174k–872k]→[186k–931k]). THE TWO k=0 FLIPS SURVIVE S1 with healthier margins: Tesla atfaultInjury ratioLo 1.008→1.072, Zoox 1.062→1.056 (full-series 1.126). (3) PRIOR-ONLY BADGES: every stress-table/card verdict badge whose k = 0 now renders faded+italic (.stress-badge.prior-only, style.css) with a Latin tooltip (PRIOR_ONLY_TIP + TODO recap: verdict rests on the prior alone, zero observed incidents; mirrors the charts' hollow-dot k=0 convention) — 7 badges on current data (Tesla atfaultInjury/airbag/serious+/fatality, Zoox atfaultInjury/serious+/fatality-ish). RED-FIRST pin in sanity-values.qual. (4) HYGIENE: faultFlipMultiplier now asserts its light-CDF base verdict equals the displayed-CI verdict (fails loudly if a future razor-thin case splits the machineries); incident-coverage.qual's widening check repointed from the retired estimateMpi to the live estimateMpiWindow marginal (counterfactual holds incident coverage at its CENTRAL value on the low edge — first attempt used the unthinned band floor and tripped the new band-ordering assert, caught by the suite); slurp.py incident_coverage's completeness detection hardened to require a MONTHLY-type submission in the deadline month (one stray 5-Day filing can no longer certify the batch; regen verified byte-identical on current data); the resolved Manifold Tesla market needed nothing — the human already commented it out of predmarkets.js on 2026-08-06. All 56 quals green; live render verified (7 prior-only badges, Latin copy rendering, zero console errors); dist-window.gif regenerated. STILL OPEN (small): human English for all five Latin TODO sites; optional Mode→Peak tooltip rename; S8 airbag-benchmark source choice (Kusano paper vs Waymo live hub); the atfaultInjury verdicts remain unpinned by choice (margins 5-13%, monthly-drift time-bomb tradeoff).
- 2026-08-21 (later 3): ZOOX RE-ANCHOR + GREEN-TEXT COPY BATCH + FAULTFRAC-UNCERTAINTY MODEL (human approvals: "ok on 2", "for 3 you can change the copy as long as it's green text indicating AI-generated. just point me to it for vetting", "6, let's do it"). SUPERSEDES the previous entry's "NOT APPLIED, awaiting human decisions" list — M2/M1/M5/S15/S3 are now applied per those approvals, and the M4 "confirmed-but-deliberate" status is resolved by (C) below. (A) ZOOX RE-ANCHOR (M2): data/vmt.csv zoox chain re-authored to Zoox's official disclosure — 2025-12 cume 1,001,000 → exactly 1,300,000 ("approximately 1.3 million driverless autonomous miles on public roads" as of Dec 31 2025; Zoox letter to NHTSA Jan 28 2026, docket NHTSA-2025-0523-0004 p.22 Q21a — supersedes the ~1M-late-2025 press floor), end-Mar exactly 2,000,000 (~2M milestone), end-Jul kept byte-identical at 3,051,000 (matches the >3M Aug-6 announcement; CUME_PIN untouched). Monthlies: Jun–Dec-2025 75/95/120/150/184/225/273k (was 55..200k, ×~1.363 preserving shape), Q1-2026 195/236/269k (was 240/290/330k), Apr–Jul 265/256/265/265k; bands 0.7x/1.3x, kyoom = running band sums (2026-07 kyoom identical by construction); rationales cite the docket; CPUC share percentages updated (Apr ~18%, May/Jun ~26%); the 2026-05 "vs our 1.86M" corroboration note now reads "matching our 2.0M". quals/zoox-vmt-provenance.qual.mjs re-pinned RED-FIRST (old data failed the new 1.3M milestone at ratio 0.770) to three milestones {2025-12: 1.3M, 2026-03: 2M, 2026-07: 3M}. Default-window zoox VMT total unchanged (2,873,000); within-window reallocation shifts 2025 months up ~36%, 2026 months down ~12-19%. IGNOREME.md zoox-provenance bullet updated to the three milestones. (B) COPY (all AI-authored text wrapped in .ai-text green per the standing convention; vetting list in chat): (1) index.html abstract item 1 rewritten (was "over 10 times safer ... with no uncertainty", numerically unsupported per M5): "roughly 10 times safer ... about 11x on at-fault crashes, far more on at-fault injuries — and on those metrics its lead over humans survives every assumption we stress-test" (default-window centrals 10.9x/175x/21x; stress lower edges 3.3x/19.6x/2.8x all > 1). (2) M1 SGO-carve-out caveat: new green paragraph in the sanity "Reporting threshold disparities" section (Third Amended SGO eff. June 16 2025; sub-$1,000 struck-by-another-vehicle exemption; ~quarter-to-a-third of Waymo's stopped-AV-struck PDO reports stopped appearing; at-fault immune) + caveat sentence and srcLinks (SGO PDF, Waymo release notes) on BOTH 'all'-metric humanMPI entries; pinned RED-FIRST in sanity-values.qual (ai-text + "Third Amended" + "June 16, 2025" in the section). (3) S3: summary-card label "VMT:" → green "Effective VMT:", tooltip now explains the reporting-completeness thinning and shows the raw window total (new vmtRawBest sum in monthlySummaryRows); numbers unchanged (relabel variant, mirroring the 07-24 tooltip precedent). (4) S15 README dates fixed per the human's blanket approval (no green possible in markdown — flagged in chat): 2025-06-27 → 2025-06-22 launch, 2026-01-01 → 2026-01-22 first PUBLIC no-monitor rides, SGO parenthetical now says the program started June 2021 and 2025-06-15 is the analysis-window start. (C) FAULTFRAC-UNCERTAINTY MODEL — resolves the 2026-06-19 deferred decision (item (b): conservative double-extreme CI pending this exact call), Codex M4, the "95% CI" naming, and the fault-probabilities-as-exact-counts finding, in one construction: the at-fault metrics' posterior is now the Poisson-binomial mixture sum_K P(K|{p_i}) Gamma(K+1/2, VMT) (p_i = the individual faultfracs; reduces bit-exactly to the plain Jeffreys posterior when every p is 0/1; fatality's 1/vehiclesInvolved fractions stay deterministic single-component by design), and EVERY displayed CI/fan/median — window cards, stress table, per-month fan bands, dist-chart markers — is now the exact quantile pair/median of the posterior MARGINALIZED over the log-normal VMT prior (the same distribution the dist chart draws), replacing the double-extreme envelope (coverage was 97.9-99.4% labeled "95%"). Machinery in crashla.js: poissonBinomialWeights (DP), mixtureComponents (end-trim 1e-12; log-concavity), makeMarginalMpiCdf (Simpson over the prior; consecutive-shape recurrences Q(a+1,w)=Q(a,w)+w^a e^-w/Γ(a+1) and pdf ratio w/a make cost ~independent of component count; ADAPTIVE node count 21..161 odd, spacing ≤ 0.5/sqrt(aMax) — a fixed 21 nodes biased data-rich quantiles ~0.3%, caught in verification; generic path for the flip-multiplier's non-consecutive scaled shapes), makeMarginalMpiQuant (safeguarded Newton on ln x, Wilson-Hilferty brackets — approxGammaQuant extracted from gammaquant), makeMixtureMarginalMpiDensity (mixture bell, single-component falls back to the original builder bit-exactly); estimateMpiWindow(k, fracs, ...) returns comps/quant; monthly recs carry atFaultFracs/atFaultInjuryFracs (3 empty-literal sites); METRIC_DEFS atfault/atfaultInjury gained fracsFn; faultFlipMultiplier scales component shapes (a_K = K·s + 1/2) and decides verdicts by two CDF threshold evaluations on a light 13-node trimmed cdf. NEW quals/fault-mixture.qual.mjs (RED-FIRST): pmf correctness, degenerate reductions, mixture-wider-than-point, and THE M4 KILLER — integrate each displayed estimate's own densityFn between its lo/hi ⇒ 0.95 ± 0.005 (was 0.979-0.994), fan-band masses likewise. Green prose sentence describing the model added to the Poisson-dispersion section; stale envelope descriptions rewritten (crashla.js incident-coverage comment block; IGNOREME.md Statistical Method bullets — envelope → exact marginal, pseudo-Poisson → Poisson-binomial). All 56 quals green; NO stress-verdict re-pins needed. VERIFIED by a 4-agent adversarial workflow: independent scipy/GL-quadrature recompute matches every displayed CI to ≤1e-5 of the app's stated model and ≤4e-6 of the exact integral post-node-fix (pmf to 1e-12; brackets stress-tested over 385-point grid); math review re-derived the recurrences and found only the node-count bias (fixed) + doc nits (fixed); consumer sweep found no breakage across windows 0-60/47-60/58-60/60-60 (zero console errors, no NaN, flip cells render sanely); completeness critic surfaced the stale docs (fixed) and the notes below. IMPACT: integer-metric CIs tighten ~5-25% per side (e.g. Waymo all [130.1M→134.9k... [130,102–260,980] → [134,866–243,005]); small-k fault metrics honestly WIDEN on the hi side (Zoox atfault hi 830M → 3.55B; Waymo atfaultInjury hi 323M → 1.06B); Waymo atfault CI [1.42M–3.87M] → [1.57M–3.40M], flip multiplier 3.3→3.8x. TWO VERDICT CHANGES, both ambiguous → "safer", both k=0 razor-thin, FLAGGED FOR HUMAN REVIEW in chat: Tesla atfaultInjury (ratioLo 1.008 — 0.8% margin) and Zoox atfaultInjury (1.062), default and full windows alike. OPEN/RESIDUAL: S1 (symmetric log-normal prior vs asymmetric authored bands) is now LOAD-BEARING — the exactness claim is exact w.r.t. this prior, and an S1 repair could plausibly un-flip Tesla's 0.8%-margin verdict (comment at the estimateMpiWindow CI notes this); the two flipped atfaultInjury verdicts are pinned by no qual (deliberate: 0.8% margin = a data-drift time bomb; revisit if wanted); faultFlipMultiplier's light verdict path could theoretically disagree with the displayed verdict in a future <0.1%-margin k>0 case (today's margins are ≥9x; belt-and-braces assert available if wanted); estimateMpi is now qual-only reference math (incident-coverage.qual exercises the retired estimator; repoint if wanted); k=0 "prior-only" stress badges (the flipped rows) carry no visual marker unlike the charts' hollow dots — a possible follow-up. Screenshot driver reverified; dist-window.gif regenerated against final data.
- 2026-08-21 (later 2): CODEX AUDIT CORROBORATED + 8 FIXES APPLIED (human: "carefully corroborate and fix the things you're sure about" + "fix the x-axis label overlap"). All ~22 findings from the external Codex audit (sourcery.html p282) verified by a 16-agent workflow (per-group verification with independent recomputation + primary-source fetches, then adversarial challenge of every confirmed bug; 13 upheld, 1 struck). APPLIED (all quals green after; 55 pass): (1) SPLIT_SAME_INCIDENT_REPORTS override in slurp.py — reports 30270-10320/10321 (MAR-2025 LA, 12:44 wrong-lane sideswipe of queued Waymo, then 12:57 SUV clip of the same now-parked-with-hazards AV) share Same Incident ID 77ba86fa433a2c4 but are DISTINCT crashes (the 10321 narrative literally says "due to a prior collision"); the dedup was silently dropping 10320. Waymo full-history count 1,980 → 1,981; faultfrac row added at 0 via the locked 3-assessor+adjudicator workflow (unanimous; stationary-AV anchor: "AV stopped in queue; car passing in opposing lane sideswiped its left"); IGNOREME.md counts updated. Default window (2025-06+) unaffected. (2) incident_coverage estimation-side zero-incident exclusion removed (slurp.py: `last_vmt == 0 or last_count == 0` → `last_vmt == 0` + SE guard max(pooled_obs,1)) — observing 0 where the reference predicts >0 IS reporting-lag evidence; excluding it one-directionally overstated completeness. 2026-07 pooled coverage 0.1106/0.0446 → 0.1074/0.0434 (obs 12 / exp 111.7; Tesla now contributes 0 obs vs 3.25 expected). (3) incident_coverage counting now dedups two-stage (Report ID first, then Same Incident ID + split override), mirroring the main path (ec2049a added Report-ID-first to the main path only; the weak single-stage key counted 9 phantom incidents in past months — zero numeric effect today, latent skew removed). (4) Waymo Dec-2025/Mar-2026 anchors now get the same minus-~150k pre-series treatment as every other milestone pin (the Sep-2025 rationale and waymo-vmt-provenance.qual both document the convention; the Oct–Dec bridge violated it): Oct–Dec 2025 monthlies ×0.9965792 (bridge x1.069 → x1.0653), Dec cume exactly 170,550,000, Mar 220,450,000, all later cumes −150k, Jul-2026 CUME_PIN re-pinned 301,600,000 → 301,450,000; provenance pins now hit their stated targets exactly. (5) Waymo CPUC Q2 CA actuals now include Driverless Pilot alongside Deployment (Pilot TotalVMTZEV Apr 51,198 / May 102,205 / Jun 660,877 — the Jun jump is mostly Period-1 no-passenger validation during the freeway pause; the applied band formula had used Deployment only, contradicting the rationales' own "±30% on the non-CA remainder only" and the Zoox treatment): Apr–Jun vmt_min/max recomputed (e.g. Jun 17,057,137/23,942,863 → 17,255,400/23,744,600), rationales now show deployment + pilot arithmetic; the 08-20 FLAG's "CPUC CA fell 7% May→Jun" softens to −1.2% with Pilot included. (6) Waymo Apr–Jul 2026 kyoom bands rebuilt by chaining the Mar anchor band with the monthly bands (the file still carried the pre-CPUC 0.6M+30%-of-central construction, asserting cumulative values its own Mar band + monthly minima exclude; kyoom-band.qual's "anchor only tightens" intent): end-Jul [276.7M, 326.5M] → [285,237,269, 317,662,731]. (7) tesla 2026-07 rationale rewritten (the fix pending since 08-17): floor no longer cites the circular "approaching 2.5M" press restatement of the deck's own 2.44M; band now justified from the deck series' MoM volatility (log-sd 0.34, ~95% one-step [84k, 324k]; floor below worst-ever 0.51x, ceiling 2.2x vs best-ever 1.48x), numbers unchanged, no fleet clause per the 08-19 directive. (8) fmtAge rotten off-by-one (d <= 7 kept 7d23h59m "stale" despite the ">7d" contract) → mins <= 7*1440; plus the user-reported x-axis fix: drawSingleMonthAxes now suppresses a stepped month label within labelStep of the always-drawn last label (2026-06/2026-07 collided at window widths ≥ 25 months). vmt.js + incidents.js regenerated OFFLINE (scratchpad regen replicating slurp main() against the checked-in snapshots; byte-identical baseline verified on the unedited master first); live headless render clean. NOT APPLIED, awaiting human decisions (details in chat): M2 Zoox re-anchor (NHTSA-2025-0523-0004 p.22: "approximately 1.3 million driverless autonomous miles" as of Dec 31 2025 vs our 1,001,000 — 30% low, band contains it with 2.8% headroom; proposed chain re-authoring keeps end-Jul 3,051,000); M1 SGO Third-Amendment caveat copy (June-16-2025 sub-$1,000 struck-by-another-vehicle carve-out biases the all-incidents-vs-human comparison pro-AV ~24-33% of that class; Waymo calls the PDO benchmark comparison "impossible"; drafted caveat needs rule-7 copy ratification); M5 index.html headline ("over 10 times safer ... no uncertainty" — on the DEFAULT window 3 metrics sit ≥10x centrally [at-fault 10.88x, at-fault-injury 175x, serious+ 21.1x] but only at-fault injury stays >10x across the app's own stress range; Codex's table reproduces exactly on the default window); S3 summary-card "VMT" label showing effective (coverage-thinned) exposure −6-9% vs raw (relabel vs switch-to-raw is the author's call); S15 README launch-date nits (06-27→06-22, 01-01→01-22, SGO-start parenthetical; README is humans-only); S1 distribution-bell symmetric-lognormal prior vs asymmetric authored bands (Tesla July endpoint coverage 92.45% not 95%; two defensible repair families — two-piece mode-at-best vs quantile-spliced median-at-best — need a human pick). CONFIRMED-BUT-DELIBERATE (no change): M4 CI bars are the documented conservative double-extreme envelope (coverage 97.9-99.4%, AGENTS 06-19 decision gates tightening on the faultfrac-uncertainty call); S2 identical human CI fans (a bounds range, labeled "Range" in tooltips); S11 Mode dot = log-density peak; S12/S13-adjacent completeness simplifications; S8 airbag benchmark pinned to Kusano 56.7M paper (Waymo's live hub now spans 1.19-2.99 IPMM — update is a source-choice call); S9 rideshare fatality range mixes report vintages deliberately as a span; M3 (94% critical-reason anchors, caveat present) + M6 (fractional deaths; both current fatal crashes are 1-death, so no undercount) are ratified methodology; V2 deadhead sentence is the settled 07-22 semantics.
- 2026-08-21 (later): ROUGHEN FILTER RIPPED OUT + LINKS BACK TO BLUE (human: "let's stick with old-fashioned blue links ... also, the lines are showing up weirdly pixelated for me. can you diagnose that?"). BUG REPORT. Replicata: load the page at devicePixelRatio 2 and zoom 8x on the top-left corner of .abstract or on any h2 rule. Expectata: a drawn line, antialiased like the text beside it. Resultata: the stroke is chopped into hard-edged segments at whole-pixel offsets with NO partial coverage on the steps, and the vertical edge has notches bitten out of it. DIAGNOSIS: feDisplacementMap resamples the source nearest-neighbour and offsets by whole DEVICE pixels, so on a 2.5px stroke every wobble is a ~40%-of-stroke-width jog with no AA. Not a browser bug: reproduced identically in Chromium, Firefox and WebKit (all three launched from the playwright cache; screenshots at dsf 1/2/3). RULED OUT, with measurements: (a) NOT a 1x-rasterize-then-upscale problem -- edge-run lengths are not quantised to dsf (23% at dsf 3, 60% at dsf 2 ~ chance) and ~50% of edge-band pixels are midtones, so the filter does run at device resolution; (b) NOT gamma -- adding color-interpolation-filters="sRGB" produced a pixel-identical result; (c) NOT filter-region size -- .abstract (253px tall) and .predmarket-grid (946px) are equally chewed. Tuning helps but cannot cure: numOctaves 2->1, baseFrequency 0.04->0.012 and a blurred displacement field make the steps longer and fewer, never softer, because the offset stays integer. FIX: all three <filter> defs deleted from index.html, .sketch-defs gone, every filter: url() gone. The heading + colophon rules are now a real vector path -- a 198px wave in a data-URI SVG used as a MASK over background: var(--line), so the shape is in the token and the color stays in the palette; starts and ends at y=4.3 with matching slope so repeat-x has no seam. .abstract and .predmarket-grid drop their border-drawing ::before and carry a plain 2.5px border again; the wobble was always coming from border-radius, which the rasteriser antialiases properly. Verified smooth at 8x in all three engines. ACCENT: --accent back to #2060c0 (human: red collides with --worse; blue collides only with Waymo's series color, which is the cheaper collision). --iffy nudged #8a6f19 -> #806716: warming --wash to #f6f2ea had quietly dropped the amber to 4.31:1 on it and .pm-subcard paints .pm-card-odds.mid there -- caught by a NEW qual that computes WCAG contrast for every meaning-bearing token against --paper/--card/--wash (--ink-faint deliberately excluded, it is the de-emphasised meta text and has always been sub-AA). QUAL SPEC CHANGED, flagged to the human: sketch-style.qual.mjs section 4 used to assert the displacement filters EXIST; it now asserts no feDisplacementMap in index.html and no filter: url() in style.css, plus that --drawn-rule is an SVG path used through mask-image WITH the -webkit- prefix (older Safari) over a tokenized background. Section 5 (no filter on a layout ancestor) kept as a hazard guard. 55 quals green; sticky <th>, tooltips at cursor+12px, filter buttons and collapsibles all still fine; repaint 82ms -> 67ms with zero filtered elements.
- 2026-08-21: NICKY CASE SKETCH SKIN on style.css (human: "careful web research and then thoroughly and carefully revamp the css to emulate the faux-hand-sketched style of nicky case? add \"design inspired by nicky case\" in the footer ... show me side-by-side options if in doubt"). RESEARCH (primary sources, raw CSS pulled from ncase.me): ncase.me/css/index.css = #333 ink on #fff, links #ff4040 hovering to #ff8080 with a `top:-2px` lift, content box `border:5px solid #333; border-radius:25px`, `hr{border-bottom:3px dashed #ddd}`, 23px/1.4em type. crowds + remember self-host PatrickHand-Regular.ttf and both carry the "fake bold" trick `b,strong{font-weight:normal; text-shadow:1px 0 0 currentColor}` because Patrick Hand ships one weight. trust uses a bespoke FuturaHandwritten.ttf (unclear license -- NOT used here) and draws its wobbly buttons as PNG sprites, which does not port. The CSS-only equivalent is the mismatched-elliptical-corner border-radius trick (`255px 15px 225px 15px / 15px 225px 15px 255px`) plus an feTurbulence+feDisplacementMap roughen filter. IMPLEMENTED: (1) fonts/PatrickHand-{latin,latin-ext}.woff2 + OFL checked in, self-hosted (the page still makes zero third-party requests). (2) Two type roles in :root, --hand (headings, buttons, th, axis + legend labels, colophon) and --sans (all prose, every figure, CI, formula) -- Patrick Hand has one weight, no tabular figures and no Greek, so the statistics deliberately stay off it. (3) Three <filter> defs in index.html; the stylesheet only ever points them at a border-drawing pseudo-element (.abstract::before, .predmarket-grid::before, h2::after, .colophon::before), never at an element containing text -- displacement smears glyphs, and a `filter` also makes its element a containing block, which would strand .chart-tip (position:fixed) and unstick the incident table's <th>. That trap is now a qual. (4) --wobble-{a,c,d,pill,nib,rule} radii, sized to the box: the classic 255px sweep reads as one confident stroke around the abstract and as a scythe through the corner of the 2000px market list. (5) Chart axes get ink weight + round caps but NO roughen filter: a zero-height <line> has a degenerate bbox, so the filter region would have to be the whole 900x520 canvas -- a full-canvas turbulence pass per axis. Data marks are never displaced. (6) The 25 stacked pm-card boxes collapsed to one drawn frame with dashed row rules. (7) Palette: --ink #333 (ncase.me's), --line IS ink (--line-strong retired, --line-soft is the new quiet rule), paper #fffdf8. ACCENT: ncase.me's #ff4040 measures 3.4:1 on white and fails WCAG AA, so the shipped value is the same hue at #d42828 (5.0:1), also far enough from Tesla's #d13b2d series color. (8) Footer: "design inspired by nicky case" linking ncase.me. VERIFIED: 55 quals green (2 new: sketch-style, colophon-credit); headless sweep of all 9 sections at 1400/1000/760/420/360 with no console or page errors; sticky <th> still sticks, chart-dot and table-cell tooltips still land at cursor+12px, filter buttons and collapsibles still work; full-page repaint 82ms with 12 filtered elements. PRE-EXISTING, NOT TOUCHED: .source-table.stress-table overflows the body width (scrollWidth 1010 vs 1040 before -- marginally better, still overflowing). OPEN FOR THE HUMAN: accent red vs keeping the old #2060c0 blue, and whether body prose also moves onto Patrick Hand; both rendered side by side.
- 2026-08-20 (later): SAFETY-MONITOR / SUPERVISION AUDIT (human: "we're counting miles with a driver's seat human monitor?? aren't those SAE level 2? if there's a human making safety-critical disengagements that would ruin our data, right??"). NO REPO CHANGES from this audit; two of Fable's own earlier statements CORRECTED (below). 44-agent workflow, 32 adversarial verdicts. Q1 — IS "In-Vehicle (Commercial / Test)" LEVEL 2? NO, settled from primary text. Third Amended SGO DEFINITIONS para 4 (verbatim, sha256-matched PDF): an ADS "means a Level 3, Level 4, or a Level 5 system ... regardless of whether it is limited to a specific operational design domain and REGARDLESS OF THE PRESENCE OF A SAFETY OPERATOR." NHTSA wrote the safety-operator question out of the ADS/Level-2 boundary deliberately. SAE J3016 agrees (Koopman J3016 User Guide myth #10: "The presence of a safety driver does not change the level"); J3018 exists specifically to govern safety-driver-supervised on-road testing of L3-5 ADS. The LEVEL is carried by field 21 "Automation System Engaged?", which reads ADS + Verified Engaged + Within ODD Yes on ALL 24 Tesla ADS-file reports incl. the 3 disputed ones. Field 18 (operator type) makes no level claim. Nor is Tesla idiosyncratic: In-Vehicle is the MAINSTREAM ADS-file code — 194/1365 rows current (14%), 797/2295 archive (35%); Waymo 42 current + 237 archive, Zoox 23 + 112, Avride 82/82, Motional 11/11 ("with a Vehicle Operator in the driver's seat"). Tesla UNDER-uses it. Tesla also files 21 commercial/test reports in the LEVEL 2 ADAS file (all "ADAS" engaged), zero Report-ID overlap with the ADS file — the file split tracks field 21 at 1364/1365 and 2021/2024. So Tesla itself sorts supervised-FSD to the L2 file and robotaxi to the ADS file by its own attestation. KEEP In-Vehicle in scope. Q2 — WOULD DROPPING THEM HELP? No, it would flatter Tesla. The 3 In-Vehicle rows (13781-14630/14631 MAR-2026, 13781-15017 APR-2026) are all Austin street, 0 mph, stationary-AV-rear-ended, PDO, no airbag, faultfrac 0. Dropping them moves exactly ONE of ten metrics — all-incidents MPI 102,450 -> 117,086 (+14.3%) — and flips zero verdicts; at-fault (the DEFAULT-selected metric), nonstationary, injury, hosp, airbag, SSI+ and fatality are bit-identical. Dropping incidents while keeping their miles is a free 14% gift to Tesla; dropping both is the exact arithmetic no-op VMT*(21/24)/21 = VMT/24. They are also not a distinct operational class — they are a CODING CHANGE: Tesla coded the same monitor-aboard Austin rides "None" for nine months, then "In-Vehicle" in Mar/Apr 2026. Q3 — IS THE UNDERLYING WORRY REAL? YES, and it is much bigger than the 3 rows, and it is NOT fixable. ~84-86% of the 2.44M-mile denominator was driven with a monitor aboard (Tesla's own >380k "fully unsupervised" / 2.44M = 15.6% unsupervised). Sensitivity at k=24: removing supervised miles from the denominator drops all-incidents MPI from 102,450 toward ~29,000 (75% removed) or ~18,000 (84.4% removed), flipping the verdict ambiguous -> ROBUSTLY WORSE; the flip threshold is 52.5% of miles removed. At-fault is far more robust (needs ~91%). BINDING CONSTRAINT: Tesla publishes NO mileage split by supervision mode, so the denominator CANNOT be split — verified against the Q2-2026 deck (SEC 8-K Ex-99.1). The app already discloses this at crashla.js:3095, and the human's own added sentence ("almost sure the passenger-seat safety monitors have at least not had the ability to intervene in real time at normal driving speeds") is the load-bearing assumption that makes the contamination tolerable — already flagged 2026-08-19 as TOO STRONG as literally worded. DEAD END, RECORDED SO NOBODY RE-DERIVES IT: Fable built a "natural experiment" from the fact that all 18 Tesla incidents JUL-2025..APR-2026 mention a safety monitor and none of the 6 in MAY/JUN-2026 do (a real, clean, reproducible split), giving monitor-aboard 2,091,000 mi / 18 = 116,167 MPI vs monitor-free 349,000 mi / 6 = 58,167 MPI, a 2.00x rate ratio. IT IS INVALID and was retracted before reaching the human. (a) SEMANTIC: Tesla treats "monitor" and "passenger" as DISJOINT — 13781-14631 says "The ADS had a safety monitor present in the vehicle with no passengers", so post-break sentences like "There were no passengers" are silent about the monitor, not evidence of its absence. It is a narrative-TEMPLATE change. (b) EXTERNAL: Electrek 2026-06-16 counted 31 active vehicles across all markets with "only 14 of those operating unsupervised... The rest still run with a human safety monitor on board" — i.e. Austin was ~75% MONITORED in June, inside the supposedly monitor-free window. Field counts ran 13% unsupervised at the Jan-22 launch -> 18% (Mar 1) -> 20% (Mar 29). (c) Elluswamy Jan-2026 described a deliberately MIXED fleet ("a few unsupervised vehicles mixed in with the broader robotaxi fleet with safety monitors"). Also note the exact conditional-binomial p was 0.14 (expected 3.43 vs observed 6) — not significant even before the confounds. SEAT-POSITION TIMELINE (verified): Jun 22 2025 - Aug 31 2025 Austin only, monitor in the FRONT PASSENGER seat on every ride (Elluswamy, Q2-2026 call: "We had safety monitors in the passenger seats of the cars back then"). Sep 1 2025 geofence expands to add ABIA/Giga/freeways and monitors move to the DRIVER'S seat for HIGHWAY trips only (@Tesla_Robotaxi Sep 4 2025: "Safety monitors are only in the driver's seat for trips that involve highway driving, as a self-imposed cautious first step"); Electrek called that "reverts to level 2". Self-imposed, NOT required by TX SB 2807. Jan 22 2026 first public no-monitor rides, mixed fleet. The DRIVER'S-SEAT share of miles specifically CANNOT be bounded from public data — genuine null result. CORRECTION 1 (Fable was wrong earlier the same day): Fable told the human "Austin did not go fully unsupervised", citing zero new isUnsupervisedPassenger flags in August in robotaxitracker's API. That inference is unsound — the tracker's own creator Ethan McKanna says that metric was LAGGING and he revised the feeding sources ("Some metrics on the site had been lagging... found more vehicles running unsupervised than the tracker had reflected"). Per The Verge via TNW 2026-08-20: "Every one of the 170 Tesla robotaxi rides logged in Austin over two weeks ran with no safety monitor on board, across 54 cars"; "Austin has seemingly stopped supervised rides for a couple weeks now"; "Plates previously marked as supervised are now offering unsupervised rides"; Miami dropped monitors in July; ~30 driverless in Dallas/Houston in the past week. Re-pulled /v1/api/compat/vehicles twice on Aug 20 — still the OLD data (48 unsup-flagged in-scope, no August flags), so the revision is NOT exposed on that endpoint; treat isUnsupervisedPassenger as STALE. Disclosure worth recording: McKanna interned with Tesla's Robotaxi team this summer. CORRECTION 2: Fable's active-fleet answer to the human ("~15-20 fully unsupervised") is consequently TOO LOW for August — the flag-based count is the stale metric. Better current read: ~54 Austin cars running unsupervised over two weeks plus ~30 Dallas/Houston, against ~79-82 in-scope vehicles seen in 7 days and 74 distinct plates with a receipt trip in 7 days. The service-wide operating fleet (~75-85) is the number that matches our denominator scope; the supervised/unsupervised SPLIT within it has moved sharply toward unsupervised since early August. ADDENDUM (full 72-agent run landed): (i) The stale-flag diagnosis is STRONGER than stated — robotaxitracker's firstSpottedUnsupervised is dead SITEWIDE, not just for Austin: the newest value across every provider and market anywhere is 2026-07-29, while trip data stays live (133 Austin trips Aug 14-20 across 40 distinct vehicles; 59 Austin vehicles active in 14 days, which independently corroborates the "54 different vehicles" figure). And the rebuilt front end no longer uses the flag at all — the "Unsupervised - no safety driver" tile is literally `Oe = new Set(["bay_area"])`, i.e. every non-Bay-Area Tesla is counted unsupervised BY DEFINITION. So Fable's "zero new flags in August" was a schema artifact, not evidence. (ii) CAVEAT on the other side: the Austin-went-monitor-free claim is SINGLE-SOURCE (McKanna via Verge/Engadget/TNW), unconfirmed by Tesla, and in tension with Electrek two days earlier counting only 14 unsupervised vehicles across ALL markets. Treat as probable, not established. (iii) The driver's-seat HIGHWAY regime was already OVER before the window closed: Sep-2025 driver's-seat monitors applied to Austin highway trips only; Dallas+Houston launched unsupervised from day one (Apr 18 2026); by Jun 3 2026 unsupervised service covered the entire 245 sq mi Austin geofence INCLUDING I-35/ABIA/Giga; Miami (Jul 3) and Orlando/Tampa (Jul 21) launched unsupervised. Decisively, ZERO of Tesla's 24 ADS-file reports is coded Highway/Freeway — the driver's-seat era contributed no incidents to our numerator at all, so the "driver's seat = L2 contamination" path is empirically empty even before the legal argument. (iv) SEAT CAPABILITY, bearing directly on the human's crashla.js:3095 sentence: per Templeton (Apr 8 2026) the PASSENGER-seat monitor had an e-stop AND "the ability to grab the steering wheel" — no pedals. That is a second independent reason the "have at least not had the ability to intervene in real time" wording is too strong. Remote-operator authority is separately bounded, VERIFIED TO PRIMARY SOURCE (Fable fetched it after the human challenged the citation): Tesla's Mar 26 2026 letter to Sen. Markey, printed in the Senate compilation "Company Responses to RAO Letter" at https://www.markey.senate.gov/imo/media/doc/company_responses_to_rao_letter.pdf (42 pp, sha256 6b9952c14f9371f6334aeab157fc65d652f789cb444b88f7747a3e82864fd2ac; Tesla letterhead "1 Tesla Road, Austin, TX 78725"; the same PDF carries Waymo/Zoox/Nuro/Aurora/Motional/May Mobility responses). VERBATIM, answer 1(b): "Tesla vehicles are not remotely driven under normal operations. As a redundancy measure in rare cases, however, RAOs are authorized to temporarily assume direct vehicle control as the final escalation maneuver after all other available intervention actions have been exhausted. RAO direct input is the last resort and is always limited in scope and duration. RAOs can only take temporary control of the vehicle at <=2 mph, and if direct access is granted by the Tesla ADS, the enforced maximum speed authority is 10 mph." NOTE THE PRECISE READING (an earlier Fable paraphrase was ambiguous): <=2 mph is the TAKEOVER THRESHOLD (the vehicle must already be at or below it), and 10 mph is the enforced ceiling once the ADS grants control — not "control capped at 2 mph". Answer 1(a) also lists the non-driving commands: "Forward/reverse nudge (low-speed, short-distance repositioning)", door lock/unlock, system reboots, destination/waypoint modification. Corroborated inside our own data: the sole Remote-coded incident 13781-15395 (Houston tree stump) logs SV precrash speed of exactly 2 mph during an RAO recovery — textbook "forward/reverse nudge". Consequence for the supervision worry: remote authority CANNOT suppress at-speed crashes, so it cuts against the contamination story rather than for it. IMPLICATION FOR NEXT CYCLE: if Austin/Miami really went monitor-free in Jul/Aug 2026, then 2026-08 onward is the first substantially UNSUPERVISED Tesla mileage, and the supervised-mile contamination that dominates the 2025-06..2026-07 window starts washing out. Worth a per-era split once there are enough post-transition incidents — but do NOT build it off narrative keywords (see DEAD END above); build it off dated external fleet reporting.
- 2026-08-20: TESLA-VMT TIGHTENING ATTEMPT (null) + CPUC Q2 PULLED IN (human: "more web research ... tighten the bound on Tesla's recent VMT?" then "go ahead"). TIGHTENING VERDICT: NOT POSSIBLE — 82-agent sweep (6 angles, 54 adversarial verdicts, 1,679 tool calls), ONE survivor and only as a ceiling argument. tesla 2026-07 STAYS 170,000 [55,000, 340,000]. Dead ends, each verified: (a) RECEIPT PANEL (the most promising new lever — robotaxitracker's open trips API, now 7,398 rider trips, ~93% synced from Tesla receipts, browser UA required, hard caps at 500/page and offset 5000): calibrating deck miles / panel miles over Feb-Jun gives scale factors 174.5/376.1/254.6/140.2/138.5 — 2.72x spread, corr(panel, deck) = +0.18, leave-one-out error 42.7%, and its own 2-sigma interval [66k, 356k] is WIDER than the placeholder. Worse, specification choice alone spans 106k (chain/recent-coverage) to 166k (pooled scale factor) — a 1.56x spread — so the panel corroborates NOTHING about the level. Elasticity of panel size w.r.t. true VMT is 0.130 (R^2 0.083): the panel measures contributor enthusiasm, not fleet activity. (b) FLEET COUNTS: move OPPOSITE to miles during the utilization-led slowdown (tracked fleet 60->68->80 while deck miles went 374k->192k->157k). Same method that missed Q2 by 1.36x/2.9x/2.2x. (c) INCIDENT-RATE INVERSION: post-launch rate IS stable (7.33 per Mmi, Pearson dispersion 1.58 excluding the Jul-2025 launch month vs 14.45 including it) but the SGO reporting lag means a month shows only 0-33% of its eventual reports at ~1 month (APR 0/1, MAY 0/3, JUN 1/3), so zero JUL-2026 Tesla incidents in the Aug-17 pull is expected under ANY July VMT in the band and carries no information — do NOT read it as a low-mileage signal. Every achievable posterior from a later July count is still wider than the placeholder. (d) REGULATORY: fetched the actual Nevada NTA permit (Tesla Robotaxi LLC AVNC Permit 002, Docket 26-05015, Jul 27 2026, https://nta.nv.gov/uploadedFiles/ntanvgov/content/Carriers/Certificates/T_to_Z/Tesla_Robotaxi_LLC_AVNC_002-cert_ADA.pdf) — sole reporting duty is accidents/system failures within 5 working days, NO mileage; 10-vehicle cap, Strip corridor only, 45 mph ceiling, no pickups within 0.25 mi of Harry Reid. TX Ch. 220, Florida s.316.85, Austin, TxDOT: no mileage duty anywhere. Tesla 10-Q: zero mileage words. Q3 deck cannot exist before ~Oct 21. (e) TESLA DIRECT: no Tesla-sourced mileage/ride/utilization figure ANYWHERE after the Jul-22 deck+call; EDGAR shows zero filings after 2026-07-23; the Cybercab launch event had not happened as of Aug 20. WHAT DID COME OUT OF IT — a defensible band derivation to keep: build a one-step-ahead band from TESLA'S OWN deck series volatility (9 observed month-over-month ratios Sep-25..Jun-26: 1.436, 1.317, 1.052, 1.478, 1.091, 1.352, 0.854, 0.513, 0.818; log-mean +0.0490, log-sd 0.3443, 1 sigma = 1.41x). From June's 157k: central 164,887, 68% [116,853, 232,666], 95% [83,960, 323,816]. So the existing [55k, 340k] is ROUGHLY a 95% interval, not an arbitrary guess — the ceiling is generous (340k needs 2.17x MoM vs a best-ever 1.48x and Tesla's own maximally-bullish "+10%/week" compounding to ~197k) and the floor is the loose edge (55k needs 0.35x vs a worst-ever 0.51x). A floor rise to ~84k was CONSIDERED AND WITHDRAWN: the receipt panel's chain specification puts July near 106-130k, which would make an 84k floor uncomfortably tight. Net: no change, but the row is now defensible on volatility grounds rather than on the mis-sourced "approaching 2.5M" artifact (that rationale rewrite is still pending from the 2026-08-17 later-2 entry). AUGUST-2026 CONTAMINATION WARNING (use next cycle): the Cybercab sweepstakes ("Ride in Robotaxi through 8/23 for a shot to attend our Cybercab launch event", @robotaxi Aug 18) is farming ultra-short rides. Tracker Aug 18 alone = 319 trips averaging 0.60 mi; 74% of August's tracker trips fall in two days but only 32% of its miles; July median trip 3.02 mi vs Aug 18-19 median 0.15-0.19 mi. Any August estimator must be MILEAGE-weighted, never ride-count-weighted (ride-count estimators inflate 3-5x), and Aug 17-23 is unusable. CPUC Q2-2026 PULLED IN (human-approved): CPUC posted the Q2 (Apr-Jun) filings on 2026-08-18, one day after the Aug-17 survey found them missing. Downloaded and parsed both zips myself. WAYMO Deployment (CA, driverless, TotalVMTZEV = P1+P2+P3 incl. deadhead): Apr 9,414,525 / May 9,704,968 / Jun 9,023,791. WAYMO Pilot (CA, separate program): Apr 51,198 / May 102,205 / Jun 660,877 — note the June jump. ZOOX Driverless (CA = SF only): Apr 47,323 / May 65,708 / Jun 67,923. APPLIED: waymo 2026-04/05/06 vmt_min/vmt_max recomputed as CA_actual + (central - CA_actual) x 0.70/1.30 — i.e. the +/-30% modeling band now applies ONLY to the non-California remainder, since ~44-49% of each month is regulator-measured. Bands roughly halve: Apr [13.30M, 24.70M] -> [16.12M, 21.88M]; May [14.35M, 26.65M] -> [17.26M, 23.74M]; Jun [14.35M, 26.65M] -> [17.06M, 23.94M]. CENTRALS DELIBERATELY UNCHANGED (19.0M/20.5M/20.5M) — moving them would break the vmt-ledger CUME_PINS and the 220.6M-anchored kyoom chain, and the implied CA share (49.5%/47.3%/44.0%, vs 52.9% in Q1) is consistent with Waymo's non-CA expansion rather than evidence our centrals are wrong. Zoox rows: rationales updated with the CA actuals but BANDS UNCHANGED — CA is only ~16-23% of Zoox miles and zoox kyoom_min has ZERO slack against the running sum of monthly bands (kyoom-band.qual invariant), so raising vmt_min there would require re-authoring the kyoom chain and re-checking the ~1M/~2M milestone pins. Waymo had ~60M of kyoom slack, so its tightening was safe. Result: 53/53 quals green, Waymo CIs narrow (all-incidents 122,614-269,730 -> 129,996-261,412; at-fault 1,339,299-3,998,469 -> 1,419,926-3,875,171), NO verdict flips, all centrals identical. FLAG FOR HUMAN: CPUC CA fell 7% May->Jun (9.70M -> 9.02M, consistent with the US-wide freeway suspension) while our US central holds June FLAT at 20.5M. If non-CA tracked CA, June US would be ~19.1M. Not changed — it would move the cumulative chain and the CUME_PINS — but the shape divergence is real and worth a decision.
- 2026-08-19 (later): HOUSTON INCLUSION IMPLEMENTED + GREEN TEXT + FULL AUDIT (human: numbered list — include the stump at faultfrac 0; explain Waymo "ambiguous", the 0.1x fatality cards, dispersion 12.82; check Geography/parenthetical; verify the trust-Tesla sentence; do the green-text TODO; Waymo cross-check freshness; audit everything). RESOLVES the previous entry's OPEN HUMAN CALL: "Remote (Commercial / Test)" added to PUBLIC_SERVICE_OPERATOR_TYPES["Tesla, Inc."] (QDD: new quals/tesla-scope.qual.mjs red first). 13781-15395 now counted: 2,047→2,048 (Tesla 24; MAY-2026 = 3; Geography Tesla 3 cities incl. Houston (1)); faultfrac 0, reasoning "Remote assistance operator, not the ADS, drove into hidden tree stump" (mirrors the passenger-caused = 0 convention). NEW GUARD in slurp main(): entities configured in PUBLIC_SERVICE_OPERATOR_TYPES must file ONLY types in their set — a novel Tesla mode now crashes the run for human classification (the silent-exclusion class that hid the stump for a month). Slurp re-run deterministic; coverage unchanged (Tesla has 0 JUL incidents so the pooled 0.1106 is untouched); Tesla dispersion 12.82→12.34, all-incidents MPI 106,131→101,740 (ratio 0.28x–2.09x, still ambiguous). README.md count moved to IGNOREME.md in the human's restructure — updated there (2,048; scope + Remote); data/README.md step 3 updated. GREEN TEXT (human TODO): new --ai-ink token (#59765b desaturated sage — deemphasizes at --ink-soft's weight, stays legibly green so the human's "green text = AI-generated" legend holds, clears WCAG AA 4.5:1 even on --wash; first cut #5c7a5e was 4.45:1 on the th wash) + .ai-text class; VMT-sources methodology <td>s carry it (pinned red-first in sanity-values.qual); the human's two <font color="green"> wrappers converted to the class, words untouched; TODO comment removed. AUDIT (34-agent workflow: diff+docs / claims web-research / independent recompute / rendered sweep, 2 skeptics per finding): recompute facet FULLY CLEAN (2,048 = 1,980/24/44 month-by-month from raw snapshots under the new scope; faultfrac 1:1 with structured cols re-derived; vmt.js byte-identical to an independent build; dispersion 12.34/1.66/0.68 exact; stress rows + card multipliers exact via scipy). Rendered sweep CLEAN (0 console errors, no NaN/undefined, slider/toggle exercises pass). FIXED from findings: four % printf artifacts in the previous entry; crashla.js fleet-forecast [AI TEXT] comment + 13 vmt.csv rationales + IGNOREME.md still saying the pre-Remote scope; "1 incidents" pluralization in the MPI cards (kLine now uses splur; 85.1/0.3 fractional counts still read "incidents", correct). FLAGGED FOR HUMAN (human copy, not touched): (i) README.md lines 50/66/68 — the "(still true?)" questions now answer NO: Tesla files three operator types (40 None / 3 In-Vehicle / 1 Remote raw rows) and the filter is no longer None-only; line 52's "empty driver's seat" denominator framing predates the fleet-miles-incl-monitor scope. (ii) index.html og:title/og:site_name (lines 8-9) still carry "Teslapologetics Companion App AKA Robotaxi Reality Check" after the title rename — two candidate replacements ("Teslapologetics" per <title> or "Teslapologetics AKA Robotaxi Reality Check" per h1/ld+json). (iii) crashla.js:3096 monitor sentence: web-verified TOO STRONG as literally worded — passenger-seat monitors demonstrably had a real-time stop means (touchscreen "Stop In Lane" + repurposed door-handle kill switch, Electrek Oct-2025 "a finger on a kill switch"; one on-camera moving-vehicle intervention, the Jun-2025 UPS-truck video) — but every documented intervention was low-speed, monitors had no steering/pedals, and Tesla moved monitors to the DRIVER seat for highway rides (Sep-2025), which itself suggests passenger-seat intervention was insufficient at speed; with the "almost sure"/"at least" hedges the sentence is defensible if "intervene" means "prevent a normal-speed collision", indefensible if it means "any real-time action". Human's call; evidence both ways recorded here. (iv) crashla.js:3397 trust sentence: legally sound framing (Rule 10b-5; Luckin precedent; "impeccable safety record" on the Jul-22 call = classic puffery vs the deck's actionable 380k/2.44M figures) — but note the app's NUMERATOR is also entirely Tesla-said (SGO filings; NHTSA opened a probe Aug-22-2025 into Tesla filing SGO reports months late; Cruise's $1.5M consent order shows enforcement teeth; the Aug-17 bollard event is SGO-reportable ~Sep-15 only IF Tesla assesses property damage occurred — single-vehicle object strikes are covered at any damage amount under the Third Amended SGO). AUDIT NITS left alone (refuted as non-actionable): guard message's "record an explicit exclusion" names a config path that doesn't exist yet (deliberate forward-pointer); tesla-scope.qual pins the guard by source-grep not behavior (guard verified live at slurp.py:827-833); README "(started 2025 June 15)" reads as the SGO's start date (human notes file).
- 2026-08-19: ROBOTAXITRACKER RE-SURVEY (human: "robotaxitracker.com is showing more data again — anything gleanable, e.g. wait times?"). NO NUMERIC CHANGES; one AGENTS.md correction applied (see bracketed CORRECTED note in the 2026-08-18 entry). Site rebuilt as a Vite SPA with an open API (v1/api/compat/{vehicles,trips,query}, v1/api/wait-time/unified/*, /data/{nhtsa-summary,texas-dmv}.json); pulled the full snapshot 2026-08-19 (1,627 vehicles: Tesla 890 incl. Bay 663; 5,000-of-6,249 trips, 93% receipt-synced; wait-time history) — snapshot lived in the session scratchpad and was LOST to a laptop reboot (/private/tmp cleared); all analysis results recorded here + in workflow journal wf_1d828670-2c6. 8-agent analyze (4 facets) + 10 adversarial verdicts: WAIT TIMES — NOTHING GLEANABLE, retro or prospective. History is ~7-day rolling (hours param capped 720 but data never exceeds 7d; possibly wiped on redeploy): Tesla series starts 2026-08-17T06:24Z (42 hourly runs, ONE sample point per area), Waymo/Zoox 2026-08-12. No July signal exists — a Wayback 2026-06-17 capture of the wait-times page says "Tesla wait-time tracking is currently down because the provider is blocking data collection" (last weekly aggregate 2026-04-10: Austin 33–54% availability, 17–18 min). Collector false-negatives dominate: Zoox LV read 0/8 available on ALL 955 runs Aug 12–19 while paid service is live; ALL 7 Tesla areas went 0/1 for 27 straight hours from Aug 18 while the same site logged 300+ Tesla receipt trips that day. Both verify lenses killed even a sidecar ingestion. RECEIPTS vs RIDES CORRIDOR — corridor holds. In-scope (TX/FL, ex Bay) receipt-synced rides Nov-2025→Jul-2026: n≈2,400 ex <0.5-mi hops, mean 3.81 mi (bootstrap CI 3.69–3.92), median ~3.0; Austin stepped 3.25→4.32 at the Jun-3-2026 METRO-WIDE Austin geofence expansion (Electrek/CleanTechnica; event not previously in repo notes); Aug is polluted by the Cybercab-event promo (332 sub-0.5-mi $3 hops on Aug 18–19). Sample is contributor-skewed (top-2 accounts 23% of trips, 45% exact-route repeats), so [4.7, 6.2, 8.3] mi/ride and the qual pin [4.5, 8.5] stand; a face-value read would move the corridor to ~[4.4, 5.2, 6.5] (+19% rides) — rejected as bias-uncorrected. FLEET REGISTRY vs JULY BAND — band [55k, 340k] not contradicted: registry-concurrent TX/FL fleet flat ~62–72 May–Jul (avg 65.6 Jul) vs TxDMV registered avg 140/day in July (only 104/181 VINs community-matched); late-July discovery burst (47 new plates Jul 21–Aug 1, ambiguous deploy-vs-discover). June calibration: 157k actual → ~78 mi/veh/day on the registry basis (the 115 constant is ~1.5x hot on this basis). 340k ceiling ⟺ 95 concurrent cars @115 all month. Both verify lenses ALSO killed adding a fleet-count clause to the pending 2026-07 rationale rewrite (coverage artifact; mislabeled 499k; self-contradictory 115) — that rewrite stays as spec'd in the 2026-08-17 later-2 entry, WITHOUT a fleet clause. NHTSA CROSS-CHECK — their dashboard numbers are the SAME mid-Aug NHTSA release, different method (current file only, latest-version per Report ID, NO operator-type filter, no archive): Waymo 1117 and Zoox 61 reproduce exactly under that method; their Tesla 45 = 24 ADS-file (all operator types) + 21 ADAS/Level-2-file "In-Vehicle (Commercial / Test)" reports (Tesla's FSD-Supervised commercial/test fleet, 14 CA incl. a Cybertruck — out of our scope by construction). Our 23/1,980/44 reproduce exactly from our snapshots; NO in-scope incident is missing. OPEN HUMAN CALL (verified by both lenses, surfaced to human): 13781-15395 (Houston stump, MAY-2026, "Remote (Commercial / Test)") — include in Tesla scope or not? Including → Tesla 23→24 (MAY-2026 3). Arguments FOR: NHTSA files it in the ADS file with ADS engaged; the vehicle's miles ARE in our denominator (fleet service miles incl. deadhead), so excluding its crash is a numerator/denominator mismatch; remote recovery mishaps are part of the service's safety record. AGAINST: a remote human was actively maneuvering the vehicle (guidance-vs-driving distinction; Waymo TeleGuidance never drives, so cross-helmer comparability cuts both ways). ALSO FLAGGED: EXPECTED_DRIVER_TYPES whitelists "Remote (Commercial / Test)" globally, so a Tesla report in a NEW operator mode never trips anti-Postel — the per-entity exclusion is silent; consider a guard that prints/asserts on excluded Tesla ADS rows. PROVENANCE FLAG (verified): the rides corridor's cited basis "trackers' observed 4-5 mi average paid ride, robotaxitracker-via-Electrek 2026-04-30" is UNVERIFIABLE — Electrek 2026-04-30 is fleet counts only (25 unsupervised, 165 active) and Electrek 2026-02-16 says ~500k miles (no ride length; the "~700k" figure matches other late-Jan/Feb press, e.g. Teslarati). The repo's own sourcery.html (~L4192, 2026-07-21) already recorded this and it was never propagated to the AGENTS 2026-07-01 entry, crashla.js:1939-41, or rides-provenance.qual.mjs. The corridor VALUE stands (human-approved 2026-07-22; receipt data corroborates within bias); the CITATION text is wrong — human decision on re-wording those three spots (quals are sacred; comment may be near human copy). TxDMV live check: Tesla 186 / Waymo 691 / Zoox 44 / Avride 344, flat Aug 11→18.
- 2026-08-18: RIDE AI ARTICLE CROSS-CHECK (human: "how do the Tesla/Zoox numbers in rideai.substack.com/p/waymo-wins-california jibe with ours?" → "follow the links"). NO REPO CHANGES. Ride AI (Sophia Tung, Aug 18): Tesla "unsupervised Austin fleet ~17, down from ~25 in spring" (= our notes) and "380,000 cumulative driverless miles to date" (= the Q2 deck's ">380k fully unsupervised" Jul-22 figure, presented as current and mis-scoped to Austin; it is the nobody-aboard SUBSET, NOT comparable to our 2.44M/2.61M paid-mile denominator — our SGO numerator counts None + In-Vehicle, so the full series is the matching denominator). Zoox section = safety-case framework only, no numbers. LINKS FOLLOWED: (1) zoox.com/journal/safety-case-framework (Aug 12) + its 12-page PDF (datocms 1786550724-zoox-safety-case-framework-2026.pdf): CIF = Collision/Injury(MAIS1+)/Fatality, miles per event, target "significantly safer than human"; human benchmark from NHTSA CRSS + FARS + FHWA SHRP2 + FHWA VMT parsed by road-speed mix; ZERO quantitative targets/miles/rates; footnote: Zoox "exempted from certain FMVSS". zoox.com/safety: reports Vol.1 2018 / Vol.2 2021 / Vol.3 2024 only — Zoox publishes NO operating/crash-rate data. (2) Electrek Aug-17 (Lambert): Nevada Transportation Authority approved Jul 27 (applied early June for 5,000) — 10-vehicle cap, Strip geofence, 45 mph, no Harry Reid pickups, re-approval to grow; unsupervised US fleet "roughly 20 operating at once", Austin peaked ~25 late April; ZOOX NV cap 100, "roughly 50 on the Strip" (consistent with our 300k/mo Zoox rows). (3) the cited @robotaxi X post is a Cybercab launch-event promo (ride through 8/23) — the "100+ Cybercabs since Feb / Austin by end-Aug" claims are Ride AI's memo reporting, not in the post. WATCH FOR IN THE SEPT-15 DROP (from Electrek Aug-18 "rams through bollards"): (a) AUSTIN BOLLARDS — unsupervised, passenger filming, car stopped for the flexible plastic posts several times then drove through; no injuries; video r/SelfDrivingCars (bladerskb) Aug 17; if Tesla files it: AUG-2026, "Other Fixed Object", operator None → counted; faultfrac by map ≈ 0.9–1.0 (cf. 11467=0.9 barrier arm after stopping, 4363=1.0). (b) HOUSTON TREE STUMP [CORRECTED 2026-08-19: the dating below was WRONG — it is ADS report 13781-15395 v1, incident MAY-2026 14:26, submitted JUN-2026 (Monthly), in the CSV since the 2026-07-15 snapshot, coded "Remote (Commercial / Test)", ADS Verified Engaged, Pole/Tree, 2 mph, PDO, the only Tesla Houston row; it is being silently excluded by PUBLIC_SERVICE_OPERATOR_TYPES (Tesla = None + In-Vehicle) TODAY, so the "human call when it appears" is already OPEN, not pending the Sept drop. Original erroneous note: "JUL-2026 … NOT in our data … lands in the Sept CSV".] Electrek also says "22 ADS incident reports filed with NHTSA" for Tesla vs our 23 deduped public-service incidents (21 Austin + 2 Dallas); their method unstated. NEVADA: Tesla permit only (not launched); when live, add a NV line to the Tesla VMT rationale (10 cars = negligible miles) and expect NV Tesla incidents in SGO.
- 2026-08-17 (later 2): TESLA JULY-VMT BAND + VMT-SOURCE SURVEY (human: "do we really have such a huge CI for Tesla July?" → "only what is impeccably epistemically correct" → "did robotaxitracker stop estimating VMT? other crowd-sourced sources?"). NO REPO CHANGES yet — findings + two proposed edits pending go. BAND VERDICT: tesla 2026-07 = 170k [55k, 340k] (0.32x–2x vs 0.85x–1.25x for deck-anchored months) is defensible as a placeholder calibrated to the Q2 scorecard (provisional rows ran 1.36x/2.9x/2.2x HIGH; end-Q2 kyoom band held with actual 1.4% above its floor); a first-draft suggestion to pull the ceiling to 1.5x June was WITHDRAWN as uncalibrated (the only post-deck datapoint, Tesla's Jul-22 ">10%/wk" claim, points up). BUT the row's floor rationale is MIS-SOURCED: "trackers put late-Jul at approaching 2.5M" — the press phrase "nearly 2.5M" IS the Q2 deck's end-June 2.44M restated on Jul 22 (verified: Yahoo/BI "7 numbers", Electrek Jul 22), and robotaxitracker's own model (live until the Aug 8–17 rebuild) was anchored ONLY through Dec-31-2025 (7k/250k/650k; never re-anchored to Q1 1.717M or Q2 2.44M) with its client-rendered nowcast value unrecoverable from Wayback (API captures 403) — so "2.5M − 2.44M ≈ 55k" is an artifact, not a July datapoint. PROPOSED (pending): rationale-only rewrite of that cell — floor ≈ 170k/2.9 (largest Q2 monthly overprediction, May), ceiling ≈ 2x June (launch-ramp month: Miami Jul 3, Orlando/Tampa Jul 21; ">10%/wk"), no independent July estimate exists — numbers unchanged, vmt.js regen. SOURCE SURVEY (20-agent sweep: tracker sites / community+social / official filings / press+analysts / Waymo+Zoox; every candidate fetched+verified; 518 tool calls): robotaxitracker.com CONFIRMED dropped its mileage model — Wayback: Jan-9-2026 extrapolation → Jan-31 linear anchor interp → Feb-9..Jul-22 "Anchor + Active-Vehicle Weighted Model" (EWMA S_t=0.25A_t+0.75S_(t-1), anchor-segment allocation, post-anchor nowcast, band clamp 10–25%); site rebuilt Next.js→Vite SPA between Aug 8 and Aug 17; live methodology = vehicle registry / rider-logged trips+receipts / wait times / first-spotted / govt data (NHTSA SGO, TxDMV, Waymo rollout); its live "miles" = contributor trip miles (10,499 mi over 2,922 trips as of Aug 8). Useful live byproducts: /data/texas-dmv*.json (TxDMV registry series) and /data/cpuc-waymo.json (Waymo CA VMT thru 2026-03 = our CPUC Q1 actuals). OTHER SOURCES — none credible for Tesla VMT: robotaxi-safety-tracker.com IS LIVE (correction to 08-10; data.json daily, fleet series to Aug 16 @143) but Austin-only fleet-count × constant 115 mi/veh/day: Austin cume 2.03M by Jun-1 (~390k/mo May) ≈ 2x Tesla's ALL-city ~200k/mo, implied current run-rate 143×115=16.4k/day (~490k/mo) vs Tesla's actual Jun 157k → ~3x hot, upper bound only. shadowmode.us retired its modeled mileage panel Aug 12 (changelog: "modeled from assumed constants … crash count hardcoded since Dec 2025"). Official TxDMV MCCS AV registry (SB 2807; API https://txmccs.txdmv.gov/api/TruckStop/companies/81edcff1-8a6e-4ed0-be1f-60668515e223/automated-motor-vehicles; front-ends texasavtracker.com by @dallasteslaclub + robotaxitracker): Tesla Robotaxi LLC 186 registered 2026 Model Y VINs (42 May-28 → 69 Jun-11 → 102 Jul-2 → 175 Jul-15 → 181 Jul-30 → 186 Aug-11), Waymo 691, Zoox 44, Avride 344 — fields are VIN/make/model/year only, Ch. 220 rules require NO mileage reporting; registered ≫ active (robotaxitracker 64 rider cars active/7d, 17 unsupervised; Gary Black Aug-13 "stuck at 90–100"). Community/X: @raines1220 fleet-mileage/MPI model relayed by Sawyer Merritt Feb-17-2026 (Q4-25-chart calibrated) — nothing newer; Reddit unreachable to the crawler (bot walls) = genuine blind spot; Manifold has no mileage markets; Substacks (Driverless Digest, Understanding AI, AV Market Strategist) nothing independent. Press/analysts: all restate the deck; Morgan Stanley Aug-11 = fleet forecast (1,500 by YE-2026) no miles; Tesla 10-Q (filed Jul-23) has zero mileage words. STRAY-CLAIM WARNING: basenor.com's "450,000 unsupervised miles/week target by end-2026" is unsourced/likely fabricated and search-engine summaries repeat it. Waymo: hub still 220.6M thru Mar; CPUC Q2-2026 (due Aug 1) NOT posted as of today (page last updated May 11); OWID grapher = CPUC replot. ZOOX — the ONE genuinely new datum (official, not crowd): "over 3 million miles on public roads" + ~1M riders (Bloomberg/Claims Journal Aug 6; LV Sun/GovTech Aug 7; Axios Aug 12). vmt.csv zoox end-Jul cume 3,051,000 [2.10M, 4.00M] lands within ~2% of it. PROPOSED (pending): pin it as a third milestone in zoox-vmt-provenance.qual ({month "2026-07", published 3,000,000}, same convention as the ~1M/~2M pins; would pass immediately, red-first not possible) + cite it in the 2026-07 rationale; scope caveat: "miles on public roads" vs earlier "autonomous miles" wording. TRICKS: individual X posts readable via https://cdn.syndication.twimg.com/tweet-result?id=<ID>&token=a; Wayback raw captures via web.archive.org/web/<ts>id_/<url> (gzip); WebFetch cannot reach web.archive.org (use curl).
- 2026-08-17 (later): FAULTFRAC MONTHLY BATCH DONE (104 incidents; human: "ok go"). Method: 12-agent workflow — the 104 unrated incidents (dumped from INCIDENT_DATA minus faultfrac.csv, date-sorted) split into 3 batches; each batch assessed by 3 INDEPENDENT assessors with different framings (map-first / anchor-first / skeptic) all reading the same locked criterion doc (P(expert human avoids) anchors + the scenario→value map + 46 stratified exemplar rows from faultfrac.csv), then one adjudicator per batch reconciled (majority unless the outlier cites a missed narrative fact; locked map value always; house-style reasoning). Result: 92 unanimous / 11 majority / 1 split; 7 flagged for human. Every non-zero and non-unanimous case was then re-read by the orchestrating agent against the narrative AND the cited precedents (all precedent values verified in the CSV). Two adjudicated values overridden: 30270-15468 0.1→0 (AV braked TO A STOP as an oncoming van crossed the double yellow on a curve — the file scores low-speed braked-to-stop crossers 0: 14627/14492/10776/8217; only 12045 sits at 0.1) and 30270-15670 0.9→1 (reversing into a parking stall struck a POLE — both pole precedents 13781-13645/30270-7794 are 1.0 per the visible-fixed-object anchor; the 0.9 line is curbs/bollards/bars). Final distribution: 71 zeros, 5×0.05, 14×0.1, 5×0.2, 1×0.25, 1×0.4 (barrier arms after lead SUV, =9192/9561), 1×0.7 (undercarriage on unspecified lot pavement, =13443/13467), 3×0.8 (raised lot entrances), 2×0.9 (roof sensor vs carport beams), 1×1. Notable: three "AV merged/lane-changed into a turn lane ahead of a stopped truck at the green" = 0.2 (11759 pattern); Zoox 30610-15707 (stopped; cyclist ran stop sign, lost control into it) = 0. Rows appended via a merge script that pulls the 6 structured columns from slurp.fault_master_row() on the raw snapshot rows (asserts exact coverage of the unrated set, allowed values, reasoning style) — slurp's subsequent sync reported 0 field updates. SEVERITY_OVERRIDE gained 4 rows for the drop's "Unknown"s (3 ambulance transports → Minor W/ Hosp: 6f1e8cc59e2ea27, 9fc806cd1d491d5, 4366476607eca89; "unknown injuries", no transport → Minor W/O: eeafa92b2068aa7). slurp re-run deterministic (snapshots unchanged). QUALS: 52/52 green; faultfrac.csv 2,047 rows = incidents 1:1. MAP CLARIFICATIONS surfaced (added to the agent memory map): undercarriage grounds on an UNSPECIFIED lot pavement feature = 0.7; low-speed AV that brakes to a stop as an oncoming/wrong-way vehicle crosses into it = 0 (the 0.1 double-yellow line is for an AV still moving at speed); reversing/driving into a POLE = 1.0 (visible-object anchor) vs bollard/curb/bar = 0.9.
- 2026-08-17: AUG NHTSA DROP SLURPED (human: "what is slurp.py complaining about?" → "ok go"). slurp.py had died on its anti-Postel whitelist: the Aug-17 current CSV (snapshot nhtsa-current-20260817T150549.csv) carries TWO never-seen values — (1) Reporting Entity "Gatik AI Inc." (one row, 30451-15790, JUN-2026 Lowell AR, in-vehicle+remote → filtered out anyway; added to EXPECTED_HELMERS) and (2) Highest Injury Severity Alleged "Serious W/ Hospitalization" (Waymo 30270-15547 v1, JUN-2026 Scottsdale AZ: AV stopped at a red in a left-turn lane, speeding SUV crossed two lanes and rear-ended it; SUV driver transported by ambulance "with serious injuries"; CP airbags, both towed) — a genuinely new NHTSA severity string. Added to slurp EXPECTED_SEVERITIES AND to crashla.js SEVERITY_INFO as {rank 5, injury, hosp, ssi} — identical to bare "Serious" (KABCO A; "Serious" already implies transport), so it lands in the injury/hospitalization/SSI+ metrics. shortenSeverity: an agent-drafted ["Serious W/", "Serious (hosp.)"] rule was BACKED OUT after the verify pass — (i) new user-visible copy → rule 6, and the "go" had not named that copy; (ii) its prefix needle would have mislabeled the dictionary-defined "Serious Without Hospitalization" sibling as "(hosp.)". The human then specified the copy ("Serious injury (hosp.)"), so the rule now exists as ["Serious W/ Hospitalization", "Serious injury (hosp.)"] — keyed on the FULL string, TODO recap comment above it per rule 6; severity-display.qual pins it AND pins shortenSeverity("Serious W/O Hospitalization") === "Serious" (the sibling must keep falling through). NHTSA republished the SGO 2021-01 Data Dictionary the same day (2026-08-17; field 73 drop-down: Fatality / Serious With|Without Hospitalization / Moderate With|Without / Minor With|Without / No Injuries Reported / Unknown) — "Serious Without Hospitalization" is defined but unseen; when it arrives it is ssi but NOT hosp, breaking the ssi ⊆ hosp nesting the quals assume (human decision; noted in the SEVERITY_INFO row comment). QDD: red-first — severity-classification.qual got a new (e) SLURP↔APP SYNC check (imports slurp.py, asserts every EXPECTED_SEVERITIES value except "" has a SEVERITY_INFO row — this is the sync the slurp.py comment demands, now enforced at whitelist time instead of at the next data refresh) plus regression expects for the new value. Red, then green. Slurp then ran clean (live fetch == the day's snapshots, deterministic; faultfrac sync 0 field updates): 1,943 → 2,047 incidents (+104, 0 dropped): Waymo +86 JUN/+11 JUL, Zoox +4 JUN/+1 JUL, Tesla +2 JUN. vmt.js: 2026-06 coverage flipped partial(0.1689)→complete now that the JUL Monthly wave is in; 2026-07 rows entered at pooled incident_coverage 0.1106 (lo 0.0446; 12 observed / 108.5 expected). README count line updated in place (2,047 as of 2026-08-17: 1,980/23/44). QUALS: 51/52 green; fault-coverage RED by design — the 104 new incidents have no faultfrac rows yet (the usual monthly batch under the locked P(expert-human-avoids) criterion is the next step; 4 of the 104 are severity "Unknown" and want SEVERITY_OVERRIDE entries per the 2026-07-15 convention). VERIFY PASS (19-agent workflow: 5 reviewers × classification / enumeration sweep / new-data categoricals / qual quality / independent data recompute, 2 skeptics per finding): classification confirmed against the dictionary text; no other enumeration in crashla.js/index.html/style.css/READMEs/quals misses the new string; the 104 new records introduce NO new values in belted/road/svMovement/cpMovement/crashWith/state; independent recompute from the two snapshots reproduces 2,047 (1,980/23/44) exactly and vmt.js/faultfrac.csv diffs are exactly as expected; the two quals were confirmed red under HEAD-crashla.js and section (e) catches the whitelist/classification desync before any slurp run. Refuted-as-nits (left alone, pre-existing or comment-only): the SEVERITY_INFO legend/crashla.js:448 comments and severity-classification qual (c) message still name ssi as "Serious"/"Fatality" (semantics unchanged; the row comment states the third member); slurp.py's stdout passenger summary counts only one of the two no-passenger encodings (console-only; app-side PAX_NONE is correct); monthly-series qual:287 message says "(Moderate W/ Hosp + Fatality)" (stale since the 2026-06 SSI+ fix; message-only). NOTE for the batch: 30270-15547 itself is a textbook stationary-AV-rear-ended = 0.
- 2026-08-10: TESLA-VMT SOURCE SURVEY (human: "is robotaxitracker.com up to date? other Tesla VMT estimates out there?"). NO REPO CHANGES — no new anchor found; findings only. robotaxitracker.com: SPLIT verdict — the live fleet/wait-time dashboard is current (rolling 7-day window: 64 rider vehicles, 17 unsupervised, 71 inactive/30d-unseen, 58 Cybercabs test fleet, unsupervised ride share 14.3% "3 of 21 rides") but its news feed stopped 2026-04-10 and it NO LONGER publishes a mileage model (about page: tracks trips/revenue, not total miles) — treat it as a fleet-count source only (Wikipedia cites it for fleet tallies: ~135 in service Dec-2025, Bay ~106), not a VMT source; the tesla 2025-06/07 rows' "robotaxitracker end-of-month cumulative delta" rationale describes a feature the site no longer surfaces (historical provenance, still accurate for those rows). Independent Tesla VMT estimates: NONE newer than the Q2 deck exists anywhere found — BI Jul-22 "7 numbers" is all Tesla's own figures (~2.5M paid, >380k unsupervised subset, ">10%/wk"); Electrek/TechCrunch/MLQ/NewsCord are chart-eyeballs of the same deck (still sloppy: "roughly 500k in April" vs vector-extracted 374k; MLQ "fleet shrinks to 21"); robotaxi-safety-tracker.com static page still Jan/Feb-2026 vintage (115 mi/veh/day model); thechargeport: "not disclosed"; teslafsdtracker has a Robotaxi (CYBRLFT) section but JS-only, no fetchable miles. So the deck-series + wide Jul placeholder (170k [55k, 340k]) remains the honest state of knowledge; next anchor = Q3 deck ~late Oct. ALSO CHECKED: NHTSA current CSV last-modified still Jul 15 12:35 GMT (curl HEAD 2026-08-10) — July drop pending, ~Aug 15; Zoox paid LV rides went LIVE Aug 10 as scheduled (Strip-corridor pickups: MGM Grand, Resorts World, T-Mobile Arena, Fashion Show Mall, Wynn, NY-NY, LVCC; comfort-tier pricing ~20-40% above UberX, no-reroute-surcharge guarantee; first-ever fares in a no-controls purpose-built AV). Phoenix: still staged (~60 Model Ys), not launched.

- 2026-08-06: ERROR-HUNT + freshness audit (human: "deep research to hunt for errors and discrepancies + bring VMT estimates/projections up to date; keep working till at least one real error"). VERIFIED CLEAN by independent recompute (scratch scripts, not the quals' own code): vmt.csv contiguity/running-sums/band-ordering/kyoom⊆running-sum/monotonicity (all 102 rows); vmt.js embedded CSV field-for-field == master (99 rows; the three 2026-07 rows correctly held out); faultfrac.csv ↔ incidents.js 1:1 (1,943/1,943, zero value mismatches); every humanMPI band edge recomputes from its cited IPMM; Tesla RIDES_HISTORY implied mi/ride = 6.2 on every row; Waymo rides-forecast arithmetic (31 wks × 600-750k) checks; Waymo safety hub re-fetched LIVE — unchanged (220.6M thru Mar-2026; 3.91/1.68/0.23; per-city figures round correctly, e.g. 67.078→67.1). Faultfrac criterion-anchor pattern scan flagged 5 rows; all read as defensible judgment calls. The zero-"Unknown"-severity surprise (was 16) traced to the documented 2026-07-15 SEVERITY_OVERRIDE batch in slurp.py — deliberate, not a bug. NHTSA: the human's same-day re-slurp (incidents.js fetch-date 2026-08-06 in the working tree, PRESERVED) shows the CSV still unmodified since 2026-07-15 — July drop pending. CPUC Q2-2026 still unpublished. REAL ERROR FOUND: the waymo 2026-06/07 rationales claimed "Atlanta paused May 21 (resumed by mid-Jul)" / "Atlanta back since mid-Jul" — REFUTED by Atlanta News First 2026-05-28 (pause began May 20 after the floodwater stranding; service began resuming May 27, weather-dependent), so Atlanta was back BEFORE June and that half of the June growth-offset premise was wrong; only the freeway suspension (May 21 → Jul 29 Phoenix; LA/SF Bay confirmed rolling out early Aug per NBC LA/Patch) actually spanned June. FIXED prose-only, centrals UNTOUCHED (the freeway suspension alone justifies the flat Jun/Jul; Atlanta is ~2-3% of miles, ±30% swamps it): both rationales corrected in vmt.csv, the Jun one identically in vmt.js. ALSO FIXED: waymo 2026-04/05 rationales + README §Apr–May said "~3,000 cars/vehicles", contradicting the repo's own FLEET_HISTORY (3,300 Feb → 3,750 May) and FutureSearch ~3,600 Q1 → now "~3,600-3,750"; zoox 2026-07 rationale firmed to the announced paid-LV launch date Aug 10, 2026 (Reuters/CNBC/TechCrunch, Aug 5). No new quals (external-fact prose corrections, not machine-checkable; numbers unchanged so all pins stand). 52 quals pass before AND after; live headless-chromium check (localhost serve + playwright-core shell 1228, binary now at chrome-headless-shell-mac-arm64/chrome-headless-shell): all sections render, corrected rationales visible in the VMT-sources table, zero console/page errors. Predmarkets untouched — the resolved Manifold Tesla-at-fault market (0.937) REMAINS enabled, still awaiting the human call flagged 2026-08-04. Tesla: no anchor newer than the Q2 deck; late-Jul trackers still "approaching 2.5M"; the 170k Jul placeholder stands. NEXT unchanged: ~mid-Aug NHTSA July drop → slurp + faultfrac-assess + the ledger freshness pin advances to 2026-08; Q3 deck ~late Oct; Waymo hub refresh ~Sep.

- 2026-08-04: MONTHLY PRE-DROP CYCLE + freshness research (human: "deep research latest VMT + double check everything" → "go for all of that"). RESEARCH: Waymo safety hub UNCHANGED (220.6M thru Mar-2026; IPMMs 3.91/1.68/0.23 — repo constants verified); freeways RESUMED Jul 29 (Phoenix first, LA/SF Bay "coming days"); the Jul-8 four-city announcement = San Diego, Las Vegas (driverless same day, employee-only per CNBC), Tampa, Denver — NOTE Waymo's own Jul-8 blog post has NO operational metrics; the ~500k rides/wk + 4M+ mi/wk figures are Jul-2026 press coverage (AFP / Driverless Digest), cite them as press not disclosure. TESLA: no anchor past the Q2 deck (2.440M end-Jun); trackers put late-Jul at "approaching 2.5M"; Austin unsupervised fleet stalled ~17-21 cars; Miami driverless Jul 3, Tampa/Orlando Jul 21; Phoenix staged (~60 Model Ys, first-responder plan filed) not launched; press still repeats chart-eyeball figures ("~900k/quarter") — deck series stands, no recalibration. ZOOX: NHTSA FMVSS exemption GRANTED Jul 30, 2026 (first ever; 8 standards, ≤2,500 veh/yr, 2 yrs), paid LV rides begin Aug; ~500k riders / ~2M cumulative miles still the latest figures. CPUC Q2-2026 not yet published. Two new third-party studies FLAGGED, not incorporated: IIHS ~Jul-24 (~50M Waymo mi 2021-24, 68% fewer police-reportable crashes/mi, Austin the one city HIGHER) and Hunter College/Sam Schwartz Jul-20 (Waymo serious-injury-or-death rate 1.5x HIGHER per mile than NYC professional FHV fleet, >3x in SF — professional-driver cohort + different metric; published counterpoint to the serious+ multiple). CHANGES (all human-approved via "go for all of that"): (1) 2026-07 vmt.csv rows added ahead of the ~mid-Aug NHTSA drop: waymo 21M → cume 301.6M ±30% (July still freeway-suppressed; resumption is an Aug tailwind); tesla 170k → cume 2.61M with wide placeholder band [55k, 340k] per the Q2 utilization lesson (floor = the tracker read taken literally, ceiling ~2x June); zoox 300k → cume 3.051M, 0.7x-1.3x. Rows are INERT until the drop (build_vmt_csv filters to incident months) — vmt.js diff verified rationale-only, no 2026-07 leak, incident_coverage untouched. (2) Stale prose refreshed: 11 zoox rationales' "FMVSS…no date as of Jun 2026" → granted Jul 30, 2026; zoox 2026-06 "undecided" likewise; waymo 2026-06 "still paused mid-Jul" → resumed Jul 29; the audit's May-21→May-19 freeway-pull nit was INVESTIGATED AND REFUTED on review — TechCrunch's own suspension article (2026-05-21) says Waymo confirmed it "on Thursday" (= May 21, a Thursday), so May 21 STANDS and the nit's "May 19 (TechCrunch)" attribution was wrong (techtimes' "72-day suspension" arithmetic was the loose source); the "~51%" nit was already fixed in an earlier commit; CPUC Q1-2026 actuals now cited in the waymo (9.09/8.19/9.11M ⇒ 52.9% share) and zoox (32.0/38.2/51.1k) Jan–Mar rationales; README Jun-2026 recall bullet notes the Jul-29 resumption. (3) NEW quals/vmt-ledger.qual.mjs (red first): per-helmer month contiguity + exact running sums + freshness-through-2026-07 + cume pins on the new rows + ISO-month format + monthly band bracket 0 < vmt_min <= vmt <= vmt_max (the app asserts band ordering only for active months; future rows needed the csv-level guard). ALSO NEW quals/zoox-vmt-provenance.qual.mjs (green-at-birth guard, no red possible without corrupting data): zoox had NO provenance qual — now pins the ~1M-late-2025 and ~2M-late-Mar-2026 published milestones (kyoom band contains the round number, central within 15%). It caught a REAL BUG mid-cycle: the FMVSS rewording introduced commas into 7 unquoted zoox rationales — slurp's row[8] read silently truncates such a rationale at its first comma and vmt-master-fidelity CANNOT see it (both sides truncate identically); the ledger now parses quote-aware and demands exactly 9 fields per row (went red on the corruption, green after quoting the rationales). (4) vmt.js regenerated OFFLINE (scratchpad script monkeypatching fetch_nhtsa_csv to the checked-in snapshots; byte-identical baseline verified on the unedited master first; NHTSA unchanged since 2026-07-15 — the working tree's incidents.js fetch-date bump is the human's same-day re-slurp, preserved). (5) Predmarket snapshot refreshed 2026-07-02 → 2026-08-04 via the checked-in script; it WARNED: manifold "Tesla Robotaxi Service at-fault accident or non-fully-autonomous by 2026?" has RESOLVED (last price 0.937, still enabled in the snapshot) — resolution handling is a HUMAN call per the script header: disable or replace it; the other 12 markets are open. 52 quals pass (was 50; the 07-24 entry's "53" was a miscount — the file count has been 50 since March). Self-review pass afterward (human: "are you totally sure?"): line-by-line diff audit of all 6 changed files (vmt.js changed lines verified numeric-prefix-identical via sort|uniq; predmarkets diff verified prices/volumes/date-only, no question/slug/enabled/url changes; incidents.js still just the fetch-date pair) + primary-source re-verification, which caught the two rationale fixes recorded above (May-21 refutation, Jul-8 press-not-disclosure) + live headless-chromium load (serve + playwright-core, cached shell 1228): full render, zero console/page errors. Qual ideas CONSIDERED AND REJECTED, revisit if wanted: predmarket snapshot-staleness (a time bomb that goes red with no code change) and resolved-market-must-be-disabled (would be red right now and would force the pending human call on the resolved Tesla market into a failing suite — the refresh script's WARNING is the designed surface). NEXT: ~mid-Aug NHTSA July drop → slurp + faultfrac-assess + the ledger freshness pin advances to 2026-08; Tesla Q3 deck ~late Oct re-pins the July placeholder; Waymo safety-hub refresh ~Sep.
- 2026-07-24: FULL NUMBERS AUDIT + fresh VMT research (human: "careful audit of all our numbers and the code + research latest VMT"). VERDICT: pipeline arithmetic CLEAN — independent (non-qual) recompute of vmt.csv cumulative sums/band ordering/month contiguity, vmt.js fidelity, faultfrac 1:1 coverage (1,943 rows = 1,883 Waymo + 21 Tesla + 39 Zoox, all in [0,1]), and the pooled incident_coverage (0.1689/0.0822 for 2026-06) reproduced exactly from first principles; human-benchmark constants all check against their cited IPMM rates. RESEARCH: (a) Tesla Q2 deck anchors CONFIRMED by Tesla's own prose (Q1 deck "1.7 million" end-Mar; Q2 call "~2.5 million" = the 2.44M chart) — Electrek/MLQ's "~1.5M end-Q1 / ~500k April / ~900k per quarter" are chart eyeballs internally inconsistent with Tesla's stated endpoints; do NOT recalibrate to press reads. Jul-2026: Miami driverless Jul 3, Orlando/Tampa Jul 21, ~20 active TX cars, "10%/wk" claim. (b) Waymo: no anchor newer than 220.6M-thru-Mar (safety page unchanged); freeway pause ongoing mid-Jul; CPUC Q1-2026 IS OUT, month-level VMT unredacted: CA driverless 9.093/8.192/9.105M Jan/Feb/Mar (+pilot ~20k/mo) ⇒ 52.9% CA share and monthly shape within ~4% of our series — first out-of-sample confirmation of the CA-scaling methodology; Q4-2025 actuals 7.90/8.41/8.42M ⇒ 56.4% share (the "~51%" previously written was wrong). (c) Zoox: >500k cumulative riders by late Jun (redesign PR 2026-06-25) — Q2 ridership ~3x Q1's pace; CPUC CA driverless 32.0/38.2/51.1k Jan/Feb/Mar; FMVSS exemption still pending, rides still free. CHANGES (human: "sure"): (1) atfaultInjury HumansAV band re-derived 272k–1,050k → 133k–956k — its derivation cited the retired blended injury band (256k/524k) after the injury metric's repin to the Kusano per-city range (125k/478k); same documented formula, current inputs; subset chains hold. (2) RIDES_HISTORY Zoox 2026-06 265k → 333k [250k, 417k] on the 500k-rider milestone; rides-provenance.qual pin added; RIDES_FORECAST Zoox 450k → 550k [350k, 1.1M]. (3) MPI tooltip now shows incident_coverage BEST with worst-case in parens (was showing the min labeled as plain "incident coverage"). (4) slurp.py incident_coverage docstring corrected (claimed best assumes f=1.0; code returns the pooled rate-ratio). (5) data/README.md schema line gained the kyoom cols + editing rule; README.md June-partial-month prose modernized (full-June since the archive merge) and CA-share table/bullets updated to the CPUC actuals. 53 quals green before AND after. DEFERRED to the next slurp cycle (vmt.csv edits require regenerating vmt.js): rationale nits — Oct–Dec "~51%" → 56.4%, freeway pull "May 21" → May 19 (TechCrunch), cite CPUC Q1-2026 actuals in the Waymo/Zoox rationales; 2026-07 rows needed before the next NHTSA drop (~mid-Aug; the slurp staleness guard will demand them); optional Jan–Mar re-shape to the CPUC profile (≤4%, immaterial within ±25% bands). Memory: project_cpuc_q1_2026_actuals.md.
- 2026-07-22 (later): Q2 DECK LANDED same day — scored the morning's provisional rows and re-pinned (human: "go"). ACTUAL (Q2-2026 deck p9, vector-extracted, 13 points; Electrek's "past 2.4M" corroborates; overlapping months re-confirm the earlier series ≤8k): Apr 2,091k / May 2,283k / Jun 2,440k cume (monthly 374k/192k/157k). SCORECARD: chart-anchored history perfect; the provisional Q2 extension ran HIGH — predicted delta +1.41M vs actual +0.72M (Apr 1.36x, May 2.9x, Jun 2.2x high); end-Q2 kyoom band [2,406k, 4,279k] HELD, actual 1.4% above its floor. DIAGNOSIS: utilization-led slowdown (miles fell ~2.4x, active fleet only ~1.5x, and it started in MAY not June) — fleet-count activity ratios can't see utilization collapses; lesson recorded in the qual header + memory: within-quarter extension months are wide-band placeholders. CHANGES: vmt.csv Apr/May/Jun re-pinned to the chart (kyoom [0.97c, 1.15c], vmt [0.85v, 1.25v]); tesla-vmt-provenance.qual extended (red first) with the 3 new pins + Jun kyoom_min ≥ 2.3M floor; rides 2026-06 row 505k→394k [294k, 519k] (implied mi/ride 6.2, in-pin); MILES_FORECAST Tesla A {10M,7M,15M}→{4.5M,3.4M,6.5M} (Jan-1-2027: 2.44M + ~1M trough-persists to ~4M near-Q1-trajectory, central +2M on the Jul Miami/Orlando/Tampa unsupervised launches + ~175 registered TX cars), B REVERTED to {40M,15M,120M} (the same-morning x1.25 per-vehicle rescale didn't survive the Q2 utilization data), C unchanged; RIDES_FORECAST A→{730k,410k,1.38M}, B→{6.5M,1.8M,25.5M}; fleet-forecast.qual robotaxi-median pin [7M,15M]→[3.4M,6.5M] (=A's band; median lands ~5M — REPIN NOT EXPLICITLY PRE-APPROVED beyond "revisit A", veto-able). NEW SUB-SERIES disclosed: 380k+ "fully unsupervised" miles (nobody aboard; subset) — third Tesla series, don't confuse with the denominator or Bay. Jul-2026 facts for next cycle: Miami/Orlando/Tampa unsupervised (six ramping metros), Cybercab production started + employee campus rides. IMPACT: Tesla default-window VMT 2,834,587 (this morning) → ~2,310k (−19%; net −6% vs the pre-correction original — the old series' Q2 overshoot outweighed its Q1 undershoot in-window), Tesla MPIs down accordingly; stress-test ratioHi back to ~2.63 (within both the old 3 and current 3.5 bound). 50 quals pass; live check clean.

- 2026-07-22: TESLA VMT RE-ANCHORED to Tesla's own disclosures (human-approved; prompted by an external "cumulative is ~1M" claim — REFUTED: that figure matches Tesla's SEPARATE Bay-Area supervised series, "crossed more than a million miles" by Oct 2025 per the Q3 call, out of SGO scope). PRIMARY SOURCE: the "Cumulative Paid Robotaxi Miles" charts in the Q4-2025 (p11) / Q1-2026 (p9) update decks, monthly values VECTOR-EXTRACTED from the PDF chart paths (pypdf ContentStream + gridline calibration; decks fetched via Wayback — assets-ir.tesla.com 403s fetchers; overlapping months agree within ~3k mi). New cume: Aug-25 20k, Sep 121k, Oct 266k, Nov 457k, Dec 658k, Jan-26 955k, Feb 1,279k, Mar 1,717k — the old tracker-interpolated rows had Aug 76,498 (3.8x HIGH) and end-Q1 1,563,047 (9% LOW; the "~2x Q4" ratio-read was right, its Q4 base wasn't). Q2 rows = same activity ratios on the corrected base (Apr 510k, May 550k, Jun 352k; end-Jun cume 3,129,000, was 2,691,047), marked PROVISIONAL in the rationales — recalibrate against the Q2 deck (earnings TODAY 2026-07-22; these rows are the prediction to compare). SEMANTICS (human asked for definitive): "paid" = fleet service miles INCL. DEADHEAD (Elluswamy Q3 call: fleet "covered more than a quarter million miles"; trackers' 115 mi/veh/day equates the disclosures to fleet-wide miles; per-vehicle throughput math agrees); residual ambiguity is one-directional (literal reading ⇒ truth HIGHER) so chart-month kyoom bands are asymmetric [0.97c, 1.15c]. KNOWN TENSION, chart wins: call's "more than a quarter million" @Oct-22 vs chart-interpolated ~224k (verbal rounding). NEW quals/tesla-vmt-provenance.qual (red first) pins all 9 chart months + kyoom_min(2026-03) ≥ 1.6M + Q1/Q4 delta ratio ∈ [1.6, 2.4]. CASCADE: (a) rides — the old [7.6, 10, 13] mi/ride corridor was calibrated to "~700k paid by late Apr 2026", actually a MID-FEBRUARY figure (Electrek 2026-02-16, Q4-call era), so Tesla RIDES_HISTORY was ~2x low; rebuilt at [4.7, 6.2, 8.3] (4-5 mi paid ride ÷ 0.6-0.85 on-trip share): Sep-25 19.5k, Dec 106k, Mar-26 277k, Jun 505k; rides-provenance.qual pin [7,14]→[4.5,8.5] (HUMAN-APPROVED). (b) forecasts (human-approved "recalibrate now"): MILES_FORECAST Tesla A {8M,5.5M,12M}→{10M,7M,15M}, B {40M,15M,120M}→{50M,19M,150M} (per-vehicle rate ×1.25), C UNCHANGED (personal-HW4 basis, not Austin-calibrated); RIDES_FORECAST A→{1.6M,840k,3.2M}, B→{8M,2.3M,32M}, C unchanged; fleet-forecast.qual's robotaxi-median-in-[7M,15M] pin still passes UNREPINNED. (c) stress-test.qual Tesla all-incident ratioHi bound 3→3.5 (denominator +16% moved it 2.78→3.23 exactly proportionally; the "ambiguous" verdict assert is separate and unchanged) — FLAG: this one pin update was NOT explicitly pre-approved; veto/revert if unwanted. TOOLING: vmt.js regenerated OFFLINE via a scratchpad script replicating slurp main() against the latest snapshots (verified byte-identical on the unchanged master first; June pooled incident_coverage 0.1645→0.1636 via the Tesla-June VMT ratio). README Tesla bullets rewritten (deck-chart primary + Bay-separate-series + deadhead reading + historical note). Also deleted a stale self-contradicting sentence in the [AI TEXT] rides comment ("Miles are intentionally NOT a toggle option" — miles IS a toggle) and the Jun-row rationale's superseded 5.7k-mi/day decomposition note. IMPACT: Tesla default-window VMT 2.691M→3.129M (+16%), Tesla MPIs ~+16% (better; the old series was biased AGAINST Tesla — the corrected numbers moved AWAY from the external ~1M claim, which was already refuted by Tesla's 1.717M PAID floor disclosed four months before the claim was made). 50 quals pass (was 49).

- 2026-07-02: Prediction-market snapshot refreshed (was 2026-06-19) via NEW checked-in tool data/refresh-predmarkets.mjs — refetches every slug's prices/volumes in place with the same shaping as crashla.js's fetchers, preserves slug/enabled curation + the header comment + the file's exact layout (answers stay one-line), bumps PREDMARKET_SNAPSHOT_DATE, and WARNS on any resolved market (resolution handling stays a human call; the header now points at the script). Third refresh in a month justified the tooling. All 13 markets verified still open (nearest close: the will-tesla-count-as-a-waymo-competi retrospective, 2026-09-01 — rerun the script then; it will warn). Diff was pure price/volume movement (Musk-out 5.2%→3.4%, Tesla-more-rides 10.2%→9.5%, level-4-summer-2025 15.7%→14.7%, Tesla>Waymo-vehicles 23%→22%). prediction-markets + predmarket-escaping quals pass; 49 total. UPCOMING (calendar, not code): (a) ~mid-Jul 2026: NHTSA publishes June incidents → monthly cycle: slurp, faultfrac-assess new incidents per the locked criterion, add 2026-06 vmt.csv rows (Tesla tracker delta, Waymo extrapolation until next hub anchor, Zoox milestone interpolation); (b) ~Sep 2026: next Waymo safety-hub update → new cumulative anchor + possibly refreshed benchmark IPMMs (waymo-vmt-provenance + human-benchmark-provenance quals will police); (c) 2026-09-01: first market close.

- 2026-07-01 (later 5): Firmed up the Tesla/Zoox cumulative-rides lanes (human-approved) — they were self-labeled "low-confidence guesses"; now both are externally anchored (rides-provenance.qual extended, red first). TESLA: rides = the repo's tracker-anchored cumulative VMT ÷ a [7.6, 10, 13] miles-per-ride corridor, calibrated on robotaxitracker-via-Electrek (2026-04-30): ~700k PAID miles by late Apr 2026 at a 4-5 mi average ride → ~140-175k Austin rides against our 1.97M total-scope miles (which add deadhead + the Bay-Area monitored mode; the Bay ride count is the residual judgment, bounded). New rows: 2025-09 17k [13k, 22k] (was 10k), 2025-12 65k [50k, 86k] (was 40k), 2026-03 156k [120k, 206k] (was 90k — the old guesses implied 16-17 mi/ride), 2026-06 290k [215k, 385k] (was 180k; extrapolates 1 month past the VMT master). Qual pins implied mi/ride ∈ [7, 14] for every row with a VMT-master month. RIDES_FORECAST Tesla A/B recalibrated to the same corridor: A 650k→800k [500k, 1.3M], B 3M→4M [1.5M, 12M]; C unchanged. ZOOX: the old rows (100k Jan-26 → 400k Jun-26) CONTRADICTED the published rider milestones the VMT series itself cites (>300k riders late 2025, >350k late Mar 2026 [CleanTechnica 2026-03-24 / Robot Report] ⇒ ≥150k rides by Dec-2025 even at 2 riders/ride, and a much FLATTER trajectory). New rows = riders ÷ occupancy 1.2-2.0 (central 1.5): 2025-12 200k [150k, 265k], 2026-03 235k [175k, 300k], 2026-06 265k [195k, 345k] (extends ~12k riders/mo); forecast 1.1M→450k [300k, 900k] (rider trajectory + Miami/Austin/4x-SF expansion + paid rides from 2026 [Fortune 2025-12-08]). NOTE: Zoox miles/rides ≈ 9-10 does NOT cross-constrain — Zoox's ~2M "autonomous miles" are testing-heavy, not service-only. Latin note updated to say all three lanes are anchored/derived (TODO English in the adjacent comment, AWAITING HUMAN). Waymo untouched. 49 quals pass; rides charts + zero-error interaction sweep verified live.

- 2026-07-01 (later 4): Completed the long-open "full-coverage line-by-line review of the UI/rendering code" TODO (from 2026-06-27/28). Read the entire rendering layer (both top charts, per-helmer charts, cards, stress/benchmark tables, legends, slider, incident browser, URL state, all 11 sanity builders, tooltips, predmarkets, init) + a zero-error live interaction sweep (all growth metrics, every helmer toggle, every metric incl. fatality, slider drags, cumulative toggle, all sections collapsed/expanded, sorts, filters; URL state round-trips). VERDICT: no correctness bugs; escaping (escHtml/escAttr) consistent on every NHTSA/user-text path; parallel structures assert-guarded. FIXED (human-approved) 6 nits: (1) stale DIST_VIS_FLOOR comments ("~2px"/"~1%") left over from the human's 1%→5% bump in the curve-peaks bugfix commit; (2) stale "they're all the same per helmer" comment on the VMT-sources rationale collection (it collects DISTINCT per-era rationales); (3) renderFleetForecastChart's `const note = ""` emitted an empty <p class="month-note"> (dead margin) — removed, and renderFleetTimeSeriesChart now emits the note <p> only when the metric has one (NEW IF, flagged per rule 11: `spec.note ? <p>…</p> : ""` — renders nothing vs an empty element, no behavioral surface); (4) deleted the dead parked srcLink block in the pm-footer (built, never appended); (5) rides-chart note said "only Waymo's are loosely sourced" — stale since rides-provenance pinning; replaced per rule 8 with LATIN + TODO English recommendation in the adjacent comment (AWAITING HUMAN ENGLISH); (6) REFACTOR: fleet mixture unified onto the band-component machinery — fleetForecastCurves now converts each {median, sigma} component to its exactly-equivalent symmetric {best, lo, hi} band (lo/hi at ±1.6449σ) per call (lazily, so the qual's corrupted-weights test still trips the forecastLanes assert) and delegates to bandForecastCurves; fleetMixtureLogDensity + fleetMixtureQuantiles DELETED (one density/quantile path for all three metrics; logNormalLogDensity itself stays — the human-benchmark densities and distribution-chart qual use it). Verified equivalence: fleet medians unchanged (Waymo 5,967, robotaxi 229, hw4 exactly 600,000 — symmetric band median == parameter). NOT touched (flagged only): sanity cross-check copy "(surface-street only? location-weighted)" reads oddly but is human English; Tesla/Zoox rides histories remain self-labeled guesses (could derive Tesla's from its anchored miles at observed mi/ride). 49 quals pass.

- 2026-07-01 (later 3): Fix #11 (human-approved): Tesla's MILES_FORECAST/RIDES_FORECAST converted from single {best, lo, hi} bands to SCENARIO MIXTURES mirroring FLEET_FORECAST (same A/B/C weights 0.71/0.24/0.05, same robotaxi/hw4 scope split) — a one-humped band can't represent "71% boring / 24% aggressive / 5% HW4" and its computed median landed between scenarios (~32M for Tesla miles) instead of inside A. All three growth metrics now draw FOUR lanes (Waymo, Zoox, Tesla robotaxi solid, Tesla all-HW4-ADS dashed conditional). Code: forecastLanes(table) generalizes fleetForecastLanes (scope-splitting shared by all metrics); bandMixtureLogDensity/Quantiles = mixtures of two-piece log-normals (component bands stay the {weight, best, lo, hi} authoring idiom; quantiles via the generic quantilesFromLogDensity); bandForecastCurves parallels fleetForecastCurves; trajectoryLanes(curves, historyFn) unifies fleetTrajectoryLanes + helmerLanes (both deleted, as was splitLogNormalQuantiles); rides yMax 1e8→4e8, miles 7e8→4e9 (+1e9 tick) to fit the hw4 fork. SCENARIO BANDS (authored, derivation in code comments; calibration: 2.41M cume through May-2026 at ~440k/mo on ~50 vehicles ≈ 8.5k mi/veh-mo; ~12-13 mi/ride): miles A {8M, 5.5M, 12M} B {40M, 15M, 120M} C {400M, 100M, 1.5B}; rides A {650k, 400k, 1.1M} B {3M, 1.2M, 10M} C {15M, 2M, 120M}; Waymo/Zoox single components unchanged. DISPLAYED RESULTS: Tesla robotaxi miles median 8.98M [5.7M, 72.4M] (the fat hi is B's mass — the honest skew signal), rides 755k [420k, 6.0M]; hw4 miles 390M, rides 15.4M, both dashed. Quals: fleet-forecast.qual expects 4 curves/metric + CDF(median)~0.5 on every drawn curve + robotaxi-median-in-A regression [7M,15M] + hw4 dashed >150M + trajectory-endpoint==quantile agreement (needed dataScript+vmtRows init added to its harness); fleet-timeseries expects 4 curves/dashes on all metrics; rides-provenance monotonicity now checks the rendered lanes. trajectoryLanes asserts non-empty Tesla history (anti-Postel vs the undefined-index crash found in the qual harness). 49 quals pass; miles/rides distribution + trajectory verified live (A-hump + B-shelf bimodality visible, hw4 fork renders).

- 2026-07-01 (later 2): Fix #10 (human-approved): completed the Waymo VMT re-baseline THROUGH Dec 2025, resolving the residual flagged in the previous entry (mid-Jul-2025 read ~103M vs the 100M milestone) and the README's documented "open calibration question". KEY DISCOVERY: the old series was floor-pinned at EVERY README milestone with an exactly constant +2,823,999 cumulative offset (old cume − 2,823,999 == published − 150k pre-series at 2023-10/12, 2024-06/07/12, 2025-01/03/06/09 — all ten, to the mile), i.e. the CPUC-scaled monthly PROFILE was already right and only the level (seeded by the old early-ramp overshoot) was wrong. So the fix preserves the documented CPUC methodology: waymo 2023-11..2025-09 monthly vmt + vmt bands VERBATIM from the pre-reshape file with cume −2,823,999; 2023-02..2023-10 profile ×1.09255 (bridges the re-based early ramp to 7.14M-Oct-2023); 2025-10..12 profile ×1.06883 (bridges 127M-Sep to the exact 170.7M Dec pin, where the old series had absorbed the offset); 2026 rows byte-unchanged; kyoom bands rebuilt with knots at all milestone months (milestones are now exact CENTRALS minus 150k, no longer floors). This REPLACED fix #1's geometric C1/C2/D segments (which had missed the unused 9.3M/22M/50M/71M/96M milestones — end-2023 was 13% high) — only fix #1's A/B era (2021-07..2023-01 exponential + Feb-Oct-23 scaled-CPUC) survives, as intended. waymo-vmt-provenance.qual extended to all 13 README milestones + a mid-Jul-2025 interpolated 100M check (both red pre-fix). HEADLINE IMPACT (approved): default-window Waymo VMT 151,853,416 → 154,677,415 (+2,823,999 exactly), all Waymo MPIs ~+1.9% (safer; removes the ~1.7% conservative bias the README had predicted): all-incidents 172,332→175,537, at-fault 2,214,673→2,255,859 (still 11x), serious+ 30x→31x. README milestone-table note + Uncertainty bands bullets rewritten (floors-encoding now a historical note; era boundaries re-cut to Jul-21..Jan-23 / Feb–Nov-23). 49 quals pass; verified live (cards, full-history cumulative chart smooth across all seams).

- 2026-07-01 (later): Review pass found + fixed 9 data/analysis errors (all human-approved, one at a time; 49 quals pass; default-window headline numbers UNCHANGED by construction except the sanity severity table). (1) BIGGEST: pre-2025 Waymo VMT contradicted Waymo's own published cumulative milestones — 4.22M at Jan-2023 vs ~1M published (Waymo blog 2023-02: 1M crossed DURING Jan 2023), 9.81M at Oct-2023 vs 7.14M (arXiv 2312.12675), converging by 2025. Rewrote waymo rows 2021-07..2025-05 (scratchpad/reshape script, exponential segments between milestone pins incl. 25.3M-through-Jul-2024 [Swiss Re/Di Lillo] + 56.7M-through-Jan-2025 [arXiv 2505.01515], minus ~150k pre-series offset for pre-Jul-2021 driverless miles); cumulative at 2025-05 preserved EXACTLY (90,378,784) so every 2025-06+ row and the default window are byte-identical. NEW quals/waymo-vmt-provenance.qual pins all 7 milestones. This also resolves the 13-months-zero-incidents anomaly (2.23M claimed miles with 0 incidents would be P~e^-15; now ~4 expected). RESIDUAL FLAG: existing 2025-06/07 rows imply ~103M at Jul-15 vs the 100M milestone (~3% high) — untouched because fixing moves default-window denominators. (2) JAN-2025 SF fatality (30270-9724) had vehiclesInvolved=2 (the slurp default) but its narrative describes ~6 vehicles (SFPD: "at least two other vehicles", "the other three vehicles", 5 injured in 4 vehicles); added VEHICLES_INVOLVED override =6 in slurp.py + regenerated incidents.js. Full-history Waymo fatality mass 0.833→0.5 (Koopman fractional-death; anti-Waymo error removed). fatality-guard.qual now pins each fatality's divisor (6/3) — a new fatality trips it before silently dividing by 2. (3) RIDES_HISTORY.Waymo sat ~30-40% below published ride milestones (10M paid trips May 20 2025 [CNBC], ~20M lifetime end-2025 [Waymo year-in-review]; both were OUTSIDE the authored bands) — re-derived 2025-06..2026-05 + forecast 48M→50M; NEW quals/rides-provenance.qual pins the milestones + monotonicity. (4) First-month kyoom coherence: a helmer's first month has cume==monthly so the bands must be identical; zoox 2024-05 kyoom_max 2250→3000 (waymo's fixed by (1)); kyoom-band.qual now asserts first-row band equality. (5) Miles/rides forecast curves labeled the MODE "Median" (two-piece log-normal puts `best` at the mode; Tesla miles' drawn median is ~32M vs the labeled 8M, CDF(8M)=0.14): extracted quantilesFromLogDensity (fleetMixtureQuantiles now delegates), added splitLogNormalSigmas/Quantiles; distribution dots + trajectory forecast endpoints now show computed [q05, median, q95] — VISIBLE CHANGE: Tesla miles median dot 8M→~32M, rides ~800k→~2.8M (honest medians of the authored densities; if `best` was MEANT as the median, the density family is the thing to change instead — flag). fleet-forecast.qual asserts CDF(median)~0.5 and chart-endpoint==quantile agreement. (6) FLEET_HISTORY [AI TEXT] anchor comment aligned with its own rows (Feb-2026 3,300; May 3,750 consistent w/ the ~3,871 Jun report). (7) Documented the scope-C-counts-toward-the-market assumption in the Tesla weight fit (C ⇒ ridehailing subset alone exceeds Waymo ⇒ market YES); alternative B~0.32/A~0.63 noted in-comment if C is treated as silent. (8) Missing period after "reports more minor events" (human-approved copy fix). (9) "Unknown" severity (16 full-history, incl. in-window JAN/FEB-2026) counted as "Property damage only" in BOTH sanity tables: SEVERITY_INFO.Unknown gains unk flag, derived UNKNOWN_SEVERITIES excludes it from propDmg; severity-breakdown gains an "Unknown" column (English header per the human's standing sanity-section-is-English decision; matches the passenger table's existing "Unknown" header) so buckets partition Total; sanity-values.qual updated (6-col arithmetic + column-present + Total idx). Verified live: full-history cumulative chart, rides/miles charts, both sanity tables, no console errors.

- 2026-07-01: De-vibe-coding pass on style.css ONLY (no copy, no DOM, no JS, no chart-internal tuning touched). (1) Added :root design tokens (--ink/-soft/-faint, --accent/-ink/-wash, --line/-strong, --wash/-deep, --safer/--worse/--iffy, --mono) and consolidated every scattered value onto them: 8 text grays -> 3, 5 blues -> 1 accent family, 2 green/red verdict pairs -> 1 (pm-card-odds/pm-dot now share the stress-badge trio), pm-* Stripe-derived palette (#556cd6/#3c4257/#697386/#8792a2/#e3e8ee) -> tokens, tan source-table theme (#eee3c0/#bba/#7a001f) -> the neutral table style. NOTE: the tan matched the deleted .ai-explainer look — if it was a deliberate "sources" theme, revert that hunk. (2) Type scale made deliberate: h1 2rem->1.6rem tighter tracking, h2 1.5rem->1.2rem with 1px (was 2px) hairline, h3 pinned 1rem; global `a { color: var(--accent) }` (links were default browser blue). (3) Abstract callout: blue-left-bar -> plain 1px bordered wash card. (4) .mpi-cards: 6-across flex -> grid minmax(280px,1fr), i.e. 3/row at 960px = humans row + AVs row (was 6 cramped ~140px columns); media-query column override deleted (grid handles it). (5) Deleted dead CSS: .ai-explainer block, .ci-bar-*/"Rate estimator" block, .month-tri (zero refs in js/html/quals/README). (6) th gains font-weight:600 + wash bg; filters buttons get focus-visible ring. PRESERVED verbatim: [data-tip] rule + comment, .table-scroll/th sticky (table-scroll.qual), all human comments, chart-internal colors (#95a0b8 axis, #334 labels, hand-tuned opacities), helmer colors in JS. Verified: 47 quals pass; desktop+mobile playwright screenshots of every section, no console/page errors. STILL TODO (carried): (c) full-coverage line-by-line review of the UI/rendering code.

- 2026-06-28: (a) DONE — firmed the AV-cities human bands for injury/airbag/serious-injury+ from Kusano & Scanlon 56.7M, source-first. (Read it via curl + a locally-installed pypdf into scratchpad/pylibs; arxiv e-print is PDF-only, no LaTeX source; arxiv/html + ar5iv + tandfonline all unavailable.) Replaced the hand-set widths with the paper's published PER-CITY benchmark range, geometrically centered on the mileage-blended value: injury 256k-524k -> 125k-478k (SF 8.02 to Phoenix 2.09 IPMM, blended 4.04); airbag 500k-700k -> 433k-704k (1.42-2.31, blended 1.69); serious+ 3.3M-6.7M -> 2.17M-8.33M (0.12-0.46, blended 0.24). Verified the paper's blended values: airbag/SSI+ match our prior 1.68/0.23; injury updated 3.91->4.04. Band SEMANTIC changed from underreporting (observed<->Blincoe) to location (Phoenix<->SF) for these three — the larger, directly-sourced uncertainty (the central already bakes in Blincoe); all-crashes still uses the underreporting synthesis; fatality unchanged (fleet deaths/VMT, Koopman/Piper). NEW quals/human-benchmark-provenance.qual pins each edge to 1e6/per-city-IPMM (1% tol) + asserts the geomean central is within 15% of Kusano's blended value. Updated the monthly-series SSI range check and the README methodology + band-interpretation. IMPACT (Waymo AV-cities multiples): injury ~4.4x -> 6.6x, airbag 7.5x, serious+ 12.2x (human centrals now match Kusano's blended; our Waymo numerator is safer than Kusano's ADS rate so multiples exceed Piper's 5x). 43 quals pass. STILL TODO: (c) full-coverage line-by-line review of the UI/rendering code.

- 2026-06-27 (later 2): Fatality metric — investigated, briefly changed, then REVERTED; net no change, original method confirmed correct. I first removed ÷vehiclesInvolved and moved the human benchmark to a crashed-vehicle basis, thinking ÷vehicles was an analog-less error. On RE-READING Piper's article that was WRONG: ÷vehiclesInvolved IS Koopman's fractional-death method, which Piper endorses, and it correctly matches the human FLEET deaths-per-VMT benchmark (each death counted once across all vehicles' miles; most fatal crashes are multi-vehicle, so a 2-vehicle fatal crash counts 0.5 on the AV's account, a 3-vehicle 0.33). Piper's own ~3x fatality figure = the original code (~2.93x); my "fixed" 1.52x was NOT Piper's. RESTORED ÷vehiclesInvolved + the fleet deaths/VMT human bands (HumansAV 87-130M, Rideshare 106-161M, HumansUS 61-83M), and replaced the old vague comment ("match fleet-wide fatality methodology") with the explicit Koopman/Piper rationale. Two valid framings exist (fleet-deaths [Piper] vs per-vehicle-involvement); we use Piper's. LESSON: don't "fix" a documented methodology choice without reading its cited source first. KEY CONTEXT: Piper/Waymo have NO standalone fatality metric; their severe-outcome metric is serious-or-fatal (SSI+ = 0.23 IPMM), which is our seriousInjury metric (already correct, already includes fatalities). The standalone fatality metric is our supplement; the SSI+ comparison is the true Piper-faithful severe one. NOTE: human fatality bands are inherently soft (k=2, basis conversions) — refineable with direct urban vehicles-in-fatal-crashes data. DECIDED (with human): do NOT collapse the injury/airbag/serious human bands to Piper points — bands are principled (under-reporting + threshold + cross-study spread are real systematic uncertainty on the human number; the Clopper-Pearson sampling CI is the small AV-side slice we already show). 42 quals pass. STILL TODO: (a) firm up the injury/airbag/serious band widths from the multi-study spread (CRSS/SHRP2/IIHS/Blincoe); (c) full-coverage line-by-line review of the UI/rendering code.

- 2026-06-27 (later): Stakes-driven review pass of the correctness-critical code. FOUND + FIXED a passenger-presence silent-misclassification (same class as severity/airbag): the SGO "Were All Passengers Belted?" field (`belted`) has TWO no-passenger encodings — "No Passengers in Vehicle" (485) and "Subject Vehicle - No Passenger In Vehicle" (436) — but the classifier recognized only the second, so 485 no-passenger incidents were counted as "with passenger". FIX: explicit PAX_NONE/PAX_PRESENT/PAX_UNKNOWN sets in crashla.js (withPax/noPax now set-membership; unk = remainder) + new quals/passenger-classification.qual (coverage: every belted value classified; disjoint; regression-pins both no-passenger strings). Verified live (Waymo in-window noPax ~0→421). Also added quals/stationarity-consistency.qual: speed and svMovement are independent stationarity signals that legitimately differ (instantaneous speed vs maneuver category); speed==0 is authoritative; the qual is a DRIFT MONITOR (disagreement 23/1836=1.3%, fails if >3%) — no metric change. REVIEWED + judged SOUND: the statistical core (lgamma/gammainc/gammaquant/estimateMpi/CI inversion — scipy-pinned by gamma-math.qual), VMT parsing (every band ordering asserted), speedBinForIncident (total), the metric accumulation (uses the fixed severity sets + airbagAny), slurp filter/dedup (Operator=None + Tesla In-Vehicle, dedup by Same Incident ID highest version — matches documented scope), and the full SV/CP archive-column audit (movement + contact areas use both; airbag fixed; tow is the only half-read pair and it's unsurfaced/latent; injury severity is a single crash-level column = any-party, correct). FLAGGED FOR HUMAN (not changed): fatality metric divides by vehiclesInvolved (reports 0.83 vs 2 whole fatalities ≈ 42%); needs confirmation it's apples-to-apples with the human FARS/rideshare benchmark, which counts WHOLE fatalities — if not, it understates AV fatality rate ~2.4x (conclusion holds either way given k=2's huge CI, but it's the highest-stakes point estimate). Also latent: vehiclesInvolved DEFAULTS to 2 for all incidents (fine now — only the 2 overridden fatalities use it — but a future single-vehicle fatality would be halved). 42 quals pass. NOTE: not a literal every-line read — focused on the paths that determine the safety numbers; UI/rendering not yet swept.

- 2026-06-27: FIXED the airbag undercount (the real bug behind later-3's anomaly). airbagAny is DEFINED as any-vehicle, but slurp's `_normalize_archive_row` copied only the archive's "SV Any Air Bags Deployed?" → "Any Air Bags Deployed?" and DROPPED "CP Any Air Bags Deployed?". The current CSV's single "Any Air Bags Deployed?" is already any-vehicle, so the bug was archive-only. Confirmed via exact (reportId, version) match: 19 archive Waymo crashes had CP airbag=Yes, SV=No — counted as no-airbag. FIX: `_normalize_archive_row` now ORs in the CP column. Patched data/incidents.js airbagAny false→true for those 19 (the JSON round-trip was byte-identical except the 19 flips; equals what the fixed slurp produces, so no network re-slurp needed). IMPACT: Waymo airbag 44→63, 0.17→0.24 IPMM (0.6x→0.8x of Waymo's published 0.30 — now genuinely comparable). Restored airbag to the Waymo cross-check + WAYMO_PUBLISHED_IPMM (0.30) + waymo-reconciliation.qual ([0.5,1.8]). De-magicked monthly-series.qual's airbag assertion (was a hardcoded 30–45 range) to recompute from INCIDENT_DATA. CORRECTION: later-3's "CP-only=0 / CP adds nothing" was a loose-match script bug; the exact (reportId,version) match is authoritative (19 cases). 40 quals pass.

- 2026-06-26 (later 3): Followed up the airbag cross-check anomaly (later-2 flagged 0.17 vs Waymo's 0.30). DIAGNOSIS: our `airbagAny` is the SUBJECT (Waymo) vehicle's airbag ONLY — slurp.py maps the SGO "SV Any Air Bags Deployed?" column (renamed to "Any Air Bags Deployed?") and DROPS the archive's "CP Any Air Bags Deployed?" column. So ours matches neither Waymo's "any vehicle" 0.30 (we miss the other car's airbag → we look low) nor cleanly its "Waymo vehicle" 0.06 (we're ~2.8x high, likely scope: all-roads/all-driverless/all-severity SGO airbag incl. minor deployments vs Waymo's surface-street location-weighted). Schema is also inconsistent (archive has SV+CP columns; the current CSV has a single ambiguous "Any Air Bags Deployed?"), so a clean cross-timeline "any vehicle" airbag isn't currently computable. ACTION: removed airbag from the Waymo cross-check + waymo-reconciliation.qual (keeps injury [0.6,1.6] + serious+ [0.3,4.5]); documented in WAYMO_PUBLISHED_IPMM. FOLLOW-UP (not done): to get true "any vehicle," re-slurp ORing SV+CP from the archive and resolve the current column's semantics; or relabel our metric "Waymo-vehicle airbag" for precision. Also: the cross-check sanity copy is ENGLISH per the human (he reverted an LLM Latinization) — rule 8's prescribed path is Latin+TODO, but for this sanity section he wants concise English; ASK before deviating from rule 8 elsewhere, don't deviate-then-flag.

- 2026-06-26 (later 2): (a) Tooltip/hover cleanup — replaced scattered inline cursor/pointer-events on chart dots with ONE CSS rule `[data-tip] { cursor: help; pointer-events: all; }`. Fixes hollow (fill:none) dots being hittable only on their ~1.5px stroke ring (pointer-events:all → full-disc hit area) and makes the cursor consistent on every tooltip target. Removed the now-redundant inline styles from the MPI/CI hit circles (CSS is the single source). Verified in headless chromium: all target types compute cursor:help + pointer-events:all, and a hollow dot's center now lands on a circle[data-tip] (was null/ring-only). (b) Metric labels: "Serious injury (SSI+)" → "Serious injury+", "Hospitalization" → "Hospitalization+" (the two nested "or-worse" tiers; trailing + = this tier or more severe, so it's clear they include fatalities). "All incidents"/"Injury"/"Airbag"/"Fatality" unchanged. monthly-series.qual label assertion updated. (c) New "Waymo published-rate cross-check" sanity subsection + quals/waymo-reconciliation.qual: full-history Waymo SGO rates vs Waymo's OWN published IPMM (new const WAYMO_PUBLISHED_IPMM = injury 0.71 / airbag 0.30 / ssi 0.01). Current: any-injury 0.62 (0.9x), airbag 0.17 (0.6x — we UNDERcount airbag vs Waymo's own figure; worth investigating the airbagAny field), serious+ 0.019 (1.9x, partly 0.01 rounding). Qual bounds are per-metric (injury tight [0.6,1.6] — WOULD have caught the silent-drop bug at 0.56x; airbag/ssi looser for scope + rounding). Cross-check copy is English (rule-8 deviation matching the all-English sanity section) — flag for Latinization. (d) CORRECTION to the earlier (later) entry's claim: Waymo's surface-street scope is NOT a material driver of our 5-vs-~3 serious-injury count gap. All 5 of our Waymo SSI+ incidents are surface-street (Intersection/Street); only 9 of 1783 Waymo incidents are Highway/Freeway, none serious/fatal. The gap is severity ADJUDICATION (SGO self-reported "Serious" vs Waymo's stricter post-hoc KABCO-A determination) plus mile-base/timing/rounding — not highway exclusion. 40 quals pass.

- 2026-06-26 (later): FIXED a scary silent-drop bug class in severity classification (surfaced by a human noticing Waymo serious-injury MPI ~40M vs Waymo's implied ~73.5M). Root cause: three hand-maintained parallel Sets (INJURY_SEVERITIES, HOSPITALIZATION_SEVERITIES, SERIOUS_INJURY_SEVERITIES) plus SEVERITY_RANK, with no coverage guard — so a severity present in the data but omitted from a Set silently counted as no-injury. Omissions found: bare "Minor" (55, the older 2023 NHTSA encoding) AND "Serious" (3) were missing from INJURY_SEVERITIES; "Serious" was missing from SERIOUS_INJURY_SEVERITIES (which instead used the softer "Moderate W/ Hospitalization" proxy); "Unknown" (16) was missing from SEVERITY_RANK (sorted via a ?? -1 fallback). Net: 58 real injuries hidden from the injury metric. FIX (crashla.js): replaced the parallel Sets with a single source of truth SEVERITY_INFO (rank/injury/hosp/ssi/fatal flags per severity); the four downstream structures are now DERIVED from it (DRY, can't drift). Redefined SSI+ to KABCO A+K = {"Serious","Fatality"} (dropped the "Moderate W/ Hospitalization" proxy); added "Serious" to injury+hosp+ssi, bare "Minor" to injury, "Unknown" to rank. GUARDS: (a) per-incident runtime assert that every severity is in SEVERITY_INFO; (b) new quals/severity-classification.qual.mjs — coverage (every INCIDENT_DATA severity classified), nesting (fatal⊆ssi⊆hosp⊆injury), rank-monotonicity (injury⟺rank≥1, ssi⟺rank≥5, the invariant that directly catches this bug), and Minor/Serious regressions; (c) slurp.py EXPECTED_SEVERITIES Anti-Postel guard so a new NHTSA severity string crashes data-build (verified complete against all snapshots). IMPACT (full-history Waymo): serious-injury MPI 43.4M→52.0M (toward Waymo's 73.5M; residual = SGO self-report vs Waymo post-hoc determination + all-roads vs surface-street + Bayesian shrinkage); any-injury rate 0.40→0.62 IPMM, now close to Waymo's OWN reported 0.71 (we'd been undercounting injuries vs Waymo's own figure — independent confirmation the dropped crashes were real); injury count 105→161. Default window (Jun 2025+) is ~unchanged: all bare-"Minor"/"Serious" predate it. 39 quals pass (was 38).

- 2026-06-26: Waymo Safety Impact advanced to 220.6M rider-only miles through Mar 2026 (Jun 24, 2026 update; Atlanta added as a 5th geography; per-city PHX 80.6M/SF 67.1M/LA 51.8M/ATX 15.8M/ATL 5.4M). (a) Recalibrated Waymo VMT Oct 2025–May 2026 in data/vmt.csv + data/vmt.js: pinned cumulative to the published anchors (170.7M end-Dec-2025, 220.6M end-Mar-2026), reshaped Oct–Dec down ~10%, kept the Jan–Mar growth (the 170.7→220.6 interval ≈ the prior 50M), re-extrapolated Apr–May from the new base; the pre-recalibration estimate was 225.2M through Mar (~2% high — methodology validated). (b) crashla.js human benchmarks refreshed to the new page's IPMMs: any-injury 3.90→3.91, airbag 1.63→1.68, SSI+ 0.22→0.23 (hospitalization HumansAV endpoints recomputed 613k/4545k→595k/4348k; injury/airbag/SSI bands stay within margin); six "(170.7M mi)" source labels → "(220.6M mi)". (c) Zoox vmt rationales: "paid LV ~late Jun 2026" → "blocked pending FMVSS exemption; no date" (still free as of Jun 2026 — FMVSS bars charging for a vehicle with no steering wheel/pedals; denominator unchanged, free miles still count). (d) README: added the 220.6M milestone row + per-city geographic breakdown + a Jun-2026 Waymo freeway-construction recall (~3,900 vehicles) note; refreshed the Piper-comparison IPMMs. (e) Deleted orphan vmt-comparison.html (unlinked; stale 127M/73M figures). The "current live Waymo page" reference values in the notes/plan below are updated to 220.6M/3.91/1.68/0.23 (the 170.7M-era figures are now historical). 38 quals pass.

- 2026-06-20 (later): MPI-over-time line now BREAKS at k=0 months again (condition: mpi===null || incidentCount===0) — my median-unify change had made it connect through prior-only months; restored the original deliberate break so a k=0 month shows only its hollow median dot, not a fake trend segment. Verified live via playwright (reinstalled playwright-core --no-save; chromium_headless_shell-1208 cached; /tmp/shot_verify.mjs serves localhost:8642 + screenshots #chart-distributions/#chart-mpi-all): Zoox at-fault = scattered hollow dots no line, Waymo = solid, Tesla = line only across its data months. Distribution chart also visually confirmed: 1% axis readable (~100K-30M not billions), mode+median dots split for skewed Zoox, k=0 at-fault-injury (Tesla/Zoox) dashed+hollow. 38 pass.

- 2026-06-20: Point estimate UNIFIED on the posterior median app-wide. Cards/stress/multiplier switched from mpiPoint(est.median=MLE) to fmtWhole(est.postMedian); multiplier no longer has the k=0 "≥ Nx" branch. MPI-over-time chart: added mpiMedian (1/gammaquant(a, vmtBest, .5)) to per-month mpiByMetric (+ humans' = geo); line/dots/tooltip/yMax now use it instead of mpiBest (MLE). k=0 months render HOLLOW median dots (renderDot gained a hollow arg) instead of up-arrows — matches the distribution chart. Deleted now-dead mpiPoint() and renderUpArrow(). mpiBest kept only for the human subset-chain assert. monthly-series qual rewritten (was: all-k=0 -> up-arrows pinned at ceiling; now: hollow median dots on-scale, non-degenerate CI whiskers). No more "≥ lo" form anywhere. 38 pass. Pending #1 (point-estimate split) RESOLVED.

- 2026-06-19 (later 8): k=0 (no-event, prior-only) distribution curves de-emphasised — faded fill (opacity .04 vs .12), dashed stroke (stroke-dasharray 6 4), hollow dots (fill:none;stroke:color) — so e.g. Tesla/Zoox at-fault-injury (both k=0, near-equal VMT) don't read as a real overlap. Triggered on est.k===0 (humans k=null unaffected). Qual section 8: fatality render has 1 dashed stroke + 2 hollow dots per k=0 curve. 38 pass. (Can't eyeball — playwright still uninstalled.)

- 2026-06-19 (later 7): Distribution chart now draws TWO markers per curve — visual peak ("Most likely") + posterior median — each tooltip labeled with which it is, plus the other central values (mode/median/mean/MLE), where mean=vmtBest/(k-0.5) and MLE=vmtBest/k both render "∞" for k<=0.5 (the near-zero-data curves where they diverge; that fanning IS the signal). None of {mean,median,mode} equals the MLE. Helmer name dropped from both the distribution markers and the MPI-over-time tooltip (dot colour + legend identify the series). NOTE (not a bug): Tesla & Zoox at-fault-injury curves look coincident because both are k=0 (same alpha=0.5 shape) with coincidentally near-equal default-window VMT (2.027M vs 2.020M). Qual updates: legend-count now 2 markers/curve; marker section asserts 2 labeled dots, no helmer, no "≥", median dot at mapX(postMedian). 38 pass.

- 2026-06-19 (later 6): Distribution chart — visibility-floor x-axis + posterior-median dot. (1) For k<0.5 (alpha<1) at-fault posteriors the tail ~ x^(-alpha) decays so slowly that EVERY intrinsic right-edge explodes (99.9% quantile 356B, even the 95% CI 1.67B in the default Zoox view), squishing all other curves into the left third. New distributionExtent(curves) helper sets the x-range to where some curve clears DIST_VIS_FLOOR (=1%, ~2px) of the TALLEST curve's peak -- a visibility floor, robust to heavy tails because a short/uncertain curve can't lower the tallest peak (yMax is a max), so its thin tail gets cut where it's a sliver. Default at-fault axis 6.7 -> 4.0 decades. Helper is shared by the marker qual (DRY). (2) The dot was at the MLE (est.median = vmtBest/k): infinite at k=0, so mpiPoint parked it at the far-left lo -- which, once the extent tightened, fell OFF-frame (the marker qual caught it). Added est.postMedian (ADS: 1/gammaquant(alpha, vmtBest, .5); human: geo) and the dot + tooltip now use it. Posterior median is finite at k=0, inside [lo,hi], and parameterization-invariant; it = mode for well-determined curves and sits right of the mode for skewed near-zero-data ones (honest skew signal), unlike the MLE which flew to +/-inf. marker qual updated: dot cx == mapX(postMedian), finite + within CI, no "≥" form. 38 quals pass. LOOSE END: the CARDS / stress display / multiplier still use the MLE (mpiPoint, "≥ lo" at k=0) -- chart now shows the median, cards show the MLE; unify app-wide to the median if we want one number everywhere.

- 2026-06-19 (later 5): Hardened against the silent-drop class of bug. monthSeriesData() now asserts (crashla.js, the `vmt === undefined` branch) that a helmer with no VMT for a month also has NO incidents that month -- an orphan incident (numerator with no denominator) fails loudly instead of vanishing. Complements the existing line-578 assert (incident month must be in the global VMT month set); together they guarantee every in-scope incident has a denominator. vmt-master-fidelity.qual gained: (a) a data-level orphan scan (every INCIDENT_DATA helmer|month must be a VMT key; currently 0 orphans across all 1836), and (b) a guard-verification that injects a synthetic Zoox 2021-09 incident (a Waymo-only month) and asserts monthSeriesData throws /orphan/. Verified all helmers clean: Tesla inc-from 2025-07 vs VMT-from 2025-06, Waymo 2021-09 vs 2021-07, Zoox 2024-05 vs 2024-05. 38 quals pass.

- 2026-06-19 (later 4): Zoox VMT-coverage gap RESOLVED (the flag two entries down). NHTSA SGO has Zoox driverless incidents back to 2024-05 (6 of them: Foster City + SF Explorer + the 2025-04 Las Vegas 0.4-fault one), but our VMT series started 2025-06, so those 6 were silently dropped (helmers.Zoox=null, no denominator). Checked the narratives: all "Zoox vehicle in autonomy"/"unoccupied Zoox", one literally "waiting for the next ride" -> same purpose-built robotaxi ride-hailing scope, not a different regime. Fix (pure data, no code/filter -- the anti-magic win): rewrote the whole Zoox series as one continuous 2024-05 -> 2026-05 ramp (25 months), miles DISTRIBUTED across the real op history and anchored to the SAME cumulative milestones (~1M Dec 2025 = 1.001M, ~2M late-Mar 2026 = 1.861M, 2.451M May 2026). Because the milestones are fixed totals, distributing ~178K into the pre-June-2025 early period means the recent months come DOWN ~6% (we'd been cramming the whole ~1M into Jun-Dec 2025). EARLY RAMP IS SOURCED (timeline, not magnitude): Foster City HQ driverless testing from 2024-05 (first SGO incident), SF driverless (SoMa/Design District) from Nov 2024, LV into 2025; 1M crossed ~early Dec 2025 (confirms the anchor). Magnitude stays near-blind -- CA DMV's ~1.2M CA miles (Dec2024-Nov2025) are drivered-dominated with no public driverless split, and Zoox's CPUC Driverless-Pilot mileage is redacted -- hence the wide 0.5-2x early bands. Cross-check: 6 early incidents / 178K mi = ~30K mi/incident vs recent ~78K, i.e. early rate ~2.6x (plausible for immature dense-SF ops). Bands: 0.5x-2x on the pre-milestone early months (near-blind), 0.7x-1.3x on the anchored recent ones; kyoom = running cumulative sums of the monthly bands (monotonic, brackets central, == running sum so the kyoom-band qual's "no wider than running sum" holds with equality). Verified: all 6 early months now COVERED; full-history Zoox at-fault k=0.1 -> 0.5 (the 0.4 early NV incident now counted); DEFAULT-window (2025-06+) Zoox at-fault vmtBest 2.14M -> 2.02M (-6%, bell slides left ~6%, honest direction), k stays 0.1 (early still out of the default window). 38 quals pass (no qual hardcoded the old Zoox VMT). data/vmt.csv + data/vmt.js both regenerated; non-zoox rows byte-unchanged.

- 2026-06-19 (later 3): Distribution-chart peak-dot fix + qual hardening. (1) The dot was drawn at the curve's MODE (peakX) but its tooltip reported the MLE point estimate (mpiPoint(est.median,...)); for right-skewed low-k bells these diverge (dot sat left of its number). Fixed: dot now at markerX = Number.isFinite(est.median) ? est.median : est.lo -- the SAME finite/∞ split mpiPoint uses -- so the dot coincides with the value in its tooltip and with the cards (all MLE-convention). Visible consequence: for sparse-fault curves the dot now sits out in the right tail (e.g. Zoox at-fault MLE = vmtBest/0.1 ≈ 21M), not on the visual peak -- honest (signals a flimsy estimate) but a deliberate look-change; flag if dot-on-peak was preferred. peakX/peakY still computed for y-axis scaling. (2) New qual section 7: replicates the chart's mapX and asserts every dot's cx == mapX(stated MPI) within 0.05px (atfault = skewed finite-median, fatality = k=0/∞ -> "≥ lo"), all dots in-frame, and the Zoox at-fault dot is >10px right of the mode (proves the fix). (3) FOUND + fixed a latent qual bug: distribution-chart.qual's ElementStub set textContent as a plain property, so escHtml (which round-trips textContent->innerHTML) returned "" -> every data-tip rendered EMPTY in that harness, so all prior tooltip checks were vacuous (only tested the attribute existed). Added the escaping setter (matching vmt-master-fidelity's stub); tooltip checks now test real content. 38 quals pass.

- 2026-06-19 (later): (a) Distribution bell now marginalizes over the VMT band. New helper marginalMpiLogDensity (crashla.js, by invGammaLogDensity) integrates InvGamma(alpha, VMT) against a log-NORMAL VMT prior (median vmtBest, [vmtMin,vmtMax] as its 95% interval) via 81-node Simpson over ±4 sigma; monthlySummaryRows densityFn switched from point-estimate InvGamma(alpha, vmtBest) to it. Effect is helmer-dependent: tiny for data-poor Zoox (alpha dominates) but LARGE for data-rich Waymo, whose "true MPI" uncertainty is VMT-dominated -- its bell legitimately widens to ~the VMT band. TWO bugs caught + fixed during this: (1) a log-UNIFORM prior (first attempt) produced a flat-topped "mesa" for Waymo (narrow sampling bell smeared across the prior's hard flat top) -> switched to log-normal; (2) xMin/xMax were left at the narrow vmtBest sampling extent, CLIPPING the now-wider marginal -> changed to bracket the marginal via the VMT band extremes (1/gammaquant(alpha, vmtMin, .999) .. 1/gammaquant(alpha, vmtMax, .001)). Verified: all helmer curves unimodal, peaked (nearPeak ~12/400, was 54), edge/peak ~0.002-0.03 (no clip). distribution-chart.qual gained: marginal integrates to ~1, MORE log-variance than the point curve, reduces exactly to invGammaLogDensity when vmtMin==vmtMax, and a MESA-regression guard (large-alpha narrow bell over a wide band must stay peaked, center > 1.1x shoulders), plus an ALL-COMBOS health check iterating every helmer×metric marginal (normalized within 2% + unimodal over its plotted extent). PERF: first cut recomputed lgamma + node grid on every one of ~250 per-curve evals = 148ms/frame (would jank the slider); refactored to makeMarginalMpiDensity(alpha,band) which hoists lgamma/nodes/prior-weights into a closure built once per curve, leaving one exp per node in the hot loop -> ~13ms/frame. Node count 81 is driven by SMOOTHNESS not point-accuracy: 61 nodes is accurate (<0.6% vs a 4001-node ref) but leaves sub-pixel bumps in the highest-alpha curve (Waymo all) as the sampling spike drifts between nodes -> non-unimodal; the all-combos qual catches it. Independently cross-checked the density formula by Monte Carlo (<0.4%). 38 quals pass. (b) NOT done -- left as the conservative double-extreme CI pending the faultfrac-uncertainty-model decision (tightening it would flip Zoox at-fault to "robustly safer" on ~zero data; see chat).
- CORRECTION to the entry below: the IN-WINDOW Zoox at-fault count is k=0.1 (alpha=0.6), NOT the k=0.5/alpha=1.0 quoted there. The 0.5 raw-CSV sum includes incident 30610-10473 (fault 0.4, APR-2025, Nevada) which is OUTSIDE the Zoox VMT window (starts 2025-06), so the app silently drops it (helmers.Zoox=null for 2025-04, no denominator). Only the single 0.1 incident (30610-14664, MAR-2026, CA) is in-window. So the at-fault CI/exponential figures in the entry below were illustrative at the wrong alpha; the reband itself (denominator) is independent of k and stands. FLAG: a real driverless at-fault incident with no VMT month is a numerator-without-denominator gap (mildly anti-Postel: silently dropped) -- either extend Zoox VMT back to APR-2025 (Zoox demonstrably operated driverlessly in NV then) or filter the incident out for scope consistency. Bears on the faultfrac-uncertainty model (in-window Zoox at-fault is literally one 0.1 judgment).
- 2026-06-19: Zoox VMT error bands tightened 0.5x–2x -> 0.7x–1.3x (all 12 zoox rows in data/vmt.csv + data/vmt.js; kyoom_min/kyoom_max/vmt_min/vmt_max recomputed at 0.7x/1.3x of cume/vmt; rationales updated; central vmt + cumulative UNCHANGED). Justified by two verified, multiply-corroborated company milestones: ~1M cumulative autonomous miles by late 2025 (>300k riders) and ~2M by late Mar 2026 (>350k riders) — both bracket our central line within ~10%, decisively refuting the old 2x upper. Verification killed the AI-research's CA-DMV row: it claimed "1.21M drivered + 20 disengagements" (≈61k mi/disengagement); autopilotwatch's DMV aggregation shows ~1.2M mi / 800 disengagements (~1,500 mi/disengagement), so the 20-disengagement figure is fabricated; and drivered/CA-DMV testing miles are out-of-SGO-scope regardless, so that anchor is excluded. Residual 0.7/1.3 band = monthly-allocation + scope ambiguity (company "autonomous miles" vs SGO public-road driverless; one-directional, could inflate denominator) + milestone rounding; kept uniform across months (KISS) rather than per-regime. KEY: this narrows only the numeric 95% CI (Zoox at-fault CI 285K–166M -> 398K–108M) and the time-series fan; the DISTRIBUTION BELL is unchanged because it uses vmtBest only and its width is set by the at-fault count k=0.5 (alpha=1.0), not VMT. README sources (2 passages) updated. 38 quals pass (vmt-master-fidelity confirms csv↔js consistency).
- 2026-06-16 (later): Prediction markets refreshed + file renamed. (1) Dropped the indented multi-outcome Polymarket events (Waymo-cities-by-June-30 cluster) and the near-resolved June-30/July-1 markets (Tesla-CA-by-June-30, Waymo-fatal-by-July-1). (2) Added two strong binary markets: "Waymo reaches 2 billion miles with three or fewer at-fault fatalities?" (on-theme safety, Manifold, ~0.75) and "Will Tesla have more autonomous vehicles providing ridehailing than Waymo on Jan 2nd 2027" (~0.30). Final set = 1 Polymarket (Musk-out-before-2027) + 6 Manifold; all binary, no near-term resolutions (next ~Sep 2026). Re-fetched all prices. (3) RENAMED data/polymarket.js -> data/predmarkets.js (it holds Manifold too). Umbrella symbols renamed for consistency: POLYMARKET_SNAPSHOT_DATE->PREDMARKET_SNAPSHOT_DATE, renderPolymarketPanel->renderPredmarketsPanel, refreshPolymarket->refreshPredmarkets, loadPolymarketData->loadPredmarketData, polymarketAgeTimer->predmarketsAgeTimer, DOM id polymarket-panel->predmarket-panel, CSS .polymarket-{panel,grid,loading,error}->.predmarket-*. KEPT source-specific names: POLYMARKET_SNAPSHOT, MANIFOLD_SNAPSHOT, polymarketUrl, fetchPolymarketEvent, renderPolymarketCard, fetchManifoldMarket, and the pm-* card classes. (4) LOOSE END flagged: no market now sets multi:true, so the multi-outcome/subcard rendering branch in renderPredmarketsPanel (+ pm-subcard CSS + fetchPolymarketEvent multi handling + the header-comment instructions) is currently unexercised — kept as an available capability for future multi-outcome Polymarket markets; remove if you want it gone.
- 2026-06-16 (later): CSS consistency — flattened the .month-panel frame (removed border/radius/padding/#fafafa background, and the .date-filtered blue border/background cue + its responsive padding override) so the 5 dashboard sections are no longer boxed; now all 8 collapsible sections render uniformly (just the h2 underline header). The date-filtered cue survives as the .date-range-label blue pill (not a frame). 36 quals pass.

- 2026-06-16 (later): Added third human cohort HumansRideshare "Humans (Uber/Lyft)" (#cc7a00, default OFF) — the rider's typical AV alternative. Data reality: the ONLY published rideshare per-mile crash rate is fatalities (Uber 0.62→0.87, Lyft 0.74→0.94 per 100M VMT, 2019–2022 safety reports → 106M–161M MPI, set explicitly on the fatality metric; lands just safer than HumansAV's 87–130M). CPUC TNC collision data is redacted (Footnote 42), so no all-crash/injury/at-fault rideshare rate exists. CORRECTED after human pushback ("uber/lyft should be DIFFERENT than humans-av-cities"): NOT a clone of AV-cities. The loop after METRIC_DEFS derives HumansRideshare for the general crash metrics (those that also carry a HumansUS band, so hospitalization/airbag/serious-injury are omitted — same coverage as HumansUS) by LEANING the AV-cities band safer + wider: lo = AV.lo/1.2 (up to 1.2x worse: urban exposure + in-app distraction), hi = AV.hi*1.5 (up to 1.5x safer: sober/professional self-selection seen in the fatality data), rounded to 2 sig figs (sig2 helper). Net: distinct band, center ~1.1x safer than AV-cities, visibly wider (e.g. at-fault 86K-650K vs AV 103K-430K). Fatality stays the sourced rideshare band (106-161M). The lean factors (1.2/1.5) are judgment calls within the confidence-band license; direction (safer) is grounded in the fatality data + driver self-selection but extrapolation to non-fatal crashes is uncertain — flag if the human wants a different lean/width. New user-facing src/derivation text written in English (matching all existing src strings) with TODO English-recommendation comments rather than Latin — flagged as a rule-8 deviation for human review. Quals: subset-chain cohort count now HUMAN_HELMERS.length + asserts rideshare fatality is distinct, general bands are strictly WIDER than AV (lo lower, hi higher), and severity-tail metrics omitted; default-state/url-state expected toggle objects gained HumansRideshare:false. Also added the human's new Manifold market "Waymo in Portland in 2026?" (slug waymo-in-portland-in-2026, p~0.70) to MANIFOLD_SNAPSHOT — it was the only quality Waymo-Portland market (the pre-existing Manifold one was a 4-bettor joke; Polymarket had none). 36 quals pass; verified live (toggle, distinct fatality curve, 10 benchmark rows, Portland card).

- 2026-06-16 (later): Split the month-panel dashboard into 5 collapsible sections (joining the existing 3), so SECTION_IDS = controls, mpi, dist, summary, vmt, browser, markets, sanity (8 total; collapsed set still in URL key `x`). Section bodies: controls="What to compare" (helmer checkboxes + metric radios + date-range slider), mpi="Miles per [...] over time" (ci-fan legend + chart-mpi-all), dist=the distribution title (chart-distributions), summary="Summary statistics" (cards), vmt="Vehicle Miles Traveled" (per-helmer charts + line/speed legends). New copy ("What to compare"/"Summary statistics"/"Vehicle Miles Traveled") came verbatim from the human, so English not Latin (rule 8 spec exception). KEY MECHANIC: a section header must stay visible when collapsed, so it can't live inside the re-rendered chart container; the two dynamic chart titles were moved OUT of renderAllHelmersMpiChart / renderDistributionChart (they no longer emit `<h3>`) into stable `<h2 class="sec-head" id="mpi-heading|dist-heading">` whose textContent renderWindowedViews sets each render (same pattern as incident-browser-heading; exact title strings preserved). Per-helmer Tesla/Waymo/Zoox h3s stay inside the VMT section body. DECISION TO FLAG: the date-range slider lives inside "What to compare", so collapsing that section also hides the slider (coherent "hide all controls", but means a shared URL with controls collapsed can't be re-windowed without expanding). Qual churn: monthly-series + distribution-chart title checks now read the #mpi-heading/#dist-heading section headers (chart bodies have no h3); stress-test distribution assertion checks no in-body h3 + title in #dist-heading; url-state collapse round-trip rebuilt from SECTION_IDS + new structural check that index.html's `<section class="collapsible" id="sec-*">`/`.sec-head` markup matches SECTION_IDS exactly. 36 quals pass; verified live (all 8 headers correct incl. dynamic titles, collapse toggles + URL `x` round-trip, slider/charts intact inside the new structure, no console errors).

- 2026-06-16: Two UX features. (1) Real-time date slider: extracted renderWindowedViews() (the window-dependent renders: mpi chart, distribution, cards, per-helmer charts) from buildMonthlyViews; the slider's `input` event now re-slices the already-computed fullMonthSeries and re-renders those via requestAnimationFrame (coalesced), WITHOUT rebuilding the slider DOM / incident table / sanity / URL (those would interrupt the drag or thrash) — they commit on `change` (release). monthSeriesData() is NOT recomputed during drag. (2) Collapsible sections: the three headed sections (Incident browser, Prediction Markets, Sources and sanity checks) are wrapped in `<section class="collapsible" id="sec-{browser,markets,sanity}">` with a `.sec-head`/`.sec-body`; clicking the header toggles a CSS triangle (::before, so it survives buildBrowser's textContent updates) and `.collapsed`. Collapsed set is in the URL under new optional key `x` (e.g. x=browser.sanity), same pattern as `d`/`c`. The top month-panel dashboard is NOT collapsible (it has no <h2>; adding one would need Latin microcopy per rule 8 — left for human decision). url-state qual: unknown-key test moved from x=1 to z=1 (x is now valid) + added collapse round-trip and x=bogus invalid-value tests. 36 quals pass; both verified live (drag updates charts on next frame without touching URL until release; collapse round-trips through URL).

- 2026-06-15: Monthly faultfrac backfill complete. After the 2026-06-15 slurp added 190 unrated incidents (1 Tesla APR + 185 Waymo + 4 Zoox across MAR-MAY 2026), assessed all under the locked P(expert-avoids) criterion and appended to data/faultfrac.csv + patched incidents.js fault fields (via /tmp/apply_faults.py; no slurp re-run). fault-coverage qual green. Also: (a) de-magicked monthly-series qual — replaced hardcoded Tesla incTotal===17/11/8 with counts recomputed from INCIDENT_DATA (tests the integer/no-scaling invariant, self-updates each refresh), per human request; (b) appended " (Fully Aggregated)" / " (6-Month Trailing Window)" to the two distribution-chart titles (renderDistributionChart gained an optional titleSuffix param; pinned in stress-test qual); (c) fixed stress-test faultfrac-flip qual — Zoox is no longer zero-at-fault-mass (now 0.1), so the k=0->null branch is tested with a synthetic estimate instead of relying on Zoox. All 36 quals pass. Verified the @raines1220 Tesla VMT comparison: our 3-mo rolling all-accident rate tracks theirs within ~10% (matches at Dec-2025 window), running ~10-22% below their implied VMT for 2026 but within our error band (our upper VMT bounds exceed theirs).

- 2026-06-12 (later): atfaultInjury lo shares moved from legal-allocation 85% to expert-avoidability ~94% (matches faultfrac criterion; NHTSA critical-reason source already linked): HumansAV lo 300K→272K, HumansUS lo 920K→830K; his unchanged (50% legal-allocation floor). Phase 3 (Waymo-faithful mode) CLOSED per human 2026-06-12: the HumansAV/HumansUS cohort split plus the non-reproduction notes serve the purpose; no separate Waymo-faithful mode. Note the residual difference: Waymo's official method also does location-dynamic weighting and surface-street-only scoping, which the cohort split does not reproduce — documented, not implemented.
- 2026-06-12 (later): Prediction markets fixed + extended. Root cause of "not refreshing": refresh went through the api.codetabs.com CORS proxy which now returns 400, and per-event failures were silently swallowed, so the button re-rendered the stale 2026-04-04 snapshot. gamma-api now serves access-control-allow-origin:* so the proxy is gone (direct fetch). Auto-refresh on page load (snapshot renders instantly, live prices replace it); failures console.error and the age label only advances when ALL fetches succeed. Added MANIFOLD_SNAPSHOT (4 curated binary markets: tesla-serves-more-fully-autonomous, will-tesla-count-as-a-waymo-competi (dreev's own), tesla-robotaxi-service-atfault-acci, will-a-waymo-selfdriving-car-be-inv) fetched live from api.manifold.markets/v0/slug/ (open CORS), rendered with Ṁ mana volumes via fmtMana. Polymarket snapshot re-fetched fresh (2026-06-12). New qual prediction-markets.qual.mjs (snapshot integrity, per-market cards, fmtMana, no-proxy + auto-refresh source checks). HEADS-UP: 4 of 5 Polymarket events and the Waymo-fatality Manifold market resolve around June 30 / July 1, 2026 — disable or replace them after they resolve (per the data/polymarket.js header convention).

## Waymo benchmark reference (current)

- Cumulative Waymo VMT is pinned to Waymo's published anchors (compatible snapshots, not contradictions): 127M end-Sep-2025, 170.7M end-Dec-2025, 220.6M end-Mar-2026 (per-city PHX 80.6M / SF 67.1M / LA 51.8M / ATX 15.8M / ATL 5.4M). The Sep-24-2026 hub update gives 271.3M end-Jun-2026 (PHX 92.1M / SF Bay 82.4M / LA 67.1M / ATX 21.1M / ATL 8.6M), pinned 2026-09-25 as hub − Atlanta D + E (D = the double-listed Atlanta miles; see the 2026-09-25 (later 3) entry and data/README.md).
- AV-cities human benchmarks for injury/airbag/serious-injury+ come from the LIVE Waymo Safety Impact hub per-city rates (thru Jun 2026, five areas; Sep-24-2026 hub update, repinned 2026-09-25; first repinned 2026-08-22, superseding the Kusano & Scanlon 56.7M paper — same methodology family), pinned by human-benchmark-provenance.qual at three significant figures with a 20% geomean-vs-blended tolerance: per-city any-injury 1.95-6.64 (blended 3.77), airbag 1.27-2.83 (1.62), SSI+ 0.104-0.391 (0.213) IPMM (Waymo's own ADS rates 0.67 / 0.29 / 0.01). These are Waymo's DYNAMIC benchmarks (re-weighted to where Waymo drove), so they move every release. all-crashes uses the Kusano observed-to-Blincoe synthesis; fatality uses the Koopman/Piper fractional-death attribution (the /vehiclesInvolved is correct -- see the 2026-06-27 entry), whose fleet-sum equals fatal crashes; HumansUS fatality spans the FARS-2024 deaths..fatal-crashes numerators 84M-91M (2026-08-24, superseding the 59M/91M per-crashed-vehicle lo).
- Definitions: rider-only = miles with no human driver in Waymo's ride-hail cities; Waymo's published comparisons are surface-street and location-weighted (dynamic, intra-city); ~43% of SGO collisions are <1 mph delta-V.

## Waymo-alignment plan (closed)

Phases 1-2 done (sourcing + labels made honest). Phase 3 closed per human 2026-06-12: keep the cross-driver app with honest caveats, NO separate Waymo-faithful mode (the dynamic benchmark is Waymo-specific, not a universal human baseline for Tesla/Zoox). Phase 4 partially realized 2026-06-28 (injury/airbag/serious AV-cities bands now use Kusano 56.7M directly). Standing guardrails: don't reuse Waymo's dynamic benchmark as the shared human baseline for all drivers; don't strengthen provenance claims beyond the cited source.

- Resolved 2026-06-11: Tesla VMT rows include Dallas/Houston from 2026-04 (unsupervised D/H launch Apr 18, 2026 per Electrek), matching the incident-counting scope. 2026-05 rows added for all three drivers (held out of the app until NHTSA publishes May incidents). Waymo Jan–Apr 2026 re-anchored to the 200M milestone (crossed Feb 6–23) plus the co-CEO's >4M rider-only mi/week (late Mar 2026); old rows were ~10–15% above those anchors. New qual `vmt-master-fidelity` verifies data/vmt.csv flows unchanged into the rendered charts.
- Open wording nit: Tesla 2025 row rationales say "netted to empty driver-seat miles", but the numerator includes the driver-seat-monitor mode (NHTSA "In-Vehicle (Commercial / Test)") from Sep 2025 highway rides. Whether robotaxitracker's mileage model includes those highway miles is unverified; numbers left untouched.
- 2026-06-11 (later): repo-wide rename "driver" -> "helmer" (code identifiers, incidents.js `helmer` key, vmt.csv header `helmer`/`helmer_cumulative_vmt`, DOM ids/classes, UI labels, qual file per-helmer-summary). NHTSA's own field names ("Driver / Operator Type") and human prose ("human drivers") untouched. Old shared URLs (c=Humans..., s=driver) now fail loudly per anti-Postel.
- 2026-06-11 (later): Narrative redaction (CBI) table commented out per human decision: all narratives are 0% CBI now (Tesla un-redacted via update reports). New quals/cbi-watch.qual.mjs fails loudly if redaction returns, with re-enable instructions; matching qual blocks commented in sanity-values/sanity-checks (h3 count 11 -> 10). User-facing term is now "Helmsbeing" (internal jargon stays "helmer").
- 2026-06-11 (later): human benchmark split into two cohorts: HumansAV "Humans (AV cities)" (#c9a800, the prior bands; fatality band tightened to urban-only 87M-130M, the national 75M lo moved to the US cohort) and HumansUS "Humans (US average)" (#8a7400, new CRSS/FARS/Blincoe national bands; no hospitalization/airbag/seriousInjury bands - no national source). Stress test and ADS card multipliers compare against HumansAV. Qual updates: subset-chain generalized per cohort, stress-test scoped to ADS rows (mirrors the app's stress table domain), monthly-series and url-state updated for new ids/labels.
- 2026-06-11: VMT master moved from the Google Sheet into the repo at `data/vmt.csv` (verbatim migration, field-for-field identical to the sheet at migration time; sheet retired). `slurp.py` now reads it locally; `vmt-sheet-*.csv` snapshots are historical. VMT edits (e.g. the Dallas/Houston task above, or new months) now happen directly in `data/vmt.csv`.
- 2026-06-12: New defaults: helmer toggles HumansAV+Tesla+Waymo (HumansUS, Zoox off); default metric is at-fault (`defaultEnabled` moved from `all` to `atfault`; `primary` card emphasis stays on `all`). Both top charts (cross-helmer MPI + distribution) render a per-curve chip legend via shared `helmerChipLegend` (reuses month-legend/month-chip, existing helmer labels only); a qual asserts the two legends are byte-identical for the same state. New qual default-state.qual.mjs pins the defaults.
- 2026-06-12: At-fault human bands rederived for universe consistency (the prior 50–65% share came from the police-visible crash mix and was invalid at the SGO any-property-damage level, where marginal contacts are mostly self-inflicted and the share → ~1): HumansAV 160K–430K → 103K–430K, HumansUS 215K–600K → 140K–600K; lo now equals the all-crash lo by construction (pinned by a new subset-chain qual), hi unchanged (police-reported universe / 50% share). Default stays at-fault, NOT at-fault injury: in the default window, at-fault injury is k=0 for Tesla (95% CI 552K–5.4B) and Zoox, and only k=2.25 for Waymo — useless as a headline. At-fault injury bands (~85%/50% shares) left untouched; known residual: its lo share is legal-allocation-flavored while faultfrac is expert-avoidability (small, anti-AV direction). Diagnosis details: at-fault fault-mass in the window is 88% property-damage-only, ~60% fixed-object/parking-infrastructure contacts (chains, bollards, barrier arms, curbs) — events structurally absent from police/CRSS/Blincoe human data (Kusano & Scanlon 2024 acknowledge the threshold mismatch; Blincoe's survey-based adjustment filters out curb-scrape-grade events via "what counts as a crash" subjectivity).
- 2026-06-12: Cross-helmer chart error bars rebound to the month's 95% credible interval (was: posterior median under vmt_min/vmt_max — a VMT-only spread that read as a CI and was misleadingly tight for sparse k; the tooltip's "Range" was already the 95% CI). Bars are now capless (a cap at a clamped endpoint would assert a false CI boundary), drawn with clampY like every other layer (was raw mapY + clip-path, which made bars vanish entirely when a squished-to-top point's whole CI sat above the y-range — e.g. Tesla k=0 at-fault months with Waymo deselected), and fade with the same coverage-based opacity as their dots. Orphaned mpi-clip def removed; README explanatory note updated; qual in monthly-series pins one on-plot bar per dot in the Waymo-deselected at-fault view.
- 2026-06-12 (roadmap batch): (1) Latin dist-note above the distribution charts disclosing the pooled-window/constant-rate assumption (TODO English in buildMonthlyViews comment). (2) Trailing-6-month companion distribution chart next to the full-window one (skipped via one if when the selected window is ≤6 months — would duplicate exactly); clip-path id now derives from the window so the two SVGs don't collide. (3) Trend model deliberately NOT built: Tesla/Zoox k too sparse for a defensible per-helmer fit; the recent-window chart carries the honesty load. (4) Faultfrac sensitivity sub-table (Latin copy, same Sensitivity-analysis h3): faultFlipMultiplier finds the smallest multiplier on the judged at-fault mass that changes the stress verdict (Waymo loses "robustly safer" at ~3.75x undercount; Tesla goes robustly-worse at ~6.31x; Zoox k=0 unflippable); estimateMpiWindow now returns its vmts. (5) Tesla denominator scope RESOLVED: both trackers anchor to Tesla fleet-wide disclosures (no monitor-seat breakdown), so the miles include Sep-2025+ driver-seat-monitor highway rides — consistent with the None + In-Vehicle (Commercial/Test) numerator; the false "netted to empty driver-seat miles" rationale corrected in vmt.csv + vmt.js (8 rows, by hand in both, no slurp run) + README. (6) Error-bar glyph added to the 95% CI fan legend item (no new copy). (7) .month-err stroke 1.4→1, opacity 0.9→0.8.
- 2026-06-12: y-range rule generalized from a partial accumulation (`if (k !== 0 && covRatio > 0.99) yMax = max(...)` — which left yMax at its init value 1 when no month qualified, e.g. Tesla-only + fatality where every month is k=0: axis read 0..1, dots all pinned at "MPI=1", bars zero-length) to a total one: every plotted point contributes at least its median (dots always on-scale), informative points (k≠0, fully reported; humans' k=null counts) contribute their VMT-spread median. Same single decision point, no empty case. Default at-fault view's axis unchanged (6.7M). Qual in monthly-series renders Tesla-only fatality and asserts dots spread below the top edge with non-degenerate bars.
- 2026-06-12: k=0 months now render on the cross-helmer MPI chart via the Jeffreys posterior (removed the `incidentCount === 0` skip from lines/errs/marks; fan bands already rendered k=0). The yMax exclusion of k=0 and low-coverage months stays, so off-scale medians clamp to the plot top (e.g. Waymo 2026-04 at-fault: 2 incidents, both faultfrac 0, renders at ~3.3M MPI with 1% coverage). Qual in monthly-series counts "(0 incidents)" tooltips; its ElementStub now mirrors browser textContent→innerHTML escaping (escHtml round-trips through a div, so tooltips were invisible to the old stub).
